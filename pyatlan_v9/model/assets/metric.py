# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Metric asset model with flattened inheritance.

This module provides:
- Metric: Flat asset class (easy to use)
- MetricAttributes: Nested attributes struct (extends AssetAttributes)
- MetricNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .asset_related import RelatedAsset
from .sql_related import RelatedColumn

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Metric(Asset):
    """
    Base class for data quality metrics assets.
    """

    # Override type_name with Metric-specific default
    type_name: Union[str, UnsetType] = "Metric"

    metric_type: Union[str, None, UnsetType] = UNSET
    """Type of the metric."""

    metric_sql: Union[str, None, UnsetType] = UNSET
    """SQL query used to compute the metric."""

    metric_filters: Union[str, None, UnsetType] = UNSET
    """Filters to be applied to the metric query."""

    metric_time_grains: Union[list[str], None, UnsetType] = UNSET
    """List of time grains to be applied to the metric query."""

    dq_is_part_of_contract: Union[bool, None, UnsetType] = UNSET
    """Whether this data quality is part of contract (true) or not (false)."""

    assets: Union[list[RelatedAsset], None, UnsetType] = UNSET
    """"""

    metric_timestamp_column: Union[RelatedColumn, None, UnsetType] = UNSET
    """"""

    metric_dimension_columns: Union[list[RelatedColumn], None, UnsetType] = UNSET
    """"""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _metric_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(json_data: Union[str, bytes], serde: Serde | None = None) -> "Metric":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Metric instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _metric_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class MetricAttributes(AssetAttributes):
    """Metric-specific attributes for nested API format."""

    metric_type: Union[str, None, UnsetType] = UNSET
    """Type of the metric."""

    metric_sql: Union[str, None, UnsetType] = UNSET
    """SQL query used to compute the metric."""

    metric_filters: Union[str, None, UnsetType] = UNSET
    """Filters to be applied to the metric query."""

    metric_time_grains: Union[list[str], None, UnsetType] = UNSET
    """List of time grains to be applied to the metric query."""

    dq_is_part_of_contract: Union[bool, None, UnsetType] = UNSET
    """Whether this data quality is part of contract (true) or not (false)."""


class MetricRelationshipAttributes(AssetRelationshipAttributes):
    """Metric-specific relationship attributes for nested API format."""

    assets: Union[list[RelatedAsset], None, UnsetType] = UNSET
    """"""

    metric_timestamp_column: Union[RelatedColumn, None, UnsetType] = UNSET
    """"""

    metric_dimension_columns: Union[list[RelatedColumn], None, UnsetType] = UNSET
    """"""


class MetricNested(AssetNested):
    """Metric in nested API format for high-performance serialization."""

    attributes: Union[MetricAttributes, UnsetType] = UNSET
    relationship_attributes: Union[MetricRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[MetricRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[MetricRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _metric_to_nested(metric: Metric) -> MetricNested:
    """Convert flat Metric to nested format."""
    attrs = MetricAttributes(
        metric_type=metric.metric_type,
        metric_sql=metric.metric_sql,
        metric_filters=metric.metric_filters,
        metric_time_grains=metric.metric_time_grains,
        dq_is_part_of_contract=metric.dq_is_part_of_contract,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = [
        "assets",
        "metric_timestamp_column",
        "metric_dimension_columns",
    ]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        metric, rel_fields, MetricRelationshipAttributes
    )
    return MetricNested(
        guid=metric.guid,
        type_name=metric.type_name,
        status=metric.status,
        version=metric.version,
        create_time=metric.create_time,
        update_time=metric.update_time,
        created_by=metric.created_by,
        updated_by=metric.updated_by,
        classifications=metric.classifications,
        classification_names=metric.classification_names,
        meanings=metric.meanings,
        labels=metric.labels,
        business_attributes=metric.business_attributes,
        custom_attributes=metric.custom_attributes,
        pending_tasks=metric.pending_tasks,
        proxy=metric.proxy,
        is_incomplete=metric.is_incomplete,
        provenance_type=metric.provenance_type,
        home_id=metric.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _metric_from_nested(nested: MetricNested) -> Metric:
    """Convert nested format to flat Metric."""
    attrs = nested.attributes if nested.attributes is not UNSET else MetricAttributes()
    # Merge relationships from all three buckets
    rel_fields: list[str] = [
        "assets",
        "metric_timestamp_column",
        "metric_dimension_columns",
    ]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        MetricRelationshipAttributes,
    )
    return Metric(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        metric_type=attrs.metric_type,
        metric_sql=attrs.metric_sql,
        metric_filters=attrs.metric_filters,
        metric_time_grains=attrs.metric_time_grains,
        dq_is_part_of_contract=attrs.dq_is_part_of_contract,
        # Merged relationship attributes
        **merged_rels,
    )


def _metric_to_nested_bytes(metric: Metric, serde: Serde) -> bytes:
    """Convert flat Metric to nested JSON bytes."""
    return serde.encode(_metric_to_nested(metric))


def _metric_from_nested_bytes(data: bytes, serde: Serde) -> Metric:
    """Convert nested JSON bytes to flat Metric."""
    nested = serde.decode(data, MetricNested)
    return _metric_from_nested(nested)
