# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
DomoDataset asset model with flattened inheritance.

This module provides:
- DomoDataset: Flat asset class (easy to use)
- DomoDatasetAttributes: Nested attributes struct (extends AssetAttributes)
- DomoDatasetNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .domo_related import RelatedDomoCard, RelatedDomoDatasetColumn

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class DomoDataset(Asset):
    """
    Instance of a Domo Dataset in Atlan.
    """

    # Override type_name with DomoDataset-specific default
    type_name: Union[str, UnsetType] = "DomoDataset"

    domo_dataset_row_count: Union[int, None, UnsetType] = UNSET
    """Number of rows in the Domo dataset."""

    domo_dataset_column_count: Union[int, None, UnsetType] = UNSET
    """Number of columns in the Domo dataset."""

    domo_dataset_type: Union[str, None, UnsetType] = UNSET
    """Type of Domo dataset."""

    domo_dataset_card_count: Union[int, None, UnsetType] = UNSET
    """Number of cards linked to the Domo dataset."""

    domo_dataset_last_run: Union[str, None, UnsetType] = UNSET
    """An ISO-8601 representation of the time the DataSet was last run."""

    domo_id: Union[str, None, UnsetType] = UNSET
    """Id of the Domo dataset."""

    domo_owner_id: Union[str, None, UnsetType] = UNSET
    """Id of the owner of the Domo dataset."""

    domo_cards: Union[list[RelatedDomoCard], None, UnsetType] = UNSET
    """Domo Cards that are contained by this Domo Dataset."""

    domo_dataset_columns: Union[list[RelatedDomoDatasetColumn], None, UnsetType] = UNSET
    """Domo Dataset Columns that are contained by this Domo Dataset."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _domo_dataset_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "DomoDataset":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            DomoDataset instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _domo_dataset_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class DomoDatasetAttributes(AssetAttributes):
    """DomoDataset-specific attributes for nested API format."""

    domo_dataset_row_count: Union[int, None, UnsetType] = UNSET
    """Number of rows in the Domo dataset."""

    domo_dataset_column_count: Union[int, None, UnsetType] = UNSET
    """Number of columns in the Domo dataset."""

    domo_dataset_type: Union[str, None, UnsetType] = UNSET
    """Type of Domo dataset."""

    domo_dataset_card_count: Union[int, None, UnsetType] = UNSET
    """Number of cards linked to the Domo dataset."""

    domo_dataset_last_run: Union[str, None, UnsetType] = UNSET
    """An ISO-8601 representation of the time the DataSet was last run."""

    domo_id: Union[str, None, UnsetType] = UNSET
    """Id of the Domo dataset."""

    domo_owner_id: Union[str, None, UnsetType] = UNSET
    """Id of the owner of the Domo dataset."""


class DomoDatasetRelationshipAttributes(AssetRelationshipAttributes):
    """DomoDataset-specific relationship attributes for nested API format."""

    domo_cards: Union[list[RelatedDomoCard], None, UnsetType] = UNSET
    """Domo Cards that are contained by this Domo Dataset."""

    domo_dataset_columns: Union[list[RelatedDomoDatasetColumn], None, UnsetType] = UNSET
    """Domo Dataset Columns that are contained by this Domo Dataset."""


class DomoDatasetNested(AssetNested):
    """DomoDataset in nested API format for high-performance serialization."""

    attributes: Union[DomoDatasetAttributes, UnsetType] = UNSET
    relationship_attributes: Union[DomoDatasetRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[
        DomoDatasetRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        DomoDatasetRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _domo_dataset_to_nested(domo_dataset: DomoDataset) -> DomoDatasetNested:
    """Convert flat DomoDataset to nested format."""
    attrs = DomoDatasetAttributes(
        domo_dataset_row_count=domo_dataset.domo_dataset_row_count,
        domo_dataset_column_count=domo_dataset.domo_dataset_column_count,
        domo_dataset_type=domo_dataset.domo_dataset_type,
        domo_dataset_card_count=domo_dataset.domo_dataset_card_count,
        domo_dataset_last_run=domo_dataset.domo_dataset_last_run,
        domo_id=domo_dataset.domo_id,
        domo_owner_id=domo_dataset.domo_owner_id,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["domo_cards", "domo_dataset_columns"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        domo_dataset, rel_fields, DomoDatasetRelationshipAttributes
    )
    return DomoDatasetNested(
        guid=domo_dataset.guid,
        type_name=domo_dataset.type_name,
        status=domo_dataset.status,
        version=domo_dataset.version,
        create_time=domo_dataset.create_time,
        update_time=domo_dataset.update_time,
        created_by=domo_dataset.created_by,
        updated_by=domo_dataset.updated_by,
        classifications=domo_dataset.classifications,
        classification_names=domo_dataset.classification_names,
        meanings=domo_dataset.meanings,
        labels=domo_dataset.labels,
        business_attributes=domo_dataset.business_attributes,
        custom_attributes=domo_dataset.custom_attributes,
        pending_tasks=domo_dataset.pending_tasks,
        proxy=domo_dataset.proxy,
        is_incomplete=domo_dataset.is_incomplete,
        provenance_type=domo_dataset.provenance_type,
        home_id=domo_dataset.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _domo_dataset_from_nested(nested: DomoDatasetNested) -> DomoDataset:
    """Convert nested format to flat DomoDataset."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else DomoDatasetAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["domo_cards", "domo_dataset_columns"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        DomoDatasetRelationshipAttributes,
    )
    return DomoDataset(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        domo_dataset_row_count=attrs.domo_dataset_row_count,
        domo_dataset_column_count=attrs.domo_dataset_column_count,
        domo_dataset_type=attrs.domo_dataset_type,
        domo_dataset_card_count=attrs.domo_dataset_card_count,
        domo_dataset_last_run=attrs.domo_dataset_last_run,
        domo_id=attrs.domo_id,
        domo_owner_id=attrs.domo_owner_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _domo_dataset_to_nested_bytes(domo_dataset: DomoDataset, serde: Serde) -> bytes:
    """Convert flat DomoDataset to nested JSON bytes."""
    return serde.encode(_domo_dataset_to_nested(domo_dataset))


def _domo_dataset_from_nested_bytes(data: bytes, serde: Serde) -> DomoDataset:
    """Convert nested JSON bytes to flat DomoDataset."""
    nested = serde.decode(data, DomoDatasetNested)
    return _domo_dataset_from_nested(nested)
