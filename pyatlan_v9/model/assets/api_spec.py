# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
APISpec asset model with flattened inheritance.

This module provides:
- APISpec: Flat asset class (easy to use)
- APISpecAttributes: Nested attributes struct (extends AssetAttributes)
- APISpecNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .api_related import RelatedAPIPath
from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class APISpec(Asset):
    """
    Instance of an API specification in Atlan.
    """

    # Override type_name with APISpec-specific default
    type_name: Union[str, UnsetType] = "APISpec"

    api_spec_terms_of_service_url: Union[str, None, UnsetType] = UNSET
    """URL to the terms of service for the API specification."""

    api_spec_contact_email: Union[str, None, UnsetType] = UNSET
    """Email address for a contact responsible for the API specification."""

    api_spec_contact_name: Union[str, None, UnsetType] = UNSET
    """Name of the contact responsible for the API specification."""

    api_spec_contact_url: Union[str, None, UnsetType] = UNSET
    """URL pointing to the contact information."""

    api_spec_license_name: Union[str, None, UnsetType] = UNSET
    """Name of the license under which the API specification is available."""

    api_spec_license_url: Union[str, None, UnsetType] = UNSET
    """URL to the license under which the API specification is available."""

    api_spec_contract_version: Union[str, None, UnsetType] = UNSET
    """Version of the contract for the API specification."""

    api_spec_service_alias: Union[str, None, UnsetType] = UNSET
    """Service alias for the API specification."""

    api_spec_type: Union[str, None, UnsetType] = UNSET
    """Type of API, for example: OpenAPI, GraphQL, etc."""

    api_spec_version: Union[str, None, UnsetType] = UNSET
    """Version of the API specification."""

    api_spec_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the API spec, if this asset is contained in an API spec."""

    api_spec_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the API spec, if this asset is contained in an API spec."""

    api_external_docs: Union[dict[str, str], None, UnsetType] = UNSET
    """External documentation of the API."""

    api_is_auth_optional: Union[bool, None, UnsetType] = UNSET
    """Whether authentication is optional (true) or required (false)."""

    api_is_object_reference: Union[bool, None, UnsetType] = UNSET
    """If this asset refers to an APIObject"""

    api_object_qualified_name: Union[str, None, UnsetType] = UNSET
    """Qualified name of the APIObject that is referred to by this asset. When apiIsObjectReference is true."""

    api_paths: Union[list[RelatedAPIPath], None, UnsetType] = UNSET
    """Paths that exist within this API specification."""

    # =========================================================================
    # Convenience Methods
    # =========================================================================

    @classmethod
    @init_guid
    def creator(cls, *, name: str, connection_qualified_name: str) -> "APISpec":
        """Create a new APISpec asset."""
        validate_required_fields(
            ["name", "connection_qualified_name"], [name, connection_qualified_name]
        )
        fields = connection_qualified_name.split("/")
        connector_name = fields[1] if len(fields) > 1 else None
        return cls(
            name=name,
            qualified_name=f"{connection_qualified_name}/{name}",
            connection_qualified_name=connection_qualified_name,
            connector_name=connector_name,
        )

    @classmethod
    def updater(cls, *, qualified_name: str, name: str) -> "APISpec":
        """Create an APISpec instance for update operations."""
        validate_required_fields(["qualified_name", "name"], [qualified_name, name])
        return cls(qualified_name=qualified_name, name=name)

    def trim_to_required(self) -> "APISpec":
        """Return only fields required for update operations."""
        return APISpec.updater(qualified_name=self.qualified_name, name=self.name)

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _api_spec_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "APISpec":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            APISpec instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _api_spec_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class APISpecAttributes(AssetAttributes):
    """APISpec-specific attributes for nested API format."""

    api_spec_terms_of_service_url: Union[str, None, UnsetType] = UNSET
    """URL to the terms of service for the API specification."""

    api_spec_contact_email: Union[str, None, UnsetType] = UNSET
    """Email address for a contact responsible for the API specification."""

    api_spec_contact_name: Union[str, None, UnsetType] = UNSET
    """Name of the contact responsible for the API specification."""

    api_spec_contact_url: Union[str, None, UnsetType] = UNSET
    """URL pointing to the contact information."""

    api_spec_license_name: Union[str, None, UnsetType] = UNSET
    """Name of the license under which the API specification is available."""

    api_spec_license_url: Union[str, None, UnsetType] = UNSET
    """URL to the license under which the API specification is available."""

    api_spec_contract_version: Union[str, None, UnsetType] = UNSET
    """Version of the contract for the API specification."""

    api_spec_service_alias: Union[str, None, UnsetType] = UNSET
    """Service alias for the API specification."""

    api_spec_type: Union[str, None, UnsetType] = UNSET
    """Type of API, for example: OpenAPI, GraphQL, etc."""

    api_spec_version: Union[str, None, UnsetType] = UNSET
    """Version of the API specification."""

    api_spec_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the API spec, if this asset is contained in an API spec."""

    api_spec_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the API spec, if this asset is contained in an API spec."""

    api_external_docs: Union[dict[str, str], None, UnsetType] = UNSET
    """External documentation of the API."""

    api_is_auth_optional: Union[bool, None, UnsetType] = UNSET
    """Whether authentication is optional (true) or required (false)."""

    api_is_object_reference: Union[bool, None, UnsetType] = UNSET
    """If this asset refers to an APIObject"""

    api_object_qualified_name: Union[str, None, UnsetType] = UNSET
    """Qualified name of the APIObject that is referred to by this asset. When apiIsObjectReference is true."""


class APISpecRelationshipAttributes(AssetRelationshipAttributes):
    """APISpec-specific relationship attributes for nested API format."""

    api_paths: Union[list[RelatedAPIPath], None, UnsetType] = UNSET
    """Paths that exist within this API specification."""


class APISpecNested(AssetNested):
    """APISpec in nested API format for high-performance serialization."""

    attributes: Union[APISpecAttributes, UnsetType] = UNSET
    relationship_attributes: Union[APISpecRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[APISpecRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[APISpecRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _api_spec_to_nested(api_spec: APISpec) -> APISpecNested:
    """Convert flat APISpec to nested format."""
    attrs_kwargs = build_attributes_kwargs(api_spec, APISpecAttributes)
    attrs = APISpecAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["api_paths"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        api_spec, rel_fields, APISpecRelationshipAttributes
    )
    return APISpecNested(
        guid=api_spec.guid,
        type_name=api_spec.type_name,
        status=api_spec.status,
        version=api_spec.version,
        create_time=api_spec.create_time,
        update_time=api_spec.update_time,
        created_by=api_spec.created_by,
        updated_by=api_spec.updated_by,
        classifications=api_spec.classifications,
        classification_names=api_spec.classification_names,
        meanings=api_spec.meanings,
        labels=api_spec.labels,
        business_attributes=api_spec.business_attributes,
        custom_attributes=api_spec.custom_attributes,
        pending_tasks=api_spec.pending_tasks,
        proxy=api_spec.proxy,
        is_incomplete=api_spec.is_incomplete,
        provenance_type=api_spec.provenance_type,
        home_id=api_spec.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _api_spec_from_nested(nested: APISpecNested) -> APISpec:
    """Convert nested format to flat APISpec."""
    attrs = nested.attributes if nested.attributes is not UNSET else APISpecAttributes()
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["api_paths"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        APISpecRelationshipAttributes,
    )
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, APISpecNested, APISpecAttributes
    )
    return APISpec(**kwargs)


def _api_spec_to_nested_bytes(api_spec: APISpec, serde: Serde) -> bytes:
    """Convert flat APISpec to nested JSON bytes."""
    return serde.encode(_api_spec_to_nested(api_spec))


def _api_spec_from_nested_bytes(data: bytes, serde: Serde) -> APISpec:
    """Convert nested JSON bytes to flat APISpec."""
    nested = serde.decode(data, APISpecNested)
    return _api_spec_from_nested(nested)
