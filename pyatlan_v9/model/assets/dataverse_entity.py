# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
DataverseEntity asset model with flattened inheritance.

This module provides:
- DataverseEntity: Flat asset class (easy to use)
- DataverseEntityAttributes: Nested attributes struct (extends AssetAttributes)
- DataverseEntityNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .dataverse_related import RelatedDataverseAttribute

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class DataverseEntity(Asset):
    """
    Instances of DataverseEntity in Atlan.
    """

    # Override type_name with DataverseEntity-specific default
    type_name: Union[str, UnsetType] = "DataverseEntity"

    dataverse_entity_schema_name: Union[str, None, UnsetType] = UNSET
    """Schema Name of the DataverseEntity."""

    dataverse_entity_table_type: Union[str, None, UnsetType] = UNSET
    """Table Type of the DataverseEntity."""

    dataverse_is_custom: Union[bool, None, UnsetType] = UNSET
    """Indicator if DataverseEntity is custom built."""

    dataverse_is_customizable: Union[bool, None, UnsetType] = UNSET
    """Indicator if DataverseEntity is customizable."""

    dataverse_is_audit_enabled: Union[bool, None, UnsetType] = UNSET
    """Indicator if DataverseEntity has auditing enabled."""

    dataverse_attributes: Union[list[RelatedDataverseAttribute], None, UnsetType] = (
        UNSET
    )
    """DataverseAttribute assets contained within this DataverseEntity."""

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        name: str,
        connection_qualified_name: str,
    ) -> "DataverseEntity":
        """Create a new DataverseEntity asset."""
        validate_required_fields(
            ["name", "connection_qualified_name"], [name, connection_qualified_name]
        )
        connector_name = (
            connection_qualified_name.split("/")[1]
            if len(connection_qualified_name.split("/")) > 1
            else ""
        )
        return cls(
            name=name,
            qualified_name=f"{connection_qualified_name}/{name}",
            connection_qualified_name=connection_qualified_name,
            connector_name=connector_name,
        )

    @classmethod
    def updater(cls, *, qualified_name: str, name: str) -> "DataverseEntity":
        """Create a DataverseEntity instance for update operations."""
        validate_required_fields(["qualified_name", "name"], [qualified_name, name])
        return cls(qualified_name=qualified_name, name=name)

    def trim_to_required(self) -> "DataverseEntity":
        """Return only fields required for update operations."""
        return DataverseEntity.updater(
            qualified_name=self.qualified_name, name=self.name
        )

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _dataverse_entity_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "DataverseEntity":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            DataverseEntity instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _dataverse_entity_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class DataverseEntityAttributes(AssetAttributes):
    """DataverseEntity-specific attributes for nested API format."""

    dataverse_entity_schema_name: Union[str, None, UnsetType] = UNSET
    """Schema Name of the DataverseEntity."""

    dataverse_entity_table_type: Union[str, None, UnsetType] = UNSET
    """Table Type of the DataverseEntity."""

    dataverse_is_custom: Union[bool, None, UnsetType] = UNSET
    """Indicator if DataverseEntity is custom built."""

    dataverse_is_customizable: Union[bool, None, UnsetType] = UNSET
    """Indicator if DataverseEntity is customizable."""

    dataverse_is_audit_enabled: Union[bool, None, UnsetType] = UNSET
    """Indicator if DataverseEntity has auditing enabled."""


class DataverseEntityRelationshipAttributes(AssetRelationshipAttributes):
    """DataverseEntity-specific relationship attributes for nested API format."""

    dataverse_attributes: Union[list[RelatedDataverseAttribute], None, UnsetType] = (
        UNSET
    )
    """DataverseAttribute assets contained within this DataverseEntity."""


class DataverseEntityNested(AssetNested):
    """DataverseEntity in nested API format for high-performance serialization."""

    attributes: Union[DataverseEntityAttributes, UnsetType] = UNSET
    relationship_attributes: Union[DataverseEntityRelationshipAttributes, UnsetType] = (
        UNSET
    )
    append_relationship_attributes: Union[
        DataverseEntityRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        DataverseEntityRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _dataverse_entity_to_nested(
    dataverse_entity: DataverseEntity,
) -> DataverseEntityNested:
    """Convert flat DataverseEntity to nested format."""
    attrs_kwargs = build_attributes_kwargs(dataverse_entity, DataverseEntityAttributes)
    attrs = DataverseEntityAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["dataverse_attributes"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        dataverse_entity, rel_fields, DataverseEntityRelationshipAttributes
    )
    return DataverseEntityNested(
        guid=dataverse_entity.guid,
        type_name=dataverse_entity.type_name,
        status=dataverse_entity.status,
        version=dataverse_entity.version,
        create_time=dataverse_entity.create_time,
        update_time=dataverse_entity.update_time,
        created_by=dataverse_entity.created_by,
        updated_by=dataverse_entity.updated_by,
        classifications=dataverse_entity.classifications,
        classification_names=dataverse_entity.classification_names,
        meanings=dataverse_entity.meanings,
        labels=dataverse_entity.labels,
        business_attributes=dataverse_entity.business_attributes,
        custom_attributes=dataverse_entity.custom_attributes,
        pending_tasks=dataverse_entity.pending_tasks,
        proxy=dataverse_entity.proxy,
        is_incomplete=dataverse_entity.is_incomplete,
        provenance_type=dataverse_entity.provenance_type,
        home_id=dataverse_entity.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _dataverse_entity_from_nested(nested: DataverseEntityNested) -> DataverseEntity:
    """Convert nested format to flat DataverseEntity."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else DataverseEntityAttributes()
    )
    rel_fields: list[str] = ["dataverse_attributes"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        DataverseEntityRelationshipAttributes,
    )
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, AssetNested, DataverseEntityAttributes
    )
    return DataverseEntity(**kwargs)


def _dataverse_entity_to_nested_bytes(
    dataverse_entity: DataverseEntity, serde: Serde
) -> bytes:
    """Convert flat DataverseEntity to nested JSON bytes."""
    return serde.encode(_dataverse_entity_to_nested(dataverse_entity))


def _dataverse_entity_from_nested_bytes(data: bytes, serde: Serde) -> DataverseEntity:
    """Convert nested JSON bytes to flat DataverseEntity."""
    nested = serde.decode(data, DataverseEntityNested)
    return _dataverse_entity_from_nested(nested)
