# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
APIPath asset model with flattened inheritance.

This module provides:
- APIPath: Flat asset class (easy to use)
- APIPathAttributes: Nested attributes struct (extends AssetAttributes)
- APIPathNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .api_related import RelatedAPISpec
from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class APIPath(Asset):
    """
    Instance of an API path that could contain one or more endpoints in Atlan.
    """

    # Override type_name with APIPath-specific default
    type_name: Union[str, UnsetType] = "APIPath"

    api_path_summary: Union[str, None, UnsetType] = UNSET
    """Descriptive summary intended to apply to all operations in this path."""

    api_path_raw_uri: Union[str, None, UnsetType] = UNSET
    """Absolute path to an individual endpoint."""

    api_path_is_templated: Union[bool, None, UnsetType] = UNSET
    """Whether the endpoint's path contains replaceable parameters (true) or not (false)."""

    api_path_available_operations: Union[list[str], None, UnsetType] = UNSET
    """List of the operations available on the endpoint."""

    api_path_available_response_codes: Union[dict[str, str], None, UnsetType] = UNSET
    """Response codes available on the path across all operations."""

    api_path_is_ingress_exposed: Union[bool, None, UnsetType] = UNSET
    """Whether the path is exposed as an ingress (true) or not (false)."""

    api_spec_type: Union[str, None, UnsetType] = UNSET
    """Type of API, for example: OpenAPI, GraphQL, etc."""

    api_spec_version: Union[str, None, UnsetType] = UNSET
    """Version of the API specification."""

    api_spec_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the API spec, if this asset is contained in an API spec."""

    api_spec_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the API spec, if this asset is contained in an API spec."""

    api_external_docs: Union[dict[str, str], None, UnsetType] = UNSET
    """External documentation of the API."""

    api_is_auth_optional: Union[bool, None, UnsetType] = UNSET
    """Whether authentication is optional (true) or required (false)."""

    api_is_object_reference: Union[bool, None, UnsetType] = UNSET
    """If this asset refers to an APIObject"""

    api_object_qualified_name: Union[str, None, UnsetType] = UNSET
    """Qualified name of the APIObject that is referred to by this asset. When apiIsObjectReference is true."""

    api_spec: Union[RelatedAPISpec, None, UnsetType] = UNSET
    """API specification in which this path exists."""

    @property
    def api_path_raw_u_r_i(self) -> Union[str, None, UnsetType]:
        """Alias for api_path_raw_uri to preserve SDK field parity."""
        return self.api_path_raw_uri

    @api_path_raw_u_r_i.setter
    def api_path_raw_u_r_i(self, value: Union[str, None, UnsetType]) -> None:
        """Alias setter for api_path_raw_uri."""
        self.api_path_raw_uri = value

    # =========================================================================
    # Convenience Methods
    # =========================================================================

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        path_raw_uri: str,
        spec_qualified_name: str,
        connection_qualified_name: Union[str, None] = None,
    ) -> "APIPath":
        """Create a new APIPath asset."""
        validate_required_fields(
            ["path_raw_uri", "spec_qualified_name"], [path_raw_uri, spec_qualified_name]
        )
        if connection_qualified_name:
            connection_qn = connection_qualified_name
        else:
            spec_parts = spec_qualified_name.split("/")
            connection_qn = (
                "/".join(spec_parts[:3])
                if len(spec_parts) >= 3
                else spec_qualified_name
            )
        conn_parts = connection_qn.split("/")
        connector_name = conn_parts[1] if len(conn_parts) > 1 else None
        return cls(
            name=path_raw_uri,
            qualified_name=f"{spec_qualified_name}{path_raw_uri}",
            api_path_raw_uri=path_raw_uri,
            api_spec_qualified_name=spec_qualified_name,
            connection_qualified_name=connection_qn,
            connector_name=connector_name,
            api_spec=RelatedAPISpec(
                unique_attributes={"qualifiedName": spec_qualified_name}
            ),
        )

    @classmethod
    def updater(cls, *, qualified_name: str, name: str) -> "APIPath":
        """Create an APIPath instance for update operations."""
        validate_required_fields(["qualified_name", "name"], [qualified_name, name])
        return cls(qualified_name=qualified_name, name=name)

    def trim_to_required(self) -> "APIPath":
        """Return only fields required for update operations."""
        return APIPath.updater(qualified_name=self.qualified_name, name=self.name)

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _api_path_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "APIPath":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            APIPath instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _api_path_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class APIPathAttributes(AssetAttributes):
    """APIPath-specific attributes for nested API format."""

    api_path_summary: Union[str, None, UnsetType] = UNSET
    """Descriptive summary intended to apply to all operations in this path."""

    api_path_raw_uri: Union[str, None, UnsetType] = UNSET
    """Absolute path to an individual endpoint."""

    api_path_is_templated: Union[bool, None, UnsetType] = UNSET
    """Whether the endpoint's path contains replaceable parameters (true) or not (false)."""

    api_path_available_operations: Union[list[str], None, UnsetType] = UNSET
    """List of the operations available on the endpoint."""

    api_path_available_response_codes: Union[dict[str, str], None, UnsetType] = UNSET
    """Response codes available on the path across all operations."""

    api_path_is_ingress_exposed: Union[bool, None, UnsetType] = UNSET
    """Whether the path is exposed as an ingress (true) or not (false)."""

    api_spec_type: Union[str, None, UnsetType] = UNSET
    """Type of API, for example: OpenAPI, GraphQL, etc."""

    api_spec_version: Union[str, None, UnsetType] = UNSET
    """Version of the API specification."""

    api_spec_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the API spec, if this asset is contained in an API spec."""

    api_spec_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the API spec, if this asset is contained in an API spec."""

    api_external_docs: Union[dict[str, str], None, UnsetType] = UNSET
    """External documentation of the API."""

    api_is_auth_optional: Union[bool, None, UnsetType] = UNSET
    """Whether authentication is optional (true) or required (false)."""

    api_is_object_reference: Union[bool, None, UnsetType] = UNSET
    """If this asset refers to an APIObject"""

    api_object_qualified_name: Union[str, None, UnsetType] = UNSET
    """Qualified name of the APIObject that is referred to by this asset. When apiIsObjectReference is true."""


class APIPathRelationshipAttributes(AssetRelationshipAttributes):
    """APIPath-specific relationship attributes for nested API format."""

    api_spec: Union[RelatedAPISpec, None, UnsetType] = UNSET
    """API specification in which this path exists."""


class APIPathNested(AssetNested):
    """APIPath in nested API format for high-performance serialization."""

    attributes: Union[APIPathAttributes, UnsetType] = UNSET
    relationship_attributes: Union[APIPathRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[APIPathRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[APIPathRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _api_path_to_nested(api_path: APIPath) -> APIPathNested:
    """Convert flat APIPath to nested format."""
    attrs_kwargs = build_attributes_kwargs(api_path, APIPathAttributes)
    attrs = APIPathAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["api_spec"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        api_path, rel_fields, APIPathRelationshipAttributes
    )
    return APIPathNested(
        guid=api_path.guid,
        type_name=api_path.type_name,
        status=api_path.status,
        version=api_path.version,
        create_time=api_path.create_time,
        update_time=api_path.update_time,
        created_by=api_path.created_by,
        updated_by=api_path.updated_by,
        classifications=api_path.classifications,
        classification_names=api_path.classification_names,
        meanings=api_path.meanings,
        labels=api_path.labels,
        business_attributes=api_path.business_attributes,
        custom_attributes=api_path.custom_attributes,
        pending_tasks=api_path.pending_tasks,
        proxy=api_path.proxy,
        is_incomplete=api_path.is_incomplete,
        provenance_type=api_path.provenance_type,
        home_id=api_path.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _api_path_from_nested(nested: APIPathNested) -> APIPath:
    """Convert nested format to flat APIPath."""
    attrs = nested.attributes if nested.attributes is not UNSET else APIPathAttributes()
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["api_spec"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        APIPathRelationshipAttributes,
    )
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, APIPathNested, APIPathAttributes
    )
    return APIPath(**kwargs)


def _api_path_to_nested_bytes(api_path: APIPath, serde: Serde) -> bytes:
    """Convert flat APIPath to nested JSON bytes."""
    return serde.encode(_api_path_to_nested(api_path))


def _api_path_from_nested_bytes(data: bytes, serde: Serde) -> APIPath:
    """Convert nested JSON bytes to flat APIPath."""
    nested = serde.decode(data, APIPathNested)
    return _api_path_from_nested(nested)
