# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# ruff: noqa: ARG002
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Folder asset model with flattened inheritance.

This module provides:
- Folder: Flat asset class (easy to use)
- FolderAttributes: Nested attributes struct (extends AssetAttributes)
- FolderNested: Nested API format struct
"""

from __future__ import annotations

import re
from typing import Any, ClassVar, List, Union

from msgspec import UNSET, UnsetType

from .anomalo_related import RelatedAnomaloCheck
from .app_related import RelatedApplication, RelatedApplicationField
from .asset import (
    _ASSET_REL_FIELDS,
    Asset,
    AssetAttributes,
    AssetNested,
    AssetRelationshipAttributes,
    _extract_asset_attrs,
    _populate_asset_attrs,
)
from .data_contract_related import RelatedDataContract
from .data_mesh_related import RelatedDataProduct
from .data_quality_related import RelatedDataQualityRule, RelatedMetric
from .gcp_dataplex_related import RelatedGCPDataplexAspectType
from .gtc_related import RelatedAtlasGlossaryTerm
from .monte_carlo_related import RelatedMCIncident, RelatedMCMonitor
from .referenceable_related import RelatedReferenceable
from .resource_related import RelatedFile, RelatedLink, RelatedReadme
from .schema_registry_related import RelatedSchemaRegistrySubject
from .soda_related import RelatedSodaCheck
from .sql_related import RelatedQuery
from pyatlan.utils import validate_required_fields
from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset

from .namespace_related import RelatedFolder, RelatedNamespace

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Folder(Asset):
    """
    Instance of a folder within a query collection in Atlan.
    """

    PARENT_QUALIFIED_NAME: ClassVar[Any] = None
    COLLECTION_QUALIFIED_NAME: ClassVar[Any] = None
    ANOMALO_CHECKS: ClassVar[Any] = None
    APPLICATION: ClassVar[Any] = None
    APPLICATION_FIELD: ClassVar[Any] = None
    DATA_CONTRACT_LATEST: ClassVar[Any] = None
    DATA_CONTRACT_LATEST_CERTIFIED: ClassVar[Any] = None
    OUTPUT_PORT_DATA_PRODUCTS: ClassVar[Any] = None
    INPUT_PORT_DATA_PRODUCTS: ClassVar[Any] = None
    METRICS: ClassVar[Any] = None
    DQ_BASE_DATASET_RULES: ClassVar[Any] = None
    DQ_REFERENCE_DATASET_RULES: ClassVar[Any] = None
    GCP_DATAPLEX_ASPECT_TYPE_METADATA_ENTITIES: ClassVar[Any] = None
    MEANINGS: ClassVar[Any] = None
    MC_MONITORS: ClassVar[Any] = None
    MC_INCIDENTS: ClassVar[Any] = None
    CHILDREN_FOLDERS: ClassVar[Any] = None
    PARENT: ClassVar[Any] = None
    USER_DEF_RELATIONSHIP_TO: ClassVar[Any] = None
    USER_DEF_RELATIONSHIP_FROM: ClassVar[Any] = None
    FILES: ClassVar[Any] = None
    LINKS: ClassVar[Any] = None
    README: ClassVar[Any] = None
    CHILDREN_QUERIES: ClassVar[Any] = None
    SCHEMA_REGISTRY_SUBJECTS: ClassVar[Any] = None
    SODA_CHECKS: ClassVar[Any] = None

    parent_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the parent folder or collection in which this folder exists."""

    collection_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the collection in which this folder exists."""

    anomalo_checks: Union[List[RelatedAnomaloCheck], None, UnsetType] = UNSET
    """Checks that run on this asset."""

    application: Union[RelatedApplication, None, UnsetType] = UNSET
    """Application owning the Asset."""

    application_field: Union[RelatedApplicationField, None, UnsetType] = UNSET
    """ApplicationField owning the Asset."""

    data_contract_latest: Union[RelatedDataContract, None, UnsetType] = UNSET
    """Latest version of the data contract (in any status) for this asset."""

    data_contract_latest_certified: Union[RelatedDataContract, None, UnsetType] = UNSET
    """Latest certified version of the data contract for this asset."""

    output_port_data_products: Union[List[RelatedDataProduct], None, UnsetType] = UNSET
    """Data products for which this asset is an output port."""

    input_port_data_products: Union[List[RelatedDataProduct], None, UnsetType] = UNSET
    """Data products for which this asset is an input port."""

    metrics: Union[List[RelatedMetric], None, UnsetType] = UNSET
    """"""

    dq_base_dataset_rules: Union[List[RelatedDataQualityRule], None, UnsetType] = UNSET
    """Rules that are applied on this dataset."""

    dq_reference_dataset_rules: Union[List[RelatedDataQualityRule], None, UnsetType] = (
        UNSET
    )
    """Rules where this dataset is referenced."""

    gcp_dataplex_aspect_type_metadata_entities: Union[
        List[RelatedGCPDataplexAspectType], None, UnsetType
    ] = UNSET
    """Dataplex entries (assets) that have aspects of this Aspect Type attached."""

    meanings: Union[List[RelatedAtlasGlossaryTerm], None, UnsetType] = UNSET
    """Glossary terms that are linked to this asset."""

    mc_monitors: Union[List[RelatedMCMonitor], None, UnsetType] = UNSET
    """Monitors that observe this asset."""

    mc_incidents: Union[List[RelatedMCIncident], None, UnsetType] = UNSET
    """"""

    children_folders: Union[List[RelatedFolder], None, UnsetType] = UNSET
    """Folders that exist within this namespace."""

    parent: Union[RelatedNamespace, None, UnsetType] = UNSET
    """Namespace in which this folder exists."""

    user_def_relationship_to: Union[List[RelatedReferenceable], None, UnsetType] = UNSET
    """"""

    user_def_relationship_from: Union[List[RelatedReferenceable], None, UnsetType] = (
        UNSET
    )
    """"""

    files: Union[List[RelatedFile], None, UnsetType] = UNSET
    """"""

    links: Union[List[RelatedLink], None, UnsetType] = UNSET
    """Links that are attached to this asset."""

    readme: Union[RelatedReadme, None, UnsetType] = UNSET
    """README that is linked to this asset."""

    children_queries: Union[List[RelatedQuery], None, UnsetType] = UNSET
    """Queries that exist within this namespace."""

    schema_registry_subjects: Union[
        List[RelatedSchemaRegistrySubject], None, UnsetType
    ] = UNSET
    """Schema registry subjects associated with this asset."""

    soda_checks: Union[List[RelatedSodaCheck], None, UnsetType] = UNSET
    """"""

    def __post_init__(self) -> None:
        self.type_name = "Folder"

    # =========================================================================
    # SDK Methods
    # =========================================================================

    _QUALIFIED_NAME_PATTERN: ClassVar[re.Pattern] = re.compile(r"^.+/[^/]+/[^/]+$")

    def validate(self, for_creation: bool = False) -> None:
        """
        Dry-run validation of this Folder instance.

        Checks that required fields (type_name, name, qualified_name) are set.
        When ``for_creation=True``, also checks hierarchy-specific fields
        (parent references, denormalized attributes) needed to create this asset.

        This is purely opt-in and is NOT called by any serde path — only by
        explicit user invocation (e.g., validating JSONL before sending to Atlan).

        Args:
            for_creation: If True, also validate fields required for asset creation.

        Raises:
            ValueError: If any required fields are missing or invalid.
        """
        errors: list[str] = []
        if self.type_name is UNSET:
            errors.append("type_name is required")
        if self.name is UNSET:
            errors.append("name is required")
        if self.qualified_name is UNSET or self.qualified_name is None:
            errors.append("qualified_name is required")
        elif not self._QUALIFIED_NAME_PATTERN.match(self.qualified_name):
            errors.append(
                f"qualified_name '{self.qualified_name}' does not match expected "
                f"pattern: {self._QUALIFIED_NAME_PATTERN.pattern}"
            )
        if for_creation:
            if self.connection_qualified_name is UNSET:
                errors.append("connection_qualified_name is required for creation")
            if self.parent is UNSET:
                errors.append("parent is required for creation")
        if errors:
            raise ValueError(f"Folder validation failed: {errors}")

    def minimize(self) -> "Folder":
        """
        Return a minimal copy of this Folder with only updater-required fields.

        Calls :meth:`validate` first to ensure the instance is valid, then
        returns a new Folder with only the fields needed for an update
        (qualified_name, name, and any type-specific additional fields).

        Returns:
            A new Folder instance with only the minimum required fields.
        """
        self.validate()
        return Folder(qualified_name=self.qualified_name, name=self.name)

    def relate(self) -> "RelatedFolder":
        """
        Create a :class:`RelatedFolder` reference from this instance.

        Returns a lightweight reference suitable for use in relationship
        attributes. Prefers ``guid`` if set, otherwise falls back to
        ``qualified_name``.

        Returns:
            A RelatedFolder reference to this asset.
        """
        if self.guid is not UNSET:
            return RelatedFolder(guid=self.guid)
        return RelatedFolder(qualified_name=self.qualified_name)

    @classmethod
    def creator(
        cls,
        *,
        name: str,
        collection_qualified_name: str | None = None,
        parent_folder_qualified_name: str | None = None,
    ) -> "Folder":

        validate_required_fields(["name"], [name])
        if not (parent_folder_qualified_name or collection_qualified_name):
            raise ValueError(
                "Either 'collection_qualified_name' or 'parent_folder_qualified_name' must be specified."
            )

        if not parent_folder_qualified_name:
            qualified_name = f"{collection_qualified_name}/{name}"
            parent_qn = collection_qualified_name
            from pyatlan_v9.model.assets import Collection

            parent_ref = Collection.ref_by_qualified_name(
                collection_qualified_name or ""
            )
        else:
            tokens = parent_folder_qualified_name.split("/")
            if len(tokens) < 4:
                raise ValueError("Invalid parent_folder_qualified_name")
            collection_qualified_name = (
                f"{tokens[0]}/{tokens[1]}/{tokens[2]}/{tokens[3]}"
            )
            qualified_name = f"{parent_folder_qualified_name}/{name}"
            parent_qn = parent_folder_qualified_name
            parent_ref = Folder.ref_by_qualified_name(parent_folder_qualified_name)

        return Folder(
            name=name,
            qualified_name=qualified_name,
            collection_qualified_name=collection_qualified_name,
            parent=parent_ref,
            parent_qualified_name=parent_qn,
        )

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return self.to_nested_bytes(serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    def to_nested_bytes(self, serde: Serde | None = None) -> bytes:
        """Serialize to Atlas nested-format JSON bytes (pure msgspec, no dict intermediate)."""
        if serde is None:
            serde = get_serde()
        return _folder_to_nested_bytes(self, serde)

    @staticmethod
    def from_json(json_data: str | bytes, serde: Serde | None = None) -> Folder:
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Folder instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _folder_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class FolderAttributes(AssetAttributes):
    """Folder-specific attributes for nested API format."""

    parent_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the parent folder or collection in which this folder exists."""

    collection_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the collection in which this folder exists."""


class FolderRelationshipAttributes(AssetRelationshipAttributes):
    """Folder-specific relationship attributes for nested API format."""

    anomalo_checks: Union[List[RelatedAnomaloCheck], None, UnsetType] = UNSET
    """Checks that run on this asset."""

    application: Union[RelatedApplication, None, UnsetType] = UNSET
    """Application owning the Asset."""

    application_field: Union[RelatedApplicationField, None, UnsetType] = UNSET
    """ApplicationField owning the Asset."""

    data_contract_latest: Union[RelatedDataContract, None, UnsetType] = UNSET
    """Latest version of the data contract (in any status) for this asset."""

    data_contract_latest_certified: Union[RelatedDataContract, None, UnsetType] = UNSET
    """Latest certified version of the data contract for this asset."""

    output_port_data_products: Union[List[RelatedDataProduct], None, UnsetType] = UNSET
    """Data products for which this asset is an output port."""

    input_port_data_products: Union[List[RelatedDataProduct], None, UnsetType] = UNSET
    """Data products for which this asset is an input port."""

    metrics: Union[List[RelatedMetric], None, UnsetType] = UNSET
    """"""

    dq_base_dataset_rules: Union[List[RelatedDataQualityRule], None, UnsetType] = UNSET
    """Rules that are applied on this dataset."""

    dq_reference_dataset_rules: Union[List[RelatedDataQualityRule], None, UnsetType] = (
        UNSET
    )
    """Rules where this dataset is referenced."""

    gcp_dataplex_aspect_type_metadata_entities: Union[
        List[RelatedGCPDataplexAspectType], None, UnsetType
    ] = UNSET
    """Dataplex entries (assets) that have aspects of this Aspect Type attached."""

    meanings: Union[List[RelatedAtlasGlossaryTerm], None, UnsetType] = UNSET
    """Glossary terms that are linked to this asset."""

    mc_monitors: Union[List[RelatedMCMonitor], None, UnsetType] = UNSET
    """Monitors that observe this asset."""

    mc_incidents: Union[List[RelatedMCIncident], None, UnsetType] = UNSET
    """"""

    children_folders: Union[List[RelatedFolder], None, UnsetType] = UNSET
    """Folders that exist within this namespace."""

    parent: Union[RelatedNamespace, None, UnsetType] = UNSET
    """Namespace in which this folder exists."""

    user_def_relationship_to: Union[List[RelatedReferenceable], None, UnsetType] = UNSET
    """"""

    user_def_relationship_from: Union[List[RelatedReferenceable], None, UnsetType] = (
        UNSET
    )
    """"""

    files: Union[List[RelatedFile], None, UnsetType] = UNSET
    """"""

    links: Union[List[RelatedLink], None, UnsetType] = UNSET
    """Links that are attached to this asset."""

    readme: Union[RelatedReadme, None, UnsetType] = UNSET
    """README that is linked to this asset."""

    children_queries: Union[List[RelatedQuery], None, UnsetType] = UNSET
    """Queries that exist within this namespace."""

    schema_registry_subjects: Union[
        List[RelatedSchemaRegistrySubject], None, UnsetType
    ] = UNSET
    """Schema registry subjects associated with this asset."""

    soda_checks: Union[List[RelatedSodaCheck], None, UnsetType] = UNSET
    """"""


class FolderNested(AssetNested):
    """Folder in nested API format for high-performance serialization."""

    attributes: Union[FolderAttributes, UnsetType] = UNSET
    relationship_attributes: Union[FolderRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[FolderRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[FolderRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION HELPERS & CONSTANTS
# =============================================================================

_FOLDER_REL_FIELDS: List[str] = [
    *_ASSET_REL_FIELDS,
    "anomalo_checks",
    "application",
    "application_field",
    "data_contract_latest",
    "data_contract_latest_certified",
    "output_port_data_products",
    "input_port_data_products",
    "metrics",
    "dq_base_dataset_rules",
    "dq_reference_dataset_rules",
    "gcp_dataplex_aspect_type_metadata_entities",
    "meanings",
    "mc_monitors",
    "mc_incidents",
    "children_folders",
    "parent",
    "user_def_relationship_to",
    "user_def_relationship_from",
    "files",
    "links",
    "readme",
    "children_queries",
    "schema_registry_subjects",
    "soda_checks",
]


def _populate_folder_attrs(attrs: FolderAttributes, obj: Folder) -> None:
    """Populate Folder-specific attributes on the attrs struct."""
    _populate_asset_attrs(attrs, obj)
    attrs.parent_qualified_name = obj.parent_qualified_name
    attrs.collection_qualified_name = obj.collection_qualified_name


def _extract_folder_attrs(attrs: FolderAttributes) -> dict:
    """Extract all Folder attributes from the attrs struct into a flat dict."""
    result = _extract_asset_attrs(attrs)
    result["parent_qualified_name"] = attrs.parent_qualified_name
    result["collection_qualified_name"] = attrs.collection_qualified_name
    return result


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _folder_to_nested(folder: Folder) -> FolderNested:
    """Convert flat Folder to nested format."""
    attrs = FolderAttributes()
    _populate_folder_attrs(attrs, folder)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    replace_rels, append_rels, remove_rels = categorize_relationships(
        folder, _FOLDER_REL_FIELDS, FolderRelationshipAttributes
    )
    return FolderNested(
        guid=folder.guid,
        type_name=folder.type_name,
        status=folder.status,
        version=folder.version,
        create_time=folder.create_time,
        update_time=folder.update_time,
        created_by=folder.created_by,
        updated_by=folder.updated_by,
        classifications=folder.classifications,
        classification_names=folder.classification_names,
        meanings=folder.meanings,
        labels=folder.labels,
        business_attributes=folder.business_attributes,
        custom_attributes=folder.custom_attributes,
        pending_tasks=folder.pending_tasks,
        proxy=folder.proxy,
        is_incomplete=folder.is_incomplete,
        provenance_type=folder.provenance_type,
        home_id=folder.home_id,
        depth=folder.depth,
        immediate_upstream=folder.immediate_upstream,
        immediate_downstream=folder.immediate_downstream,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _folder_from_nested(nested: FolderNested) -> Folder:
    """Convert nested format to flat Folder."""
    attrs = nested.attributes if nested.attributes is not UNSET else FolderAttributes()
    # Merge relationships from all three buckets
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        _FOLDER_REL_FIELDS,
        FolderRelationshipAttributes,
    )
    return Folder(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        depth=nested.depth,
        immediate_upstream=nested.immediate_upstream,
        immediate_downstream=nested.immediate_downstream,
        **_extract_folder_attrs(attrs),
        # Merged relationship attributes
        **merged_rels,
    )


def _folder_to_nested_bytes(folder: Folder, serde: Serde) -> bytes:
    """Convert flat Folder to nested JSON bytes."""
    return serde.encode(_folder_to_nested(folder))


def _folder_from_nested_bytes(data: bytes, serde: Serde) -> Folder:
    """Convert nested JSON bytes to flat Folder."""
    nested = serde.decode(data, FolderNested)
    return _folder_from_nested(nested)


# ---------------------------------------------------------------------------
# Deferred field descriptor initialization
# ---------------------------------------------------------------------------
from pyatlan.model.fields.atlan_fields import (  # noqa: E402
    KeywordTextField,
    RelationField,
)

Folder.PARENT_QUALIFIED_NAME = KeywordTextField(
    "parentQualifiedName", "parentQualifiedName", "parentQualifiedName.text"
)
Folder.COLLECTION_QUALIFIED_NAME = KeywordTextField(
    "collectionQualifiedName", "collectionQualifiedName", "collectionQualifiedName.text"
)
Folder.ANOMALO_CHECKS = RelationField("anomaloChecks")
Folder.APPLICATION = RelationField("application")
Folder.APPLICATION_FIELD = RelationField("applicationField")
Folder.DATA_CONTRACT_LATEST = RelationField("dataContractLatest")
Folder.DATA_CONTRACT_LATEST_CERTIFIED = RelationField("dataContractLatestCertified")
Folder.OUTPUT_PORT_DATA_PRODUCTS = RelationField("outputPortDataProducts")
Folder.INPUT_PORT_DATA_PRODUCTS = RelationField("inputPortDataProducts")
Folder.METRICS = RelationField("metrics")
Folder.DQ_BASE_DATASET_RULES = RelationField("dqBaseDatasetRules")
Folder.DQ_REFERENCE_DATASET_RULES = RelationField("dqReferenceDatasetRules")
Folder.GCP_DATAPLEX_ASPECT_TYPE_METADATA_ENTITIES = RelationField(
    "gcpDataplexAspectTypeMetadataEntities"
)
Folder.MEANINGS = RelationField("meanings")
Folder.MC_MONITORS = RelationField("mcMonitors")
Folder.MC_INCIDENTS = RelationField("mcIncidents")
Folder.CHILDREN_FOLDERS = RelationField("childrenFolders")
Folder.PARENT = RelationField("parent")
Folder.USER_DEF_RELATIONSHIP_TO = RelationField("userDefRelationshipTo")
Folder.USER_DEF_RELATIONSHIP_FROM = RelationField("userDefRelationshipFrom")
Folder.FILES = RelationField("files")
Folder.LINKS = RelationField("links")
Folder.README = RelationField("readme")
Folder.CHILDREN_QUERIES = RelationField("childrenQueries")
Folder.SCHEMA_REGISTRY_SUBJECTS = RelationField("schemaRegistrySubjects")
Folder.SODA_CHECKS = RelationField("sodaChecks")
