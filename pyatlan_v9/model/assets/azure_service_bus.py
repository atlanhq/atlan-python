# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
AzureServiceBus asset model with flattened inheritance.

This module provides:
- AzureServiceBus: Flat asset class (easy to use)
- AzureServiceBusAttributes: Nested attributes struct (extends AssetAttributes)
- AzureServiceBusNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class AzureServiceBus(Asset):
    """
    Base class for all AzureServiceBus types.
    """

    # Override type_name with AzureServiceBus-specific default
    type_name: Union[str, UnsetType] = "AzureServiceBus"

    azure_service_bus_namespace_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AzureServiceBus Namespace in which this asset exists."""

    azure_service_bus_namespace_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the AzureServiceBus Namespace in which this asset exists."""

    azure_service_bus_schema_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AzureServiceBus Schema in which this asset exists."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _azure_service_bus_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "AzureServiceBus":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            AzureServiceBus instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _azure_service_bus_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class AzureServiceBusAttributes(AssetAttributes):
    """AzureServiceBus-specific attributes for nested API format."""

    azure_service_bus_namespace_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AzureServiceBus Namespace in which this asset exists."""

    azure_service_bus_namespace_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the AzureServiceBus Namespace in which this asset exists."""

    azure_service_bus_schema_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AzureServiceBus Schema in which this asset exists."""


class AzureServiceBusRelationshipAttributes(AssetRelationshipAttributes):
    """AzureServiceBus-specific relationship attributes for nested API format."""

    pass


class AzureServiceBusNested(AssetNested):
    """AzureServiceBus in nested API format for high-performance serialization."""

    attributes: Union[AzureServiceBusAttributes, UnsetType] = UNSET
    relationship_attributes: Union[AzureServiceBusRelationshipAttributes, UnsetType] = (
        UNSET
    )
    append_relationship_attributes: Union[
        AzureServiceBusRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        AzureServiceBusRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _azure_service_bus_to_nested(
    azure_service_bus: AzureServiceBus,
) -> AzureServiceBusNested:
    """Convert flat AzureServiceBus to nested format."""
    attrs = AzureServiceBusAttributes(
        azure_service_bus_namespace_qualified_name=azure_service_bus.azure_service_bus_namespace_qualified_name,
        azure_service_bus_namespace_name=azure_service_bus.azure_service_bus_namespace_name,
        azure_service_bus_schema_qualified_name=azure_service_bus.azure_service_bus_schema_qualified_name,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        azure_service_bus, rel_fields, AzureServiceBusRelationshipAttributes
    )
    return AzureServiceBusNested(
        guid=azure_service_bus.guid,
        type_name=azure_service_bus.type_name,
        status=azure_service_bus.status,
        version=azure_service_bus.version,
        create_time=azure_service_bus.create_time,
        update_time=azure_service_bus.update_time,
        created_by=azure_service_bus.created_by,
        updated_by=azure_service_bus.updated_by,
        classifications=azure_service_bus.classifications,
        classification_names=azure_service_bus.classification_names,
        meanings=azure_service_bus.meanings,
        labels=azure_service_bus.labels,
        business_attributes=azure_service_bus.business_attributes,
        custom_attributes=azure_service_bus.custom_attributes,
        pending_tasks=azure_service_bus.pending_tasks,
        proxy=azure_service_bus.proxy,
        is_incomplete=azure_service_bus.is_incomplete,
        provenance_type=azure_service_bus.provenance_type,
        home_id=azure_service_bus.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _azure_service_bus_from_nested(nested: AzureServiceBusNested) -> AzureServiceBus:
    """Convert nested format to flat AzureServiceBus."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else AzureServiceBusAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        AzureServiceBusRelationshipAttributes,
    )
    return AzureServiceBus(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        azure_service_bus_namespace_qualified_name=attrs.azure_service_bus_namespace_qualified_name,
        azure_service_bus_namespace_name=attrs.azure_service_bus_namespace_name,
        azure_service_bus_schema_qualified_name=attrs.azure_service_bus_schema_qualified_name,
        # Merged relationship attributes
        **merged_rels,
    )


def _azure_service_bus_to_nested_bytes(
    azure_service_bus: AzureServiceBus, serde: Serde
) -> bytes:
    """Convert flat AzureServiceBus to nested JSON bytes."""
    return serde.encode(_azure_service_bus_to_nested(azure_service_bus))


def _azure_service_bus_from_nested_bytes(data: bytes, serde: Serde) -> AzureServiceBus:
    """Convert nested JSON bytes to flat AzureServiceBus."""
    nested = serde.decode(data, AzureServiceBusNested)
    return _azure_service_bus_from_nested(nested)
