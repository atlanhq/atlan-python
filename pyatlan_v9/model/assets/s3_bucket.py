# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
S3Bucket asset model with flattened inheritance.

This module provides:
- S3Bucket: Flat asset class (easy to use)
- S3BucketAttributes: Nested attributes struct (extends AssetAttributes)
- S3BucketNested: Nested API format struct
"""

from __future__ import annotations

from typing import Any, Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset
from pyatlan_v9.utils import init_guid

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .s3_related import RelatedS3Object, RelatedS3Prefix

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class S3Bucket(Asset):
    """
    Instance of an S3 bucket in Atlan.
    """

    # Override type_name with S3Bucket-specific default
    type_name: Union[str, UnsetType] = "S3Bucket"

    s3_object_count: Union[int, None, UnsetType] = UNSET
    """Number of objects within the bucket."""

    s3_bucket_versioning_enabled: Union[bool, None, UnsetType] = UNSET
    """Whether versioning is enabled for the bucket (true) or not (false)."""

    s3_e_tag: Union[str, None, UnsetType] = UNSET
    """Entity tag for the asset. An entity tag is a hash of the object and represents changes to the contents of an object only, not its metadata."""

    s3_encryption: Union[str, None, UnsetType] = UNSET
    """"""

    s3_parent_prefix_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the immediate parent prefix in which this asset exists."""

    s3_prefix_hierarchy: Union[list[dict[str, str]], None, UnsetType] = UNSET
    """Ordered array of prefix assets with qualified name and name representing the complete prefix hierarchy path for this asset, from immediate parent to root prefix."""

    aws_arn: Union[str, None, UnsetType] = UNSET
    """Amazon Resource Name (ARN) for this asset. This uniquely identifies the asset in AWS, and thus must be unique across all AWS asset instances."""

    aws_partition: Union[str, None, UnsetType] = UNSET
    """Group of AWS region and service objects."""

    aws_service: Union[str, None, UnsetType] = UNSET
    """Type of service in which the asset exists."""

    aws_region: Union[str, None, UnsetType] = UNSET
    """Physical region where the data center in which the asset exists is clustered."""

    aws_account_id: Union[str, None, UnsetType] = UNSET
    """12-digit number that uniquely identifies an AWS account."""

    aws_resource_id: Union[str, None, UnsetType] = UNSET
    """Unique resource ID assigned when a new resource is created."""

    aws_owner_name: Union[str, None, UnsetType] = UNSET
    """Root user's name."""

    aws_owner_id: Union[str, None, UnsetType] = UNSET
    """Root user's ID."""

    aws_tags: Union[list[dict[str, Any]], None, UnsetType] = UNSET
    """List of tags that have been applied to the asset in AWS."""

    objects: Union[list[RelatedS3Object], None, UnsetType] = UNSET
    """S3 objects within this bucket."""

    s3_prefixes: Union[list[RelatedS3Prefix], None, UnsetType] = UNSET
    """S3 prefixes contained in this bucket."""

    # =========================================================================
    # Creator Methods
    # =========================================================================

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        name: str,
        connection_qualified_name: str,
        aws_arn: str | None = None,
    ) -> "S3Bucket":
        """
        Create a new S3Bucket asset.

        Args:
            name: Name of the bucket
            connection_qualified_name: Unique name of the connection in which this bucket exists
            aws_arn: Amazon Resource Name (ARN) for the bucket (optional)

        Returns:
            S3Bucket instance ready to be created

        Raises:
            ValueError: If required parameters are missing or invalid
        """
        if name is None:
            raise ValueError("name is required")
        if connection_qualified_name is None:
            raise ValueError("connection_qualified_name is required")

        if name.strip() == "":
            raise ValueError("name cannot be blank")
        if connection_qualified_name.strip() == "":
            raise ValueError("connection_qualified_name cannot be blank")

        fields = connection_qualified_name.split("/")
        if len(fields) != 3:
            raise ValueError("Invalid connection_qualified_name")

        if fields[0].replace(" ", "") == "" or fields[2].replace(" ", "") == "":
            raise ValueError("Invalid connection_qualified_name")

        if fields[1].lower() != "s3":
            raise ValueError("Invalid connection_qualified_name")

        connector_name = fields[1]
        qualified_name = f"{connection_qualified_name}/{aws_arn if aws_arn else name}"

        return cls(
            name=name,
            qualified_name=qualified_name,
            connection_qualified_name=connection_qualified_name,
            connector_name=connector_name,
            aws_arn=aws_arn,
        )

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _s3_bucket_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "S3Bucket":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            S3Bucket instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _s3_bucket_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class S3BucketAttributes(AssetAttributes):
    """S3Bucket-specific attributes for nested API format."""

    s3_object_count: Union[int, None, UnsetType] = UNSET
    """Number of objects within the bucket."""

    s3_bucket_versioning_enabled: Union[bool, None, UnsetType] = UNSET
    """Whether versioning is enabled for the bucket (true) or not (false)."""

    s3_e_tag: Union[str, None, UnsetType] = UNSET
    """Entity tag for the asset. An entity tag is a hash of the object and represents changes to the contents of an object only, not its metadata."""

    s3_encryption: Union[str, None, UnsetType] = UNSET
    """"""

    s3_parent_prefix_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the immediate parent prefix in which this asset exists."""

    s3_prefix_hierarchy: Union[list[dict[str, str]], None, UnsetType] = UNSET
    """Ordered array of prefix assets with qualified name and name representing the complete prefix hierarchy path for this asset, from immediate parent to root prefix."""

    aws_arn: Union[str, None, UnsetType] = UNSET
    """Amazon Resource Name (ARN) for this asset. This uniquely identifies the asset in AWS, and thus must be unique across all AWS asset instances."""

    aws_partition: Union[str, None, UnsetType] = UNSET
    """Group of AWS region and service objects."""

    aws_service: Union[str, None, UnsetType] = UNSET
    """Type of service in which the asset exists."""

    aws_region: Union[str, None, UnsetType] = UNSET
    """Physical region where the data center in which the asset exists is clustered."""

    aws_account_id: Union[str, None, UnsetType] = UNSET
    """12-digit number that uniquely identifies an AWS account."""

    aws_resource_id: Union[str, None, UnsetType] = UNSET
    """Unique resource ID assigned when a new resource is created."""

    aws_owner_name: Union[str, None, UnsetType] = UNSET
    """Root user's name."""

    aws_owner_id: Union[str, None, UnsetType] = UNSET
    """Root user's ID."""

    aws_tags: Union[list[dict[str, Any]], None, UnsetType] = UNSET
    """List of tags that have been applied to the asset in AWS."""


class S3BucketRelationshipAttributes(AssetRelationshipAttributes):
    """S3Bucket-specific relationship attributes for nested API format."""

    objects: Union[list[RelatedS3Object], None, UnsetType] = UNSET
    """S3 objects within this bucket."""

    s3_prefixes: Union[list[RelatedS3Prefix], None, UnsetType] = UNSET
    """S3 prefixes contained in this bucket."""


class S3BucketNested(AssetNested):
    """S3Bucket in nested API format for high-performance serialization."""

    attributes: Union[S3BucketAttributes, UnsetType] = UNSET
    relationship_attributes: Union[S3BucketRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[S3BucketRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[S3BucketRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _s3_bucket_to_nested(s3_bucket: S3Bucket) -> S3BucketNested:
    """Convert flat S3Bucket to nested format."""
    attrs = S3BucketAttributes(
        s3_object_count=s3_bucket.s3_object_count,
        s3_bucket_versioning_enabled=s3_bucket.s3_bucket_versioning_enabled,
        s3_e_tag=s3_bucket.s3_e_tag,
        s3_encryption=s3_bucket.s3_encryption,
        s3_parent_prefix_qualified_name=s3_bucket.s3_parent_prefix_qualified_name,
        s3_prefix_hierarchy=s3_bucket.s3_prefix_hierarchy,
        aws_arn=s3_bucket.aws_arn,
        aws_partition=s3_bucket.aws_partition,
        aws_service=s3_bucket.aws_service,
        aws_region=s3_bucket.aws_region,
        aws_account_id=s3_bucket.aws_account_id,
        aws_resource_id=s3_bucket.aws_resource_id,
        aws_owner_name=s3_bucket.aws_owner_name,
        aws_owner_id=s3_bucket.aws_owner_id,
        aws_tags=s3_bucket.aws_tags,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["objects", "s3_prefixes"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        s3_bucket, rel_fields, S3BucketRelationshipAttributes
    )
    return S3BucketNested(
        guid=s3_bucket.guid,
        type_name=s3_bucket.type_name,
        status=s3_bucket.status,
        version=s3_bucket.version,
        create_time=s3_bucket.create_time,
        update_time=s3_bucket.update_time,
        created_by=s3_bucket.created_by,
        updated_by=s3_bucket.updated_by,
        classifications=s3_bucket.classifications,
        classification_names=s3_bucket.classification_names,
        meanings=s3_bucket.meanings,
        labels=s3_bucket.labels,
        business_attributes=s3_bucket.business_attributes,
        custom_attributes=s3_bucket.custom_attributes,
        pending_tasks=s3_bucket.pending_tasks,
        proxy=s3_bucket.proxy,
        is_incomplete=s3_bucket.is_incomplete,
        provenance_type=s3_bucket.provenance_type,
        home_id=s3_bucket.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _s3_bucket_from_nested(nested: S3BucketNested) -> S3Bucket:
    """Convert nested format to flat S3Bucket."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else S3BucketAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["objects", "s3_prefixes"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        S3BucketRelationshipAttributes,
    )
    return S3Bucket(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        s3_object_count=attrs.s3_object_count,
        s3_bucket_versioning_enabled=attrs.s3_bucket_versioning_enabled,
        s3_e_tag=attrs.s3_e_tag,
        s3_encryption=attrs.s3_encryption,
        s3_parent_prefix_qualified_name=attrs.s3_parent_prefix_qualified_name,
        s3_prefix_hierarchy=attrs.s3_prefix_hierarchy,
        aws_arn=attrs.aws_arn,
        aws_partition=attrs.aws_partition,
        aws_service=attrs.aws_service,
        aws_region=attrs.aws_region,
        aws_account_id=attrs.aws_account_id,
        aws_resource_id=attrs.aws_resource_id,
        aws_owner_name=attrs.aws_owner_name,
        aws_owner_id=attrs.aws_owner_id,
        aws_tags=attrs.aws_tags,
        # Merged relationship attributes
        **merged_rels,
    )


def _s3_bucket_to_nested_bytes(s3_bucket: S3Bucket, serde: Serde) -> bytes:
    """Convert flat S3Bucket to nested JSON bytes."""
    return serde.encode(_s3_bucket_to_nested(s3_bucket))


def _s3_bucket_from_nested_bytes(data: bytes, serde: Serde) -> S3Bucket:
    """Convert nested JSON bytes to flat S3Bucket."""
    nested = serde.decode(data, S3BucketNested)
    return _s3_bucket_from_nested(nested)
