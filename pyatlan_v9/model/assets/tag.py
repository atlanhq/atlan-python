# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Tag asset model with flattened inheritance.

This module provides:
- Tag: Flat asset class (easy to use)
- TagAttributes: Nested attributes struct (extends AssetAttributes)
- TagNested: Nested API format struct
"""

from __future__ import annotations

from typing import Any, Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Tag(Asset):
    """
    Base class for all (source-specific) tag assets.
    """

    # Override type_name with Tag-specific default
    type_name: Union[str, UnsetType] = "Tag"

    tag_id: Union[str, None, UnsetType] = UNSET
    """Unique identifier of the tag in the source system."""

    tag_attributes: Union[list[dict[str, Any]], None, UnsetType] = UNSET
    """Attributes associated with the tag in the source system."""

    tag_allowed_values: Union[list[str], None, UnsetType] = UNSET
    """Allowed values for the tag in the source system. These are denormalized from tagAttributes for ease of querying."""

    mapped_classification_name: Union[str, None, UnsetType] = UNSET
    """Name of the classification in Atlan that is mapped to this tag."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _tag_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(json_data: Union[str, bytes], serde: Serde | None = None) -> "Tag":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Tag instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _tag_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class TagAttributes(AssetAttributes):
    """Tag-specific attributes for nested API format."""

    tag_id: Union[str, None, UnsetType] = UNSET
    """Unique identifier of the tag in the source system."""

    tag_attributes: Union[list[dict[str, Any]], None, UnsetType] = UNSET
    """Attributes associated with the tag in the source system."""

    tag_allowed_values: Union[list[str], None, UnsetType] = UNSET
    """Allowed values for the tag in the source system. These are denormalized from tagAttributes for ease of querying."""

    mapped_classification_name: Union[str, None, UnsetType] = UNSET
    """Name of the classification in Atlan that is mapped to this tag."""


class TagRelationshipAttributes(AssetRelationshipAttributes):
    """Tag-specific relationship attributes for nested API format."""

    pass


class TagNested(AssetNested):
    """Tag in nested API format for high-performance serialization."""

    attributes: Union[TagAttributes, UnsetType] = UNSET
    relationship_attributes: Union[TagRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[TagRelationshipAttributes, UnsetType] = UNSET
    remove_relationship_attributes: Union[TagRelationshipAttributes, UnsetType] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _tag_to_nested(tag: Tag) -> TagNested:
    """Convert flat Tag to nested format."""
    attrs = TagAttributes(
        tag_id=tag.tag_id,
        tag_attributes=tag.tag_attributes,
        tag_allowed_values=tag.tag_allowed_values,
        mapped_classification_name=tag.mapped_classification_name,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        tag, rel_fields, TagRelationshipAttributes
    )
    return TagNested(
        guid=tag.guid,
        type_name=tag.type_name,
        status=tag.status,
        version=tag.version,
        create_time=tag.create_time,
        update_time=tag.update_time,
        created_by=tag.created_by,
        updated_by=tag.updated_by,
        classifications=tag.classifications,
        classification_names=tag.classification_names,
        meanings=tag.meanings,
        labels=tag.labels,
        business_attributes=tag.business_attributes,
        custom_attributes=tag.custom_attributes,
        pending_tasks=tag.pending_tasks,
        proxy=tag.proxy,
        is_incomplete=tag.is_incomplete,
        provenance_type=tag.provenance_type,
        home_id=tag.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _tag_from_nested(nested: TagNested) -> Tag:
    """Convert nested format to flat Tag."""
    attrs = nested.attributes if nested.attributes is not UNSET else TagAttributes()
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        TagRelationshipAttributes,
    )
    return Tag(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        tag_id=attrs.tag_id,
        tag_attributes=attrs.tag_attributes,
        tag_allowed_values=attrs.tag_allowed_values,
        mapped_classification_name=attrs.mapped_classification_name,
        # Merged relationship attributes
        **merged_rels,
    )


def _tag_to_nested_bytes(tag: Tag, serde: Serde) -> bytes:
    """Convert flat Tag to nested JSON bytes."""
    return serde.encode(_tag_to_nested(tag))


def _tag_from_nested_bytes(data: bytes, serde: Serde) -> Tag:
    """Convert nested JSON bytes to flat Tag."""
    nested = serde.decode(data, TagNested)
    return _tag_from_nested(nested)
