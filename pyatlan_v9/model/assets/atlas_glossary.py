# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
AtlasGlossary asset model with flattened inheritance.

This module provides:
- AtlasGlossary: Flat asset class (easy to use)
- AtlasGlossaryAttributes: Nested attributes struct (extends AssetAttributes)
- AtlasGlossaryNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

import msgspec
from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset
from pyatlan_v9.utils import init_guid

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .gtc_related import RelatedAtlasGlossaryCategory, RelatedAtlasGlossaryTerm

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class AtlasGlossary(Asset):
    """
    Instance of a glossary in Atlan.
    """

    # Override type_name with AtlasGlossary-specific default
    type_name: Union[str, UnsetType] = "AtlasGlossary"

    short_description: Union[str, None, UnsetType] = UNSET
    """Unused. A short definition of the glossary. See 'description' and 'userDescription' instead."""

    long_description: Union[str, None, UnsetType] = UNSET
    """Unused. A longer description of the glossary. See 'readme' instead."""

    language: Union[str, None, UnsetType] = UNSET
    """Unused. Language of the glossary's contents."""

    usage: Union[str, None, UnsetType] = UNSET
    """Unused. Inteded usage for the glossary."""

    additional_attributes: Union[dict[str, str], None, UnsetType] = UNSET
    """Unused. Arbitrary set of additional attributes associated with this glossary."""

    glossary_type: Union[str, None, UnsetType] = UNSET
    """"""

    terms: Union[list[RelatedAtlasGlossaryTerm], None, UnsetType] = UNSET
    """Terms contained within this glossary."""

    categories: Union[list[RelatedAtlasGlossaryCategory], None, UnsetType] = UNSET
    """Categories contained within this glossary."""

    # =========================================================================
    # Creator Methods
    # =========================================================================

    @classmethod
    @init_guid
    def creator(cls, *, name: str) -> "AtlasGlossary":
        """
        Create a new AtlasGlossary asset.

        Args:
            name: Name of the glossary

        Returns:
            AtlasGlossary instance ready to be created

        Raises:
            ValueError: If name is not provided
        """
        if not name:
            raise ValueError("name is required")

        # Generate a unique qualified name using a simple ID generator
        import uuid

        qualified_name = str(uuid.uuid4().hex[:16])

        return AtlasGlossary(name=name, qualified_name=qualified_name)

    @classmethod
    def create(cls, *, name: str) -> "AtlasGlossary":
        """
        Create a new AtlasGlossary asset (deprecated - use creator instead).

        Args:
            name: Name of the glossary

        Returns:
            AtlasGlossary instance ready to be created
        """
        return cls.creator(name=name)

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _atlas_glossary_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "AtlasGlossary":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            AtlasGlossary instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _atlas_glossary_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class AtlasGlossaryAttributes(AssetAttributes):
    """AtlasGlossary-specific attributes for nested API format."""

    short_description: Union[str, None, UnsetType] = UNSET
    """Unused. A short definition of the glossary. See 'description' and 'userDescription' instead."""

    long_description: Union[str, None, UnsetType] = UNSET
    """Unused. A longer description of the glossary. See 'readme' instead."""

    language: Union[str, None, UnsetType] = UNSET
    """Unused. Language of the glossary's contents."""

    usage: Union[str, None, UnsetType] = UNSET
    """Unused. Inteded usage for the glossary."""

    additional_attributes: Union[dict[str, str], None, UnsetType] = UNSET
    """Unused. Arbitrary set of additional attributes associated with this glossary."""

    glossary_type: Union[str, None, UnsetType] = UNSET
    """"""


class AtlasGlossaryRelationshipAttributes(AssetRelationshipAttributes):
    """AtlasGlossary-specific relationship attributes for nested API format."""

    terms: Union[list[RelatedAtlasGlossaryTerm], None, UnsetType] = UNSET
    """Terms contained within this glossary."""

    categories: Union[list[RelatedAtlasGlossaryCategory], None, UnsetType] = UNSET
    """Categories contained within this glossary."""


class AtlasGlossaryNested(AssetNested):
    """AtlasGlossary in nested API format for high-performance serialization."""

    attributes: Union[AtlasGlossaryAttributes, UnsetType] = UNSET
    relationship_attributes: Union[AtlasGlossaryRelationshipAttributes, UnsetType] = (
        UNSET
    )
    append_relationship_attributes: Union[
        AtlasGlossaryRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        AtlasGlossaryRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _atlas_glossary_to_nested(atlas_glossary: AtlasGlossary) -> AtlasGlossaryNested:
    """Convert flat AtlasGlossary to nested format."""
    # Get all attribute field names dynamically
    attr_field_names = {f.name for f in msgspec.structs.fields(AtlasGlossaryAttributes)}

    # Build attributes dict with only the fields that exist in AtlasGlossaryAttributes
    attrs_kwargs = {
        name: getattr(atlas_glossary, name)
        for name in attr_field_names
        if hasattr(atlas_glossary, name)
    }
    attrs = AtlasGlossaryAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["terms", "categories"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        atlas_glossary, rel_fields, AtlasGlossaryRelationshipAttributes
    )
    return AtlasGlossaryNested(
        guid=atlas_glossary.guid,
        type_name=atlas_glossary.type_name,
        status=atlas_glossary.status,
        version=atlas_glossary.version,
        create_time=atlas_glossary.create_time,
        update_time=atlas_glossary.update_time,
        created_by=atlas_glossary.created_by,
        updated_by=atlas_glossary.updated_by,
        classifications=atlas_glossary.classifications,
        classification_names=atlas_glossary.classification_names,
        meanings=atlas_glossary.meanings,
        labels=atlas_glossary.labels,
        business_attributes=atlas_glossary.business_attributes,
        custom_attributes=atlas_glossary.custom_attributes,
        pending_tasks=atlas_glossary.pending_tasks,
        proxy=atlas_glossary.proxy,
        is_incomplete=atlas_glossary.is_incomplete,
        provenance_type=atlas_glossary.provenance_type,
        home_id=atlas_glossary.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _atlas_glossary_from_nested(nested: AtlasGlossaryNested) -> AtlasGlossary:
    """Convert nested format to flat AtlasGlossary."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else AtlasGlossaryAttributes()
    )

    # Merge relationships from all three buckets
    rel_fields: list[str] = ["terms", "categories"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        AtlasGlossaryRelationshipAttributes,
    )

    # Get top-level field names (exclude attributes and relationship fields)
    top_level_fields = {
        f.name
        for f in msgspec.structs.fields(AssetNested)
        if f.name
        not in (
            "attributes",
            "relationship_attributes",
            "append_relationship_attributes",
            "remove_relationship_attributes",
        )
    }

    # Get attribute field names
    attr_field_names = {f.name for f in msgspec.structs.fields(AtlasGlossaryAttributes)}

    # Build kwargs: top-level fields + attribute fields + relationships
    kwargs = {}

    # Add top-level fields from nested
    for name in top_level_fields:
        if hasattr(nested, name):
            kwargs[name] = getattr(nested, name)

    # Add attribute fields from attrs
    for name in attr_field_names:
        if hasattr(attrs, name):
            kwargs[name] = getattr(attrs, name)

    # Add merged relationships
    kwargs.update(merged_rels)

    return AtlasGlossary(**kwargs)


def _atlas_glossary_to_nested_bytes(
    atlas_glossary: AtlasGlossary, serde: Serde
) -> bytes:
    """Convert flat AtlasGlossary to nested JSON bytes."""
    return serde.encode(_atlas_glossary_to_nested(atlas_glossary))


def _atlas_glossary_from_nested_bytes(data: bytes, serde: Serde) -> AtlasGlossary:
    """Convert nested JSON bytes to flat AtlasGlossary."""
    nested = serde.decode(data, AtlasGlossaryNested)
    return _atlas_glossary_from_nested(nested)
