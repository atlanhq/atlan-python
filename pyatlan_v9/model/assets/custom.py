# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Custom asset model with flattened inheritance.

This module provides:
- Custom: Flat asset class (easy to use)
- CustomAttributes: Nested attributes struct (extends AssetAttributes)
- CustomNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Custom(Asset):
    """
    Base class for all Custom types.
    """

    # Override type_name with Custom-specific default
    type_name: Union[str, UnsetType] = "Custom"

    pass

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _custom_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(json_data: Union[str, bytes], serde: Serde | None = None) -> "Custom":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Custom instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _custom_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class CustomAttributes(AssetAttributes):
    """Custom-specific attributes for nested API format."""

    pass


class CustomRelationshipAttributes(AssetRelationshipAttributes):
    """Custom-specific relationship attributes for nested API format."""

    pass


class CustomNested(AssetNested):
    """Custom in nested API format for high-performance serialization."""

    attributes: Union[CustomAttributes, UnsetType] = UNSET
    relationship_attributes: Union[CustomRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[CustomRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[CustomRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _custom_to_nested(custom: Custom) -> CustomNested:
    """Convert flat Custom to nested format."""
    attrs = CustomAttributes()
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        custom, rel_fields, CustomRelationshipAttributes
    )
    return CustomNested(
        guid=custom.guid,
        type_name=custom.type_name,
        status=custom.status,
        version=custom.version,
        create_time=custom.create_time,
        update_time=custom.update_time,
        created_by=custom.created_by,
        updated_by=custom.updated_by,
        classifications=custom.classifications,
        classification_names=custom.classification_names,
        meanings=custom.meanings,
        labels=custom.labels,
        business_attributes=custom.business_attributes,
        custom_attributes=custom.custom_attributes,
        pending_tasks=custom.pending_tasks,
        proxy=custom.proxy,
        is_incomplete=custom.is_incomplete,
        provenance_type=custom.provenance_type,
        home_id=custom.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _custom_from_nested(nested: CustomNested) -> Custom:
    """Convert nested format to flat Custom."""
    attrs = nested.attributes if nested.attributes is not UNSET else CustomAttributes()
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        CustomRelationshipAttributes,
    )
    return Custom(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _custom_to_nested_bytes(custom: Custom, serde: Serde) -> bytes:
    """Convert flat Custom to nested JSON bytes."""
    return serde.encode(_custom_to_nested(custom))


def _custom_from_nested_bytes(data: bytes, serde: Serde) -> Custom:
    """Convert nested JSON bytes to flat Custom."""
    nested = serde.decode(data, CustomNested)
    return _custom_from_nested(nested)
