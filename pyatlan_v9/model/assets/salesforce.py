# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Salesforce asset model with flattened inheritance.

This module provides:
- Salesforce: Flat asset class (easy to use)
- SalesforceAttributes: Nested attributes struct (extends AssetAttributes)
- SalesforceNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Salesforce(Asset):
    """
    Base class for Salesforce assets.
    """

    # Override type_name with Salesforce-specific default
    type_name: Union[str, UnsetType] = "Salesforce"

    organization_qualified_name: Union[str, None, UnsetType] = UNSET
    """Fully-qualified name of the organization in Salesforce."""

    api_name: Union[str, None, UnsetType] = UNSET
    """Name of this asset in the Salesforce API."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _salesforce_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "Salesforce":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Salesforce instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _salesforce_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class SalesforceAttributes(AssetAttributes):
    """Salesforce-specific attributes for nested API format."""

    organization_qualified_name: Union[str, None, UnsetType] = UNSET
    """Fully-qualified name of the organization in Salesforce."""

    api_name: Union[str, None, UnsetType] = UNSET
    """Name of this asset in the Salesforce API."""


class SalesforceRelationshipAttributes(AssetRelationshipAttributes):
    """Salesforce-specific relationship attributes for nested API format."""

    pass


class SalesforceNested(AssetNested):
    """Salesforce in nested API format for high-performance serialization."""

    attributes: Union[SalesforceAttributes, UnsetType] = UNSET
    relationship_attributes: Union[SalesforceRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[
        SalesforceRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        SalesforceRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _salesforce_to_nested(salesforce: Salesforce) -> SalesforceNested:
    """Convert flat Salesforce to nested format."""
    attrs = SalesforceAttributes(
        organization_qualified_name=salesforce.organization_qualified_name,
        api_name=salesforce.api_name,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        salesforce, rel_fields, SalesforceRelationshipAttributes
    )
    return SalesforceNested(
        guid=salesforce.guid,
        type_name=salesforce.type_name,
        status=salesforce.status,
        version=salesforce.version,
        create_time=salesforce.create_time,
        update_time=salesforce.update_time,
        created_by=salesforce.created_by,
        updated_by=salesforce.updated_by,
        classifications=salesforce.classifications,
        classification_names=salesforce.classification_names,
        meanings=salesforce.meanings,
        labels=salesforce.labels,
        business_attributes=salesforce.business_attributes,
        custom_attributes=salesforce.custom_attributes,
        pending_tasks=salesforce.pending_tasks,
        proxy=salesforce.proxy,
        is_incomplete=salesforce.is_incomplete,
        provenance_type=salesforce.provenance_type,
        home_id=salesforce.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _salesforce_from_nested(nested: SalesforceNested) -> Salesforce:
    """Convert nested format to flat Salesforce."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else SalesforceAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        SalesforceRelationshipAttributes,
    )
    return Salesforce(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        organization_qualified_name=attrs.organization_qualified_name,
        api_name=attrs.api_name,
        # Merged relationship attributes
        **merged_rels,
    )


def _salesforce_to_nested_bytes(salesforce: Salesforce, serde: Serde) -> bytes:
    """Convert flat Salesforce to nested JSON bytes."""
    return serde.encode(_salesforce_to_nested(salesforce))


def _salesforce_from_nested_bytes(data: bytes, serde: Serde) -> Salesforce:
    """Convert nested JSON bytes to flat Salesforce."""
    nested = serde.decode(data, SalesforceNested)
    return _salesforce_from_nested(nested)
