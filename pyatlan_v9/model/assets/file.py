# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
File asset model with flattened inheritance.

This module provides:
- File: Flat asset class (easy to use)
- FileAttributes: Nested attributes struct (extends AssetAttributes)
- FileNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .asset_related import RelatedAsset

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class File(Asset):
    """
    Instance of a static file in Atlan.
    """

    # Override type_name with File-specific default
    type_name: Union[str, UnsetType] = "File"

    file_type: Union[str, None, UnsetType] = UNSET
    """Type (extension) of the file."""

    file_path: Union[str, None, UnsetType] = UNSET
    """URL giving the online location where the file can be accessed."""

    link: Union[str, None, UnsetType] = UNSET
    """URL to the resource."""

    is_global: Union[bool, None, UnsetType] = UNSET
    """Whether the resource is global (true) or not (false)."""

    reference: Union[str, None, UnsetType] = UNSET
    """Reference to the resource."""

    resource_metadata: Union[dict[str, str], None, UnsetType] = UNSET
    """Metadata of the resource."""

    file_assets: Union[RelatedAsset, None, UnsetType] = UNSET
    """"""

    # =========================================================================
    # Convenience Methods
    # =========================================================================

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        name: str,
        connection_qualified_name: str,
        file_type: str,
    ) -> "File":
        """
        Create a new File asset.

        Args:
            name: Simple name of the file
            connection_qualified_name: Unique name of the connection in which this file exists
            file_type: Type of the file (e.g., PDF, CSV)

        Returns:
            New File instance with all fields populated

        Raises:
            ValueError: If required parameters are missing or blank
        """
        if isinstance(name, str) and name.strip() == "":
            raise ValueError("name cannot be blank")
        if (
            isinstance(connection_qualified_name, str)
            and connection_qualified_name.strip() == ""
        ):
            raise ValueError("connection_qualified_name cannot be blank")
        if isinstance(file_type, str) and file_type.strip() == "":
            raise ValueError("file_type cannot be blank")
        validate_required_fields(
            ["name", "connection_qualified_name", "file_type"],
            [name, connection_qualified_name, file_type],
        )

        fields = connection_qualified_name.split("/")
        connector_name = fields[1] if len(fields) > 1 else None
        qualified_name = f"{connection_qualified_name}/{name}"

        return cls(
            name=name,
            qualified_name=qualified_name,
            file_type=file_type,
            connector_name=connector_name,
            connection_qualified_name=connection_qualified_name,
        )

    @classmethod
    def updater(cls, *, qualified_name: str, name: str) -> "File":
        """
        Create a File instance for updating an existing asset.

        Args:
            qualified_name: Unique name of the file to update
            name: Simple name of the file

        Returns:
            File instance configured for updates

        Raises:
            ValueError: If required parameters are missing
        """
        validate_required_fields(["qualified_name", "name"], [qualified_name, name])
        return cls(qualified_name=qualified_name, name=name)

    def trim_to_required(self) -> "File":
        """
        Return a File with only required fields for reference.

        Returns:
            File instance with only qualified_name and name set
        """
        return File(qualified_name=self.qualified_name, name=self.name)

    # Backward compatibility aliases
    @classmethod
    def create(cls, **kwargs) -> "File":
        """Backward compatibility alias for creator()."""
        return cls.creator(**kwargs)

    @classmethod
    def create_for_modification(cls, **kwargs) -> "File":
        """Backward compatibility alias for updater()."""
        return cls.updater(**kwargs)

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _file_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(json_data: Union[str, bytes], serde: Serde | None = None) -> "File":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            File instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _file_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class FileAttributes(AssetAttributes):
    """File-specific attributes for nested API format."""

    file_type: Union[str, None, UnsetType] = UNSET
    """Type (extension) of the file."""

    file_path: Union[str, None, UnsetType] = UNSET
    """URL giving the online location where the file can be accessed."""

    link: Union[str, None, UnsetType] = UNSET
    """URL to the resource."""

    is_global: Union[bool, None, UnsetType] = UNSET
    """Whether the resource is global (true) or not (false)."""

    reference: Union[str, None, UnsetType] = UNSET
    """Reference to the resource."""

    resource_metadata: Union[dict[str, str], None, UnsetType] = UNSET
    """Metadata of the resource."""


class FileRelationshipAttributes(AssetRelationshipAttributes):
    """File-specific relationship attributes for nested API format."""

    file_assets: Union[RelatedAsset, None, UnsetType] = UNSET
    """"""


class FileNested(AssetNested):
    """File in nested API format for high-performance serialization."""

    attributes: Union[FileAttributes, UnsetType] = UNSET
    relationship_attributes: Union[FileRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[FileRelationshipAttributes, UnsetType] = UNSET
    remove_relationship_attributes: Union[FileRelationshipAttributes, UnsetType] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _file_to_nested(file: File) -> FileNested:
    """Convert flat File to nested format using dynamic field extraction."""
    attrs_kwargs = build_attributes_kwargs(file, FileAttributes)
    attrs = FileAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["file_assets"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        file, rel_fields, FileRelationshipAttributes
    )
    return FileNested(
        guid=file.guid,
        type_name=file.type_name,
        status=file.status,
        version=file.version,
        create_time=file.create_time,
        update_time=file.update_time,
        created_by=file.created_by,
        updated_by=file.updated_by,
        classifications=file.classifications,
        classification_names=file.classification_names,
        meanings=file.meanings,
        labels=file.labels,
        business_attributes=file.business_attributes,
        custom_attributes=file.custom_attributes,
        pending_tasks=file.pending_tasks,
        proxy=file.proxy,
        is_incomplete=file.is_incomplete,
        provenance_type=file.provenance_type,
        home_id=file.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _file_from_nested(nested: FileNested) -> File:
    """Convert nested format to flat File using dynamic field extraction."""
    attrs = nested.attributes if nested.attributes is not UNSET else FileAttributes()

    # Merge relationships from all three buckets
    rel_fields: list[str] = ["file_assets"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        FileRelationshipAttributes,
    )

    # Build flat kwargs using dynamic field extraction
    kwargs = build_flat_kwargs(nested, attrs, merged_rels, AssetNested, FileAttributes)

    return File(**kwargs)


def _file_to_nested_bytes(file: File, serde: Serde) -> bytes:
    """Convert flat File to nested JSON bytes."""
    return serde.encode(_file_to_nested(file))


def _file_from_nested_bytes(data: bytes, serde: Serde) -> File:
    """Convert nested JSON bytes to flat File."""
    nested = serde.decode(data, FileNested)
    return _file_from_nested(nested)
