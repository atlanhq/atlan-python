# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
QlikSpace asset model with flattened inheritance.

This module provides:
- QlikSpace: Flat asset class (easy to use)
- QlikSpaceAttributes: Nested attributes struct (extends AssetAttributes)
- QlikSpaceNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .qlik_related import RelatedQlikApp, RelatedQlikDataset

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class QlikSpace(Asset):
    """
    Instance of a Qlik space in Atlan.
    """

    # Override type_name with QlikSpace-specific default
    type_name: Union[str, UnsetType] = "QlikSpace"

    qlik_type: Union[str, None, UnsetType] = UNSET
    """Type of this space, for exmaple: Private, Shared, etc."""

    qlik_id: Union[str, None, UnsetType] = UNSET
    """Identifier of this asset, from Qlik."""

    qlik_qri: Union[str, None, UnsetType] = UNSET
    """Unique QRI of this asset, from Qlik."""

    qlik_space_id: Union[str, None, UnsetType] = UNSET
    """Identifier of the space in which this asset exists, from Qlik."""

    qlik_space_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the space in which this asset exists."""

    qlik_app_id: Union[str, None, UnsetType] = UNSET
    """Identifier of the app in which this asset belongs, from Qlik."""

    qlik_app_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the app where this asset belongs."""

    qlik_owner_id: Union[str, None, UnsetType] = UNSET
    """Identifier of the owner of this asset, in Qlik."""

    qlik_is_published: Union[bool, None, UnsetType] = UNSET
    """Whether this asset is published in Qlik (true) or not (false)."""

    qlik_apps: Union[list[RelatedQlikApp], None, UnsetType] = UNSET
    """Apps that exist within this space."""

    qlik_datasets: Union[list[RelatedQlikDataset], None, UnsetType] = UNSET
    """Datasets that exist within this space."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _qlik_space_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "QlikSpace":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            QlikSpace instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _qlik_space_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class QlikSpaceAttributes(AssetAttributes):
    """QlikSpace-specific attributes for nested API format."""

    qlik_type: Union[str, None, UnsetType] = UNSET
    """Type of this space, for exmaple: Private, Shared, etc."""

    qlik_id: Union[str, None, UnsetType] = UNSET
    """Identifier of this asset, from Qlik."""

    qlik_qri: Union[str, None, UnsetType] = UNSET
    """Unique QRI of this asset, from Qlik."""

    qlik_space_id: Union[str, None, UnsetType] = UNSET
    """Identifier of the space in which this asset exists, from Qlik."""

    qlik_space_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the space in which this asset exists."""

    qlik_app_id: Union[str, None, UnsetType] = UNSET
    """Identifier of the app in which this asset belongs, from Qlik."""

    qlik_app_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the app where this asset belongs."""

    qlik_owner_id: Union[str, None, UnsetType] = UNSET
    """Identifier of the owner of this asset, in Qlik."""

    qlik_is_published: Union[bool, None, UnsetType] = UNSET
    """Whether this asset is published in Qlik (true) or not (false)."""


class QlikSpaceRelationshipAttributes(AssetRelationshipAttributes):
    """QlikSpace-specific relationship attributes for nested API format."""

    qlik_apps: Union[list[RelatedQlikApp], None, UnsetType] = UNSET
    """Apps that exist within this space."""

    qlik_datasets: Union[list[RelatedQlikDataset], None, UnsetType] = UNSET
    """Datasets that exist within this space."""


class QlikSpaceNested(AssetNested):
    """QlikSpace in nested API format for high-performance serialization."""

    attributes: Union[QlikSpaceAttributes, UnsetType] = UNSET
    relationship_attributes: Union[QlikSpaceRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[
        QlikSpaceRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        QlikSpaceRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _qlik_space_to_nested(qlik_space: QlikSpace) -> QlikSpaceNested:
    """Convert flat QlikSpace to nested format."""
    attrs = QlikSpaceAttributes(
        qlik_type=qlik_space.qlik_type,
        qlik_id=qlik_space.qlik_id,
        qlik_qri=qlik_space.qlik_qri,
        qlik_space_id=qlik_space.qlik_space_id,
        qlik_space_qualified_name=qlik_space.qlik_space_qualified_name,
        qlik_app_id=qlik_space.qlik_app_id,
        qlik_app_qualified_name=qlik_space.qlik_app_qualified_name,
        qlik_owner_id=qlik_space.qlik_owner_id,
        qlik_is_published=qlik_space.qlik_is_published,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["qlik_apps", "qlik_datasets"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        qlik_space, rel_fields, QlikSpaceRelationshipAttributes
    )
    return QlikSpaceNested(
        guid=qlik_space.guid,
        type_name=qlik_space.type_name,
        status=qlik_space.status,
        version=qlik_space.version,
        create_time=qlik_space.create_time,
        update_time=qlik_space.update_time,
        created_by=qlik_space.created_by,
        updated_by=qlik_space.updated_by,
        classifications=qlik_space.classifications,
        classification_names=qlik_space.classification_names,
        meanings=qlik_space.meanings,
        labels=qlik_space.labels,
        business_attributes=qlik_space.business_attributes,
        custom_attributes=qlik_space.custom_attributes,
        pending_tasks=qlik_space.pending_tasks,
        proxy=qlik_space.proxy,
        is_incomplete=qlik_space.is_incomplete,
        provenance_type=qlik_space.provenance_type,
        home_id=qlik_space.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _qlik_space_from_nested(nested: QlikSpaceNested) -> QlikSpace:
    """Convert nested format to flat QlikSpace."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else QlikSpaceAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["qlik_apps", "qlik_datasets"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        QlikSpaceRelationshipAttributes,
    )
    return QlikSpace(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        qlik_type=attrs.qlik_type,
        qlik_id=attrs.qlik_id,
        qlik_qri=attrs.qlik_qri,
        qlik_space_id=attrs.qlik_space_id,
        qlik_space_qualified_name=attrs.qlik_space_qualified_name,
        qlik_app_id=attrs.qlik_app_id,
        qlik_app_qualified_name=attrs.qlik_app_qualified_name,
        qlik_owner_id=attrs.qlik_owner_id,
        qlik_is_published=attrs.qlik_is_published,
        # Merged relationship attributes
        **merged_rels,
    )


def _qlik_space_to_nested_bytes(qlik_space: QlikSpace, serde: Serde) -> bytes:
    """Convert flat QlikSpace to nested JSON bytes."""
    return serde.encode(_qlik_space_to_nested(qlik_space))


def _qlik_space_from_nested_bytes(data: bytes, serde: Serde) -> QlikSpace:
    """Convert nested JSON bytes to flat QlikSpace."""
    nested = serde.decode(data, QlikSpaceNested)
    return _qlik_space_from_nested(nested)
