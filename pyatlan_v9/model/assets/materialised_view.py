# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
MaterialisedView asset model with flattened inheritance.

This module provides:
- MaterialisedView: Flat asset class (easy to use)
- MaterialisedViewAttributes: Nested attributes struct (extends AssetAttributes)
- MaterialisedViewNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .sql_related import RelatedColumn, RelatedSchema

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class MaterialisedView(Asset):
    """
    Instance of a materialized view in Atlan.
    """

    # Override type_name with MaterialisedView-specific default
    type_name: Union[str, UnsetType] = "MaterialisedView"

    refresh_mode: Union[str, None, UnsetType] = UNSET
    """Refresh mode for this materialized view."""

    refresh_method: Union[str, None, UnsetType] = UNSET
    """Refresh method for this materialized view."""

    staleness: Union[str, None, UnsetType] = UNSET
    """Staleness of this materialized view."""

    stale_since_date: Union[int, None, UnsetType] = UNSET
    """Time (epoch) from which this materialized view is stale, in milliseconds."""

    column_count: Union[int, None, UnsetType] = UNSET
    """Number of columns in this materialized view."""

    row_count: Union[int, None, UnsetType] = UNSET
    """Number of rows in this materialized view."""

    size_bytes: Union[int, None, UnsetType] = UNSET
    """Size of this materialized view, in bytes."""

    is_query_preview: Union[bool, None, UnsetType] = UNSET
    """Whether it's possible to run a preview query on this materialized view (true) or not (false)."""

    query_preview_config: Union[dict[str, str], None, UnsetType] = UNSET
    """Configuration for the query preview of this materialized view."""

    alias: Union[str, None, UnsetType] = UNSET
    """Alias for this materialized view."""

    is_temporary: Union[bool, None, UnsetType] = UNSET
    """Whether this materialized view is temporary (true) or not (false)."""

    definition: Union[str, None, UnsetType] = UNSET
    """SQL definition of this materialized view."""

    query_count: Union[int, None, UnsetType] = UNSET
    """Number of times this asset has been queried."""

    query_user_count: Union[int, None, UnsetType] = UNSET
    """Number of unique users who have queried this asset."""

    query_user_map: Union[dict[str, int], None, UnsetType] = UNSET
    """Map of unique users who have queried this asset to the number of times they have queried it."""

    query_count_updated_at: Union[int, None, UnsetType] = UNSET
    """Time (epoch) at which the query count was last updated, in milliseconds."""

    database_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the database in which this SQL asset exists, or empty if it does not exist within a database."""

    database_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the database in which this SQL asset exists, or empty if it does not exist within a database."""

    schema_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the schema in which this SQL asset exists, or empty if it does not exist within a schema."""

    schema_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the schema in which this SQL asset exists, or empty if it does not exist within a schema."""

    table_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the table in which this SQL asset exists, or empty if it does not exist within a table."""

    table_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the table in which this SQL asset exists, or empty if it does not exist within a table."""

    view_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the view in which this SQL asset exists, or empty if it does not exist within a view."""

    view_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the view in which this SQL asset exists, or empty if it does not exist within a view."""

    calculation_view_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the calculation view in which this SQL asset exists, or empty if it does not exist within a calculation view."""

    calculation_view_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the calculation view in which this SQL asset exists, or empty if it does not exist within a calculation view."""

    is_profiled: Union[bool, None, UnsetType] = UNSET
    """Whether this asset has been profiled (true) or not (false)."""

    last_profiled_at: Union[int, None, UnsetType] = UNSET
    """Time (epoch) at which this asset was last profiled, in milliseconds."""

    sql_ai_model_context_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the context in which the model versions exist, or empty if it does not exist within an AI model context."""

    sql_is_secure: Union[bool, None, UnsetType] = UNSET
    """Whether this asset is secure (true) or not (false)."""

    columns: Union[list[RelatedColumn], None, UnsetType] = UNSET
    """Columns that exist within this materialized view."""

    atlan_schema: Union[RelatedSchema, None, UnsetType] = UNSET
    """Schema in which this materialized view exists."""

    # =========================================================================
    # Convenience Methods
    # =========================================================================

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        name: str,
        schema_qualified_name: str,
        schema_name: str | None = None,
        database_name: str | None = None,
        database_qualified_name: str | None = None,
        connection_qualified_name: str | None = None,
    ) -> "MaterialisedView":
        """
        Create a new MaterialisedView asset with auto-derived fields.

        Args:
            name: Simple name of the materialized view
            schema_qualified_name: Unique name of the schema in which this materialized view exists
            schema_name: Simple name of the schema (auto-derived if not provided)
            database_name: Simple name of the database (auto-derived if not provided)
            database_qualified_name: Unique name of the database (auto-derived if not provided)
            connection_qualified_name: Unique name of the connection (auto-derived if not provided)

        Returns:
            New MaterialisedView instance with all fields populated

        Raises:
            ValueError: If required parameters are missing or invalid
        """
        validate_required_fields(
            ["name", "schema_qualified_name"], [name, schema_qualified_name]
        )

        fields = schema_qualified_name.split("/")
        if len(fields) != 5:
            raise ValueError(
                f"Invalid schema_qualified_name: {schema_qualified_name}. "
                "Expected format: default/connector/connection_id/database/schema"
            )

        connector_name = fields[1]
        connection_qn = (
            connection_qualified_name or f"{fields[0]}/{fields[1]}/{fields[2]}"
        )
        db_name = database_name or fields[3]
        sch_name = schema_name or fields[4]
        db_qualified_name = database_qualified_name or f"{connection_qn}/{db_name}"
        qualified_name = f"{schema_qualified_name}/{name}"

        return cls(
            name=name,
            qualified_name=qualified_name,
            database_name=db_name,
            database_qualified_name=db_qualified_name,
            schema_name=sch_name,
            schema_qualified_name=schema_qualified_name,
            connector_name=connector_name,
            connection_qualified_name=connection_qn,
            atlan_schema=RelatedSchema(qualified_name=schema_qualified_name),
        )

    @classmethod
    def updater(cls, *, qualified_name: str, name: str) -> "MaterialisedView":
        """
        Create a MaterialisedView instance for updating an existing asset.

        Args:
            qualified_name: Unique name of the materialized view to update
            name: Simple name of the materialized view

        Returns:
            MaterialisedView instance configured for updates

        Raises:
            ValueError: If required parameters are missing
        """
        validate_required_fields(["qualified_name", "name"], [qualified_name, name])
        return cls(qualified_name=qualified_name, name=name)

    def trim_to_required(self) -> "MaterialisedView":
        """
        Return a MaterialisedView with only required fields for reference.

        Returns:
            MaterialisedView instance with only qualified_name and name set
        """
        return MaterialisedView(qualified_name=self.qualified_name, name=self.name)

    # Backward compatibility aliases
    @classmethod
    def create(cls, **kwargs) -> "MaterialisedView":
        """Backward compatibility alias for creator()."""
        return cls.creator(**kwargs)

    @classmethod
    def create_for_modification(cls, **kwargs) -> "MaterialisedView":
        """Backward compatibility alias for updater()."""
        return cls.updater(**kwargs)

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _materialised_view_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "MaterialisedView":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            MaterialisedView instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _materialised_view_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class MaterialisedViewAttributes(AssetAttributes):
    """MaterialisedView-specific attributes for nested API format."""

    refresh_mode: Union[str, None, UnsetType] = UNSET
    """Refresh mode for this materialized view."""

    refresh_method: Union[str, None, UnsetType] = UNSET
    """Refresh method for this materialized view."""

    staleness: Union[str, None, UnsetType] = UNSET
    """Staleness of this materialized view."""

    stale_since_date: Union[int, None, UnsetType] = UNSET
    """Time (epoch) from which this materialized view is stale, in milliseconds."""

    column_count: Union[int, None, UnsetType] = UNSET
    """Number of columns in this materialized view."""

    row_count: Union[int, None, UnsetType] = UNSET
    """Number of rows in this materialized view."""

    size_bytes: Union[int, None, UnsetType] = UNSET
    """Size of this materialized view, in bytes."""

    is_query_preview: Union[bool, None, UnsetType] = UNSET
    """Whether it's possible to run a preview query on this materialized view (true) or not (false)."""

    query_preview_config: Union[dict[str, str], None, UnsetType] = UNSET
    """Configuration for the query preview of this materialized view."""

    alias: Union[str, None, UnsetType] = UNSET
    """Alias for this materialized view."""

    is_temporary: Union[bool, None, UnsetType] = UNSET
    """Whether this materialized view is temporary (true) or not (false)."""

    definition: Union[str, None, UnsetType] = UNSET
    """SQL definition of this materialized view."""

    query_count: Union[int, None, UnsetType] = UNSET
    """Number of times this asset has been queried."""

    query_user_count: Union[int, None, UnsetType] = UNSET
    """Number of unique users who have queried this asset."""

    query_user_map: Union[dict[str, int], None, UnsetType] = UNSET
    """Map of unique users who have queried this asset to the number of times they have queried it."""

    query_count_updated_at: Union[int, None, UnsetType] = UNSET
    """Time (epoch) at which the query count was last updated, in milliseconds."""

    database_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the database in which this SQL asset exists, or empty if it does not exist within a database."""

    database_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the database in which this SQL asset exists, or empty if it does not exist within a database."""

    schema_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the schema in which this SQL asset exists, or empty if it does not exist within a schema."""

    schema_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the schema in which this SQL asset exists, or empty if it does not exist within a schema."""

    table_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the table in which this SQL asset exists, or empty if it does not exist within a table."""

    table_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the table in which this SQL asset exists, or empty if it does not exist within a table."""

    view_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the view in which this SQL asset exists, or empty if it does not exist within a view."""

    view_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the view in which this SQL asset exists, or empty if it does not exist within a view."""

    calculation_view_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the calculation view in which this SQL asset exists, or empty if it does not exist within a calculation view."""

    calculation_view_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the calculation view in which this SQL asset exists, or empty if it does not exist within a calculation view."""

    is_profiled: Union[bool, None, UnsetType] = UNSET
    """Whether this asset has been profiled (true) or not (false)."""

    last_profiled_at: Union[int, None, UnsetType] = UNSET
    """Time (epoch) at which this asset was last profiled, in milliseconds."""

    sql_ai_model_context_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the context in which the model versions exist, or empty if it does not exist within an AI model context."""

    sql_is_secure: Union[bool, None, UnsetType] = UNSET
    """Whether this asset is secure (true) or not (false)."""


class MaterialisedViewRelationshipAttributes(AssetRelationshipAttributes):
    """MaterialisedView-specific relationship attributes for nested API format."""

    columns: Union[list[RelatedColumn], None, UnsetType] = UNSET
    """Columns that exist within this materialized view."""

    atlan_schema: Union[RelatedSchema, None, UnsetType] = UNSET
    """Schema in which this materialized view exists."""


class MaterialisedViewNested(AssetNested):
    """MaterialisedView in nested API format for high-performance serialization."""

    attributes: Union[MaterialisedViewAttributes, UnsetType] = UNSET
    relationship_attributes: Union[
        MaterialisedViewRelationshipAttributes, UnsetType
    ] = UNSET
    append_relationship_attributes: Union[
        MaterialisedViewRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        MaterialisedViewRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _materialised_view_to_nested(
    materialised_view: MaterialisedView,
) -> MaterialisedViewNested:
    """Convert flat MaterialisedView to nested format using dynamic field extraction."""
    attrs_kwargs = build_attributes_kwargs(
        materialised_view, MaterialisedViewAttributes
    )
    attrs = MaterialisedViewAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["columns", "atlan_schema"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        materialised_view, rel_fields, MaterialisedViewRelationshipAttributes
    )
    return MaterialisedViewNested(
        guid=materialised_view.guid,
        type_name=materialised_view.type_name,
        status=materialised_view.status,
        version=materialised_view.version,
        create_time=materialised_view.create_time,
        update_time=materialised_view.update_time,
        created_by=materialised_view.created_by,
        updated_by=materialised_view.updated_by,
        classifications=materialised_view.classifications,
        classification_names=materialised_view.classification_names,
        meanings=materialised_view.meanings,
        labels=materialised_view.labels,
        business_attributes=materialised_view.business_attributes,
        custom_attributes=materialised_view.custom_attributes,
        pending_tasks=materialised_view.pending_tasks,
        proxy=materialised_view.proxy,
        is_incomplete=materialised_view.is_incomplete,
        provenance_type=materialised_view.provenance_type,
        home_id=materialised_view.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _materialised_view_from_nested(nested: MaterialisedViewNested) -> MaterialisedView:
    """Convert nested format to flat MaterialisedView using dynamic field extraction."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else MaterialisedViewAttributes()
    )

    # Merge relationships from all three buckets
    rel_fields: list[str] = ["columns", "atlan_schema"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        MaterialisedViewRelationshipAttributes,
    )

    # Build flat kwargs using dynamic field extraction
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, AssetNested, MaterialisedViewAttributes
    )

    return MaterialisedView(**kwargs)


def _materialised_view_to_nested_bytes(
    materialised_view: MaterialisedView, serde: Serde
) -> bytes:
    """Convert flat MaterialisedView to nested JSON bytes."""
    return serde.encode(_materialised_view_to_nested(materialised_view))


def _materialised_view_from_nested_bytes(data: bytes, serde: Serde) -> MaterialisedView:
    """Convert nested JSON bytes to flat MaterialisedView."""
    nested = serde.decode(data, MaterialisedViewNested)
    return _materialised_view_from_nested(nested)
