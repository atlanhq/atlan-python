# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Kafka asset model with flattened inheritance.

This module provides:
- Kafka: Flat asset class (easy to use)
- KafkaAttributes: Nested attributes struct (extends AssetAttributes)
- KafkaNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Kafka(Asset):
    """
    Base class for Kafka assets.
    """

    # Override type_name with Kafka-specific default
    type_name: Union[str, UnsetType] = "Kafka"

    pass

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _kafka_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(json_data: Union[str, bytes], serde: Serde | None = None) -> "Kafka":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Kafka instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _kafka_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class KafkaAttributes(AssetAttributes):
    """Kafka-specific attributes for nested API format."""

    pass


class KafkaRelationshipAttributes(AssetRelationshipAttributes):
    """Kafka-specific relationship attributes for nested API format."""

    pass


class KafkaNested(AssetNested):
    """Kafka in nested API format for high-performance serialization."""

    attributes: Union[KafkaAttributes, UnsetType] = UNSET
    relationship_attributes: Union[KafkaRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[KafkaRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[KafkaRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _kafka_to_nested(kafka: Kafka) -> KafkaNested:
    """Convert flat Kafka to nested format."""
    attrs = KafkaAttributes()
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        kafka, rel_fields, KafkaRelationshipAttributes
    )
    return KafkaNested(
        guid=kafka.guid,
        type_name=kafka.type_name,
        status=kafka.status,
        version=kafka.version,
        create_time=kafka.create_time,
        update_time=kafka.update_time,
        created_by=kafka.created_by,
        updated_by=kafka.updated_by,
        classifications=kafka.classifications,
        classification_names=kafka.classification_names,
        meanings=kafka.meanings,
        labels=kafka.labels,
        business_attributes=kafka.business_attributes,
        custom_attributes=kafka.custom_attributes,
        pending_tasks=kafka.pending_tasks,
        proxy=kafka.proxy,
        is_incomplete=kafka.is_incomplete,
        provenance_type=kafka.provenance_type,
        home_id=kafka.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _kafka_from_nested(nested: KafkaNested) -> Kafka:
    """Convert nested format to flat Kafka."""
    attrs = nested.attributes if nested.attributes is not UNSET else KafkaAttributes()
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        KafkaRelationshipAttributes,
    )
    return Kafka(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _kafka_to_nested_bytes(kafka: Kafka, serde: Serde) -> bytes:
    """Convert flat Kafka to nested JSON bytes."""
    return serde.encode(_kafka_to_nested(kafka))


def _kafka_from_nested_bytes(data: bytes, serde: Serde) -> Kafka:
    """Convert nested JSON bytes to flat Kafka."""
    nested = serde.decode(data, KafkaNested)
    return _kafka_from_nested(nested)
