# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
DataProduct asset model with flattened inheritance.

This module provides:
- DataProduct: Flat asset class (easy to use)
- DataProductAttributes: Nested attributes struct (extends AssetAttributes)
- DataProductNested: Nested API format struct
"""

from __future__ import annotations

import re
from typing import TYPE_CHECKING, Union

import msgspec
from msgspec import UNSET, UnsetType

from pyatlan.errors import ErrorCode
from pyatlan.model.data_mesh import DataProductsAssetsDSL
from pyatlan.model.enums import DataProductStatus
from pyatlan.model.search import IndexSearchRequest
from pyatlan_v9.model.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .asset_related import RelatedAsset
from .data_mesh_related import RelatedDataDomain

if TYPE_CHECKING:
    from pyatlan.client.atlan import AtlanClient


# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class DataProduct(Asset):
    """
    Instance of a data product in Atlan.
    """

    # Override type_name with DataProduct-specific default
    type_name: Union[str, UnsetType] = "DataProduct"

    data_product_status: Union[str, None, UnsetType] = UNSET
    """Status of this data product."""

    daap_status: Union[str, None, UnsetType] = UNSET
    """Status of this data product."""

    data_product_criticality: Union[str, None, UnsetType] = UNSET
    """Criticality of this data product."""

    daap_criticality: Union[str, None, UnsetType] = UNSET
    """Criticality of this data product."""

    data_product_sensitivity: Union[str, None, UnsetType] = UNSET
    """Information sensitivity of this data product."""

    daap_sensitivity: Union[str, None, UnsetType] = UNSET
    """Information sensitivity of this data product."""

    data_product_visibility: Union[str, None, UnsetType] = UNSET
    """Visibility of a data product."""

    daap_visibility: Union[str, None, UnsetType] = UNSET
    """Visibility of a data product."""

    data_product_assets_dsl: Union[str, None, UnsetType] = UNSET
    """Search DSL used to define which assets are part of this data product."""

    data_product_assets_playbook_filter: Union[str, None, UnsetType] = UNSET
    """Playbook filter to define which assets are part of this data product."""

    data_product_score_value: Union[float, None, UnsetType] = UNSET
    """Score of this data product."""

    data_mesh_score_updated_at: Union[int, None, UnsetType] = UNSET
    """Timestamp when the score of this data product was last updated."""

    daap_visibility_users: Union[list[str], None, UnsetType] = UNSET
    """list of users for product visibility control"""

    daap_visibility_groups: Union[list[str], None, UnsetType] = UNSET
    """list of groups for product visibility control"""

    daap_output_port_guids: Union[list[str], None, UnsetType] = UNSET
    """Output ports guids for this data product."""

    daap_input_port_guids: Union[list[str], None, UnsetType] = UNSET
    """Input ports guids for this data product."""

    daap_lineage_status: Union[str, None, UnsetType] = UNSET
    """Status of this data product lineage."""

    parent_domain_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the parent domain in which this asset exists."""

    super_domain_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the top-level domain in which this asset exists."""

    data_domain: Union[RelatedDataDomain, None, UnsetType] = UNSET
    """Data domain in which this data product exists."""

    output_ports: Union[list[RelatedAsset], None, UnsetType] = UNSET
    """Output ports for this data product."""

    input_ports: Union[list[RelatedAsset], None, UnsetType] = UNSET
    """Input ports for this data product."""

    # =========================================================================
    # Convenience Methods
    # =========================================================================

    @classmethod
    def _get_super_domain_qualified_name(
        cls, domain_qualified_name: str
    ) -> Union[str, None]:
        """Extract the top-most ancestor domain qualified name."""
        domain_qn_prefix = re.compile(r"(default/domain/[a-zA-Z0-9-]+/super)/.*")
        if domain_qualified_name:
            match = domain_qn_prefix.match(domain_qualified_name)
            if match and match.group(1):
                return match.group(1)
            if domain_qualified_name.startswith("default/domain/"):
                return domain_qualified_name
        return None

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        name: str,
        domain_qualified_name: str,
        asset_selection: IndexSearchRequest,
    ) -> "DataProduct":
        """Create a new DataProduct asset."""
        validate_required_fields(
            ["name", "domain_qualified_name", "asset_selection"],
            [name, domain_qualified_name, asset_selection],
        )
        assets_playbook_filter = '{"condition":"AND","isGroupLocked":false,"rules":[]}'
        return cls(
            name=name,
            data_product_assets_dsl=DataProductsAssetsDSL.get_asset_selection(
                asset_selection
            ),
            data_domain=RelatedDataDomain(
                unique_attributes={"qualifiedName": domain_qualified_name}
            ),
            qualified_name=f"{domain_qualified_name}/product/{name}",
            data_product_assets_playbook_filter=assets_playbook_filter,
            parent_domain_qualified_name=domain_qualified_name,
            super_domain_qualified_name=cls._get_super_domain_qualified_name(
                domain_qualified_name
            ),
            daap_status=DataProductStatus.ACTIVE,
        )

    @classmethod
    @init_guid
    def updater(
        cls,
        *,
        qualified_name: str,
        name: str,
        asset_selection: Union[IndexSearchRequest, None] = None,
    ) -> "DataProduct":
        """Create a DataProduct instance for update operations."""
        validate_required_fields(["name", "qualified_name"], [name, qualified_name])
        fields = qualified_name.split("/")
        if len(fields) < 5:
            raise ValueError(f"Invalid data product qualified_name: {qualified_name}")
        product = cls(qualified_name=qualified_name, name=name)
        if asset_selection:
            product.data_product_assets_dsl = DataProductsAssetsDSL.get_asset_selection(
                asset_selection
            )
        return product

    def trim_to_required(self) -> "DataProduct":
        """Return only the required fields for updates."""
        return DataProduct.updater(qualified_name=self.qualified_name, name=self.name)

    def get_assets(self, client: "AtlanClient"):
        """Retrieve assets linked to this data product."""
        dp_dsl = self.data_product_assets_dsl
        if not dp_dsl:
            raise ErrorCode.MISSING_DATA_PRODUCT_ASSET_DSL.exception_with_parameters()
        request = IndexSearchRequest(**msgspec.json.decode(dp_dsl).get("query", {}))
        return client.asset.search(request)

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _data_product_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "DataProduct":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            DataProduct instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _data_product_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class DataProductAttributes(AssetAttributes):
    """DataProduct-specific attributes for nested API format."""

    data_product_status: Union[str, None, UnsetType] = UNSET
    """Status of this data product."""

    daap_status: Union[str, None, UnsetType] = UNSET
    """Status of this data product."""

    data_product_criticality: Union[str, None, UnsetType] = UNSET
    """Criticality of this data product."""

    daap_criticality: Union[str, None, UnsetType] = UNSET
    """Criticality of this data product."""

    data_product_sensitivity: Union[str, None, UnsetType] = UNSET
    """Information sensitivity of this data product."""

    daap_sensitivity: Union[str, None, UnsetType] = UNSET
    """Information sensitivity of this data product."""

    data_product_visibility: Union[str, None, UnsetType] = UNSET
    """Visibility of a data product."""

    daap_visibility: Union[str, None, UnsetType] = UNSET
    """Visibility of a data product."""

    data_product_assets_dsl: Union[str, None, UnsetType] = UNSET
    """Search DSL used to define which assets are part of this data product."""

    data_product_assets_playbook_filter: Union[str, None, UnsetType] = UNSET
    """Playbook filter to define which assets are part of this data product."""

    data_product_score_value: Union[float, None, UnsetType] = UNSET
    """Score of this data product."""

    data_mesh_score_updated_at: Union[int, None, UnsetType] = UNSET
    """Timestamp when the score of this data product was last updated."""

    daap_visibility_users: Union[list[str], None, UnsetType] = UNSET
    """list of users for product visibility control"""

    daap_visibility_groups: Union[list[str], None, UnsetType] = UNSET
    """list of groups for product visibility control"""

    daap_output_port_guids: Union[list[str], None, UnsetType] = UNSET
    """Output ports guids for this data product."""

    daap_input_port_guids: Union[list[str], None, UnsetType] = UNSET
    """Input ports guids for this data product."""

    daap_lineage_status: Union[str, None, UnsetType] = UNSET
    """Status of this data product lineage."""

    parent_domain_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the parent domain in which this asset exists."""

    super_domain_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the top-level domain in which this asset exists."""


class DataProductRelationshipAttributes(AssetRelationshipAttributes):
    """DataProduct-specific relationship attributes for nested API format."""

    data_domain: Union[RelatedDataDomain, None, UnsetType] = UNSET
    """Data domain in which this data product exists."""

    output_ports: Union[list[RelatedAsset], None, UnsetType] = UNSET
    """Output ports for this data product."""

    input_ports: Union[list[RelatedAsset], None, UnsetType] = UNSET
    """Input ports for this data product."""


class DataProductNested(AssetNested):
    """DataProduct in nested API format for high-performance serialization."""

    attributes: Union[DataProductAttributes, UnsetType] = UNSET
    relationship_attributes: Union[DataProductRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[
        DataProductRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        DataProductRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _data_product_to_nested(data_product: DataProduct) -> DataProductNested:
    """Convert flat DataProduct to nested format."""
    attrs_kwargs = build_attributes_kwargs(data_product, DataProductAttributes)
    attrs = DataProductAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["data_domain", "output_ports", "input_ports"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        data_product, rel_fields, DataProductRelationshipAttributes
    )
    return DataProductNested(
        guid=data_product.guid,
        type_name=data_product.type_name,
        status=data_product.status,
        version=data_product.version,
        create_time=data_product.create_time,
        update_time=data_product.update_time,
        created_by=data_product.created_by,
        updated_by=data_product.updated_by,
        classifications=data_product.classifications,
        classification_names=data_product.classification_names,
        meanings=data_product.meanings,
        labels=data_product.labels,
        business_attributes=data_product.business_attributes,
        custom_attributes=data_product.custom_attributes,
        pending_tasks=data_product.pending_tasks,
        proxy=data_product.proxy,
        is_incomplete=data_product.is_incomplete,
        provenance_type=data_product.provenance_type,
        home_id=data_product.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _data_product_from_nested(nested: DataProductNested) -> DataProduct:
    """Convert nested format to flat DataProduct."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else DataProductAttributes()
    )
    rel_fields: list[str] = ["data_domain", "output_ports", "input_ports"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        DataProductRelationshipAttributes,
    )
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, AssetNested, DataProductAttributes
    )
    return DataProduct(**kwargs)


def _data_product_to_nested_bytes(data_product: DataProduct, serde: Serde) -> bytes:
    """Convert flat DataProduct to nested JSON bytes."""
    return serde.encode(_data_product_to_nested(data_product))


def _data_product_from_nested_bytes(data: bytes, serde: Serde) -> DataProduct:
    """Convert nested JSON bytes to flat DataProduct."""
    nested = serde.decode(data, DataProductNested)
    return _data_product_from_nested(nested)
