# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Related type classes for WorkflowNotification module.

This module contains all Related{Type} classes for the WorkflowNotification type hierarchy.
These classes are used for relationship attributes to reference related entities.
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from .notification_related import RelatedNotification
from .referenceable_related import RelatedReferenceable

__all__ = [
    "RelatedWorkflowNotification",
]


class RelatedWorkflowNotification(RelatedNotification):
    """
    Related entity reference for WorkflowNotification assets.

    Extends RelatedNotification with WorkflowNotification-specific attributes.
    """

    # type_name inherited from parent with default=UNSET
    # __post_init__ sets it to "WorkflowNotification" so it serializes correctly

    workflow_notification_workflow_slug: Union[str, None, UnsetType] = UNSET
    """Slug of the workflow this notification is about. What grouping by workflow groups on, and how a check finds the live notification before deciding whether to raise another."""

    workflow_notification_last_preflight_result: Union[str, None, UnsetType] = UNSET
    """The whole check response as one escaped JSON string, rewritten on every check while the notification is live. Rendered by the widget the interface already has, so nothing is denormalised into attributes."""

    workflow_notification_preflight_failure_count: Union[int, None, UnsetType] = UNSET
    """How many times the check has failed for this notification, counted from the failures that raised it and incremented on every failure since."""

    workflow_notification_last_preflight_failed_at: Union[int, None, UnsetType] = UNSET
    """When the check last failed. Distinct from when it was last checked, which every write already records."""

    workflow_notification_action: Union[str, None, UnsetType] = UNSET
    """Which option the person took. Declared here rather than on the notification supertype because these values only mean anything for a workflow."""

    def __post_init__(self) -> None:
        RelatedReferenceable.__post_init__(self)
        self.type_name = "WorkflowNotification"
