# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
PresetChart asset model with flattened inheritance.

This module provides:
- PresetChart: Flat asset class (easy to use)
- PresetChartAttributes: Nested attributes struct (extends AssetAttributes)
- PresetChartNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .preset_related import RelatedPresetDashboard

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class PresetChart(Asset):
    """
    Instance of a Preset chart in Atlan.
    """

    # Override type_name with PresetChart-specific default
    type_name: Union[str, UnsetType] = "PresetChart"

    preset_chart_description_markdown: Union[str, None, UnsetType] = UNSET
    """"""

    preset_chart_form_data: Union[dict[str, str], None, UnsetType] = UNSET
    """"""

    preset_workspace_id: Union[int, None, UnsetType] = UNSET
    """Identifier of the workspace in which this asset exists, in Preset."""

    preset_workspace_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the workspace in which this asset exists."""

    preset_dashboard_id: Union[int, None, UnsetType] = UNSET
    """Identifier of the dashboard in which this asset exists, in Preset."""

    preset_dashboard_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the dashboard in which this asset exists."""

    preset_dashboard: Union[RelatedPresetDashboard, None, UnsetType] = UNSET
    """Dashboard in which this chart exists."""

    # =========================================================================
    # Convenience Methods
    # =========================================================================

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        name: str,
        preset_dashboard_qualified_name: str,
        connection_qualified_name: str | None = None,
    ) -> "PresetChart":
        validate_required_fields(
            ["name", "preset_dashboard_qualified_name"],
            [name, preset_dashboard_qualified_name],
        )
        fields = preset_dashboard_qualified_name.split("/")
        connector_name = fields[1] if len(fields) > 1 else None
        connection_qn = connection_qualified_name or (
            f"{fields[0]}/{fields[1]}/{fields[2]}" if len(fields) >= 3 else None
        )
        return cls(
            name=name,
            qualified_name=f"{preset_dashboard_qualified_name}/{name}",
            preset_dashboard_qualified_name=preset_dashboard_qualified_name,
            connection_qualified_name=connection_qn,
            connector_name=connector_name,
            preset_dashboard=RelatedPresetDashboard(
                qualified_name=preset_dashboard_qualified_name
            ),
        )

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _preset_chart_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "PresetChart":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            PresetChart instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _preset_chart_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class PresetChartAttributes(AssetAttributes):
    """PresetChart-specific attributes for nested API format."""

    preset_chart_description_markdown: Union[str, None, UnsetType] = UNSET
    """"""

    preset_chart_form_data: Union[dict[str, str], None, UnsetType] = UNSET
    """"""

    preset_workspace_id: Union[int, None, UnsetType] = UNSET
    """Identifier of the workspace in which this asset exists, in Preset."""

    preset_workspace_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the workspace in which this asset exists."""

    preset_dashboard_id: Union[int, None, UnsetType] = UNSET
    """Identifier of the dashboard in which this asset exists, in Preset."""

    preset_dashboard_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the dashboard in which this asset exists."""


class PresetChartRelationshipAttributes(AssetRelationshipAttributes):
    """PresetChart-specific relationship attributes for nested API format."""

    preset_dashboard: Union[RelatedPresetDashboard, None, UnsetType] = UNSET
    """Dashboard in which this chart exists."""


class PresetChartNested(AssetNested):
    """PresetChart in nested API format for high-performance serialization."""

    attributes: Union[PresetChartAttributes, UnsetType] = UNSET
    relationship_attributes: Union[PresetChartRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[
        PresetChartRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        PresetChartRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _preset_chart_to_nested(preset_chart: PresetChart) -> PresetChartNested:
    """Convert flat PresetChart to nested format."""
    attrs_kwargs = build_attributes_kwargs(preset_chart, PresetChartAttributes)
    attrs = PresetChartAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["preset_dashboard"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        preset_chart, rel_fields, PresetChartRelationshipAttributes
    )
    return PresetChartNested(
        guid=preset_chart.guid,
        type_name=preset_chart.type_name,
        status=preset_chart.status,
        version=preset_chart.version,
        create_time=preset_chart.create_time,
        update_time=preset_chart.update_time,
        created_by=preset_chart.created_by,
        updated_by=preset_chart.updated_by,
        classifications=preset_chart.classifications,
        classification_names=preset_chart.classification_names,
        meanings=preset_chart.meanings,
        labels=preset_chart.labels,
        business_attributes=preset_chart.business_attributes,
        custom_attributes=preset_chart.custom_attributes,
        pending_tasks=preset_chart.pending_tasks,
        proxy=preset_chart.proxy,
        is_incomplete=preset_chart.is_incomplete,
        provenance_type=preset_chart.provenance_type,
        home_id=preset_chart.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _preset_chart_from_nested(nested: PresetChartNested) -> PresetChart:
    """Convert nested format to flat PresetChart."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else PresetChartAttributes()
    )
    rel_fields: list[str] = ["preset_dashboard"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        PresetChartRelationshipAttributes,
    )
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, AssetNested, PresetChartAttributes
    )
    return PresetChart(**kwargs)


def _preset_chart_to_nested_bytes(preset_chart: PresetChart, serde: Serde) -> bytes:
    """Convert flat PresetChart to nested JSON bytes."""
    return serde.encode(_preset_chart_to_nested(preset_chart))


def _preset_chart_from_nested_bytes(data: bytes, serde: Serde) -> PresetChart:
    """Convert nested JSON bytes to flat PresetChart."""
    nested = serde.decode(data, PresetChartNested)
    return _preset_chart_from_nested(nested)
