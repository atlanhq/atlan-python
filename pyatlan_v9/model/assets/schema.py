# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Schema asset model with flattened inheritance.

This module provides:
- Schema: Flat asset class (easy to use)
- SchemaAttributes: Nested attributes struct (extends AssetAttributes)
- SchemaNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .sql_related import (
    RelatedCalculationView,
    RelatedDatabase,
    RelatedFunction,
    RelatedMaterialisedView,
    RelatedProcedure,
    RelatedTable,
    RelatedView,
)

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Schema(Asset):
    """
    Instance of a database schema in Atlan.
    """

    # Override type_name with Schema-specific default
    type_name: Union[str, UnsetType] = "Schema"

    table_count: Union[int, None, UnsetType] = UNSET
    """Number of tables in this schema."""

    sql_external_location: Union[str, None, UnsetType] = UNSET
    """External location of this schema, for example: an S3 object location."""

    views_count: Union[int, None, UnsetType] = UNSET
    """Number of views in this schema."""

    linked_schema_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the Linked Schema on which this Schema is dependent. This concept is mostly applicable for linked datasets/datasource in Google BigQuery via Analytics Hub Listing"""

    query_count: Union[int, None, UnsetType] = UNSET
    """Number of times this asset has been queried."""

    query_user_count: Union[int, None, UnsetType] = UNSET
    """Number of unique users who have queried this asset."""

    query_user_map: Union[dict[str, int], None, UnsetType] = UNSET
    """Map of unique users who have queried this asset to the number of times they have queried it."""

    query_count_updated_at: Union[int, None, UnsetType] = UNSET
    """Time (epoch) at which the query count was last updated, in milliseconds."""

    database_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the database in which this SQL asset exists, or empty if it does not exist within a database."""

    database_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the database in which this SQL asset exists, or empty if it does not exist within a database."""

    schema_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the schema in which this SQL asset exists, or empty if it does not exist within a schema."""

    schema_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the schema in which this SQL asset exists, or empty if it does not exist within a schema."""

    table_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the table in which this SQL asset exists, or empty if it does not exist within a table."""

    table_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the table in which this SQL asset exists, or empty if it does not exist within a table."""

    view_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the view in which this SQL asset exists, or empty if it does not exist within a view."""

    view_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the view in which this SQL asset exists, or empty if it does not exist within a view."""

    calculation_view_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the calculation view in which this SQL asset exists, or empty if it does not exist within a calculation view."""

    calculation_view_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the calculation view in which this SQL asset exists, or empty if it does not exist within a calculation view."""

    is_profiled: Union[bool, None, UnsetType] = UNSET
    """Whether this asset has been profiled (true) or not (false)."""

    last_profiled_at: Union[int, None, UnsetType] = UNSET
    """Time (epoch) at which this asset was last profiled, in milliseconds."""

    sql_ai_model_context_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the context in which the model versions exist, or empty if it does not exist within an AI model context."""

    sql_is_secure: Union[bool, None, UnsetType] = UNSET
    """Whether this asset is secure (true) or not (false)."""

    calculation_views: Union[list[RelatedCalculationView], None, UnsetType] = UNSET
    """Calculation views that exist within this schema."""

    functions: Union[list[RelatedFunction], None, UnsetType] = UNSET
    """Functions that exist within this schema."""

    materialised_views: Union[list[RelatedMaterialisedView], None, UnsetType] = UNSET
    """Materialized views that exist within this schema."""

    procedures: Union[list[RelatedProcedure], None, UnsetType] = UNSET
    """Stored procedures that exist within this schema."""

    database: Union[RelatedDatabase, None, UnsetType] = UNSET
    """Database in which this schema exists."""

    tables: Union[list[RelatedTable], None, UnsetType] = UNSET
    """Tables that exist within this schema."""

    views: Union[list[RelatedView], None, UnsetType] = UNSET
    """Views that exist within this schema."""

    # =========================================================================
    # Convenience Methods
    # =========================================================================

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        name: str,
        database_qualified_name: str,
        database_name: str | None = None,
        connection_qualified_name: str | None = None,
    ) -> "Schema":
        """
        Create a new Schema asset with auto-derived fields.

        Args:
            name: Simple name of the schema
            database_qualified_name: Unique name of the database in which this schema exists
            database_name: Simple name of the database (auto-derived if not provided)
            connection_qualified_name: Unique name of the connection (auto-derived if not provided)

        Returns:
            New Schema instance with all fields populated

        Raises:
            ValueError: If required parameters are missing or invalid
        """
        validate_required_fields(
            ["name", "database_qualified_name"], [name, database_qualified_name]
        )

        # Validate database_qualified_name format: default/connector/connection_id/database
        fields = database_qualified_name.split("/")
        if len(fields) != 4:
            raise ValueError(
                f"Invalid database_qualified_name: {database_qualified_name}. "
                "Expected format: default/connector/connection_id/database"
            )

        # Derive other fields from database_qualified_name
        connector_name = fields[1]
        connection_qn = (
            connection_qualified_name or f"{fields[0]}/{fields[1]}/{fields[2]}"
        )
        db_name = database_name or fields[3]
        qualified_name = f"{database_qualified_name}/{name}"

        return cls(
            name=name,
            qualified_name=qualified_name,
            database_name=db_name,
            database_qualified_name=database_qualified_name,
            connector_name=connector_name,
            connection_qualified_name=connection_qn,
            database=RelatedDatabase(qualified_name=database_qualified_name),
        )

    @classmethod
    def updater(cls, *, qualified_name: str, name: str) -> "Schema":
        """
        Create a Schema instance for updating an existing asset.

        Args:
            qualified_name: Unique name of the schema to update
            name: Simple name of the schema

        Returns:
            Schema instance configured for updates

        Raises:
            ValueError: If required parameters are missing
        """
        validate_required_fields(["qualified_name", "name"], [qualified_name, name])
        return cls(qualified_name=qualified_name, name=name)

    def trim_to_required(self) -> "Schema":
        """
        Return a Schema with only required fields for reference.

        Returns:
            Schema instance with only qualified_name and name set
        """
        return Schema(qualified_name=self.qualified_name, name=self.name)

    # Backward compatibility aliases
    @classmethod
    def create(cls, **kwargs) -> "Schema":
        """Backward compatibility alias for creator()."""
        return cls.creator(**kwargs)

    @classmethod
    def create_for_modification(cls, **kwargs) -> "Schema":
        """Backward compatibility alias for updater()."""
        return cls.updater(**kwargs)

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _schema__to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(json_data: Union[str, bytes], serde: Serde | None = None) -> "Schema":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Schema instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _schema__from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class SchemaAttributes(AssetAttributes):
    """Schema-specific attributes for nested API format."""

    table_count: Union[int, None, UnsetType] = UNSET
    """Number of tables in this schema."""

    sql_external_location: Union[str, None, UnsetType] = UNSET
    """External location of this schema, for example: an S3 object location."""

    views_count: Union[int, None, UnsetType] = UNSET
    """Number of views in this schema."""

    linked_schema_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the Linked Schema on which this Schema is dependent. This concept is mostly applicable for linked datasets/datasource in Google BigQuery via Analytics Hub Listing"""

    query_count: Union[int, None, UnsetType] = UNSET
    """Number of times this asset has been queried."""

    query_user_count: Union[int, None, UnsetType] = UNSET
    """Number of unique users who have queried this asset."""

    query_user_map: Union[dict[str, int], None, UnsetType] = UNSET
    """Map of unique users who have queried this asset to the number of times they have queried it."""

    query_count_updated_at: Union[int, None, UnsetType] = UNSET
    """Time (epoch) at which the query count was last updated, in milliseconds."""

    database_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the database in which this SQL asset exists, or empty if it does not exist within a database."""

    database_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the database in which this SQL asset exists, or empty if it does not exist within a database."""

    schema_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the schema in which this SQL asset exists, or empty if it does not exist within a schema."""

    schema_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the schema in which this SQL asset exists, or empty if it does not exist within a schema."""

    table_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the table in which this SQL asset exists, or empty if it does not exist within a table."""

    table_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the table in which this SQL asset exists, or empty if it does not exist within a table."""

    view_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the view in which this SQL asset exists, or empty if it does not exist within a view."""

    view_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the view in which this SQL asset exists, or empty if it does not exist within a view."""

    calculation_view_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the calculation view in which this SQL asset exists, or empty if it does not exist within a calculation view."""

    calculation_view_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the calculation view in which this SQL asset exists, or empty if it does not exist within a calculation view."""

    is_profiled: Union[bool, None, UnsetType] = UNSET
    """Whether this asset has been profiled (true) or not (false)."""

    last_profiled_at: Union[int, None, UnsetType] = UNSET
    """Time (epoch) at which this asset was last profiled, in milliseconds."""

    sql_ai_model_context_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the context in which the model versions exist, or empty if it does not exist within an AI model context."""

    sql_is_secure: Union[bool, None, UnsetType] = UNSET
    """Whether this asset is secure (true) or not (false)."""


class SchemaRelationshipAttributes(AssetRelationshipAttributes):
    """Schema-specific relationship attributes for nested API format."""

    calculation_views: Union[list[RelatedCalculationView], None, UnsetType] = UNSET
    """Calculation views that exist within this schema."""

    functions: Union[list[RelatedFunction], None, UnsetType] = UNSET
    """Functions that exist within this schema."""

    materialised_views: Union[list[RelatedMaterialisedView], None, UnsetType] = UNSET
    """Materialized views that exist within this schema."""

    procedures: Union[list[RelatedProcedure], None, UnsetType] = UNSET
    """Stored procedures that exist within this schema."""

    database: Union[RelatedDatabase, None, UnsetType] = UNSET
    """Database in which this schema exists."""

    tables: Union[list[RelatedTable], None, UnsetType] = UNSET
    """Tables that exist within this schema."""

    views: Union[list[RelatedView], None, UnsetType] = UNSET
    """Views that exist within this schema."""


class SchemaNested(AssetNested):
    """Schema in nested API format for high-performance serialization."""

    attributes: Union[SchemaAttributes, UnsetType] = UNSET
    relationship_attributes: Union[SchemaRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[SchemaRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[SchemaRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _schema__to_nested(schema_: Schema) -> SchemaNested:
    """Convert flat Schema to nested format using dynamic field extraction."""
    # Build attributes using dynamic field extraction
    attrs_kwargs = build_attributes_kwargs(schema_, SchemaAttributes)
    attrs = SchemaAttributes(**attrs_kwargs)

    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = [
        "calculation_views",
        "functions",
        "materialised_views",
        "procedures",
        "database",
        "tables",
        "views",
    ]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        schema_, rel_fields, SchemaRelationshipAttributes
    )

    return SchemaNested(
        guid=schema_.guid,
        type_name=schema_.type_name,
        status=schema_.status,
        version=schema_.version,
        create_time=schema_.create_time,
        update_time=schema_.update_time,
        created_by=schema_.created_by,
        updated_by=schema_.updated_by,
        classifications=schema_.classifications,
        classification_names=schema_.classification_names,
        meanings=schema_.meanings,
        labels=schema_.labels,
        business_attributes=schema_.business_attributes,
        custom_attributes=schema_.custom_attributes,
        pending_tasks=schema_.pending_tasks,
        proxy=schema_.proxy,
        is_incomplete=schema_.is_incomplete,
        provenance_type=schema_.provenance_type,
        home_id=schema_.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _schema__from_nested(nested: SchemaNested) -> Schema:
    """Convert nested format to flat Schema using dynamic field extraction."""
    attrs = nested.attributes if nested.attributes is not UNSET else SchemaAttributes()

    # Merge relationships from all three buckets
    rel_fields: list[str] = [
        "calculation_views",
        "functions",
        "materialised_views",
        "procedures",
        "database",
        "tables",
        "views",
    ]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        SchemaRelationshipAttributes,
    )

    # Build flat kwargs using dynamic field extraction
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, AssetNested, SchemaAttributes
    )

    return Schema(**kwargs)


def _schema__to_nested_bytes(schema_: Schema, serde: Serde) -> bytes:
    """Convert flat Schema to nested JSON bytes."""
    return serde.encode(_schema__to_nested(schema_))


def _schema__from_nested_bytes(data: bytes, serde: Serde) -> Schema:
    """Convert nested JSON bytes to flat Schema."""
    nested = serde.decode(data, SchemaNested)
    return _schema__from_nested(nested)
