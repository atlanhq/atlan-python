# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
GCSObject asset model with flattened inheritance.

This module provides:
- GCSObject: Flat asset class (easy to use)
- GCSObjectAttributes: Nested attributes struct (extends AssetAttributes)
- GCSObjectNested: Nested API format struct
"""

from __future__ import annotations

from typing import Any, Union

from msgspec import UNSET, UnsetType

from pyatlan.model.utils import construct_object_key
from pyatlan_v9.model.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .gcs_related import RelatedGCSBucket

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class GCSObject(Asset):
    """
    Instance of a Google Cloud Storage object in Atlan.
    """

    # Override type_name with GCSObject-specific default
    type_name: Union[str, UnsetType] = "GCSObject"

    gcs_bucket_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the bucket in which this object exists."""

    gcs_bucket_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the bucket in which this object exists."""

    gcs_object_size: Union[int, None, UnsetType] = UNSET
    """Object size in bytes."""

    gcs_object_key: Union[str, None, UnsetType] = UNSET
    """Key of this object, in GCS."""

    gcs_object_media_link: Union[str, None, UnsetType] = UNSET
    """Media link to this object."""

    gcs_object_hold_type: Union[str, None, UnsetType] = UNSET
    """Type of hold on this object."""

    gcs_object_generation_id: Union[int, None, UnsetType] = UNSET
    """Generation ID of this object."""

    gcs_object_crc32_c_hash: Union[str, None, UnsetType] = UNSET
    """CRC32C hash of this object."""

    gcs_object_md5_hash: Union[str, None, UnsetType] = UNSET
    """MD5 hash of this object."""

    gcs_object_data_last_modified_time: Union[int, None, UnsetType] = UNSET
    """Time (epoch) at which this object's data was last modified, in milliseconds."""

    gcs_object_content_type: Union[str, None, UnsetType] = UNSET
    """Type of content in this object."""

    gcs_object_content_encoding: Union[str, None, UnsetType] = UNSET
    """Content encoding of this object."""

    gcs_object_content_disposition: Union[str, None, UnsetType] = UNSET
    """Information about how this object's content should be presented."""

    gcs_object_content_language: Union[str, None, UnsetType] = UNSET
    """Language of this object's contents."""

    gcs_object_retention_expiration_date: Union[int, None, UnsetType] = UNSET
    """Retention expiration date of this object."""

    gcs_storage_class: Union[str, None, UnsetType] = UNSET
    """Storage class of this asset."""

    gcs_encryption_type: Union[str, None, UnsetType] = UNSET
    """Encryption algorithm used to encrypt this asset."""

    gcs_e_tag: Union[str, None, UnsetType] = UNSET
    """Entity tag for the asset. An entity tag is a hash of the object and represents changes to the contents of an object only, not its metadata."""

    gcs_requester_pays: Union[bool, None, UnsetType] = UNSET
    """Whether the requester pays header was sent when this asset was created (true) or not (false)."""

    gcs_access_control: Union[str, None, UnsetType] = UNSET
    """Access control list for this asset."""

    gcs_meta_generation_id: Union[int, None, UnsetType] = UNSET
    """Version of metadata for this asset at this generation. Used for preconditions and detecting changes in metadata. A metageneration number is only meaningful in the context of a particular generation of a particular asset."""

    google_service: Union[str, None, UnsetType] = UNSET
    """Service in Google in which the asset exists."""

    google_project_name: Union[str, None, UnsetType] = UNSET
    """Name of the project in which the asset exists."""

    google_project_id: Union[str, None, UnsetType] = UNSET
    """ID of the project in which the asset exists."""

    cloud_project_number: Union[int, None, UnsetType] = UNSET
    """Number of the project in which the asset exists."""

    google_location: Union[str, None, UnsetType] = UNSET
    """Location of this asset in Google."""

    google_location_type: Union[str, None, UnsetType] = UNSET
    """Type of location of this asset in Google."""

    google_labels: Union[list[dict[str, Any]], None, UnsetType] = UNSET
    """List of labels that have been applied to the asset in Google."""

    google_tags: Union[list[dict[str, Any]], None, UnsetType] = UNSET
    """List of tags that have been applied to the asset in Google."""

    gcs_bucket: Union[RelatedGCSBucket, None, UnsetType] = UNSET
    """GCS bucket in which the object exists."""

    # =========================================================================
    # Creator Methods
    # =========================================================================

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        name: str,
        gcs_bucket_name: str,
        gcs_bucket_qualified_name: str,
        connection_qualified_name: str | None = None,
    ) -> "GCSObject":
        """
        Create a new GCSObject asset.

        Args:
            name: Name of the object
            gcs_bucket_name: Simple name of the bucket
            gcs_bucket_qualified_name: Unique name of the bucket
            connection_qualified_name: Unique name of the connection (optional,
                derived from gcs_bucket_qualified_name if not provided)

        Returns:
            GCSObject instance ready to be created

        Raises:
            ValueError: If required parameters are missing
        """
        validate_required_fields(
            ["name", "gcs_bucket_name", "gcs_bucket_qualified_name"],
            [name, gcs_bucket_name, gcs_bucket_qualified_name],
        )
        if connection_qualified_name:
            connector_name = connection_qualified_name.split("/")[1]
        else:
            # Derive connection_qualified_name from gcs_bucket_qualified_name
            # gcs_bucket_qualified_name format: "default/gcs/123456789/mybucket"
            parts = gcs_bucket_qualified_name.split("/")
            connection_qualified_name = "/".join(parts[:3])
            connector_name = parts[1]

        return cls(
            name=name,
            connection_qualified_name=connection_qualified_name,
            qualified_name=f"{gcs_bucket_qualified_name}/{name}",
            connector_name=connector_name,
            gcs_bucket_name=gcs_bucket_name,
            gcs_bucket_qualified_name=gcs_bucket_qualified_name,
        )

    @classmethod
    @init_guid
    def creator_with_prefix(
        cls,
        *,
        name: str,
        connection_qualified_name: str,
        gcs_bucket_name: str,
        gcs_bucket_qualified_name: str,
        prefix: str = "",
    ) -> "GCSObject":
        """
        Create a new GCSObject asset using a prefix-based object key.

        Args:
            name: Name of the object
            connection_qualified_name: Unique name of the connection
            gcs_bucket_name: Simple name of the bucket
            gcs_bucket_qualified_name: Unique name of the bucket
            prefix: Prefix (folder path) for the object

        Returns:
            GCSObject instance ready to be created

        Raises:
            ValueError: If required parameters are missing or invalid
        """
        validate_required_fields(
            [
                "name",
                "connection_qualified_name",
                "gcs_bucket_name",
                "gcs_bucket_qualified_name",
            ],
            [
                name,
                connection_qualified_name,
                gcs_bucket_name,
                gcs_bucket_qualified_name,
            ],
        )
        fields = connection_qualified_name.split("/")
        if len(fields) != 3:
            raise ValueError("Invalid connection_qualified_name")
        if fields[0].replace(" ", "") == "" or fields[2].replace(" ", "") == "":
            raise ValueError("Invalid connection_qualified_name")
        if fields[1].lower() != "gcs":
            raise ValueError("Invalid connection_qualified_name")

        connector_name = fields[1]
        object_key = construct_object_key(prefix, name)
        return cls(
            name=name,
            gcs_object_key=object_key,
            connection_qualified_name=connection_qualified_name,
            qualified_name=f"{gcs_bucket_qualified_name}/{object_key}",
            connector_name=connector_name,
            gcs_bucket_name=gcs_bucket_name,
            gcs_bucket_qualified_name=gcs_bucket_qualified_name,
        )

    @classmethod
    def updater(cls, *, qualified_name: str, name: str) -> "GCSObject":
        """
        Create a GCSObject instance for modification.

        Args:
            qualified_name: Unique name of the GCSObject to update
            name: Human-readable name of the GCSObject

        Returns:
            GCSObject instance ready for update

        Raises:
            ValueError: If required parameters are missing
        """
        validate_required_fields(["qualified_name", "name"], [qualified_name, name])
        return cls(qualified_name=qualified_name, name=name)

    def trim_to_required(self) -> "GCSObject":
        """
        Return a copy of this GCSObject with only the minimum required fields for update.

        Returns:
            GCSObject with only qualified_name and name set
        """
        return GCSObject.updater(qualified_name=self.qualified_name, name=self.name)

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _gcs_object_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "GCSObject":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            GCSObject instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _gcs_object_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class GCSObjectAttributes(AssetAttributes):
    """GCSObject-specific attributes for nested API format."""

    gcs_bucket_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the bucket in which this object exists."""

    gcs_bucket_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the bucket in which this object exists."""

    gcs_object_size: Union[int, None, UnsetType] = UNSET
    """Object size in bytes."""

    gcs_object_key: Union[str, None, UnsetType] = UNSET
    """Key of this object, in GCS."""

    gcs_object_media_link: Union[str, None, UnsetType] = UNSET
    """Media link to this object."""

    gcs_object_hold_type: Union[str, None, UnsetType] = UNSET
    """Type of hold on this object."""

    gcs_object_generation_id: Union[int, None, UnsetType] = UNSET
    """Generation ID of this object."""

    gcs_object_crc32_c_hash: Union[str, None, UnsetType] = UNSET
    """CRC32C hash of this object."""

    gcs_object_md5_hash: Union[str, None, UnsetType] = UNSET
    """MD5 hash of this object."""

    gcs_object_data_last_modified_time: Union[int, None, UnsetType] = UNSET
    """Time (epoch) at which this object's data was last modified, in milliseconds."""

    gcs_object_content_type: Union[str, None, UnsetType] = UNSET
    """Type of content in this object."""

    gcs_object_content_encoding: Union[str, None, UnsetType] = UNSET
    """Content encoding of this object."""

    gcs_object_content_disposition: Union[str, None, UnsetType] = UNSET
    """Information about how this object's content should be presented."""

    gcs_object_content_language: Union[str, None, UnsetType] = UNSET
    """Language of this object's contents."""

    gcs_object_retention_expiration_date: Union[int, None, UnsetType] = UNSET
    """Retention expiration date of this object."""

    gcs_storage_class: Union[str, None, UnsetType] = UNSET
    """Storage class of this asset."""

    gcs_encryption_type: Union[str, None, UnsetType] = UNSET
    """Encryption algorithm used to encrypt this asset."""

    gcs_e_tag: Union[str, None, UnsetType] = UNSET
    """Entity tag for the asset. An entity tag is a hash of the object and represents changes to the contents of an object only, not its metadata."""

    gcs_requester_pays: Union[bool, None, UnsetType] = UNSET
    """Whether the requester pays header was sent when this asset was created (true) or not (false)."""

    gcs_access_control: Union[str, None, UnsetType] = UNSET
    """Access control list for this asset."""

    gcs_meta_generation_id: Union[int, None, UnsetType] = UNSET
    """Version of metadata for this asset at this generation. Used for preconditions and detecting changes in metadata. A metageneration number is only meaningful in the context of a particular generation of a particular asset."""

    google_service: Union[str, None, UnsetType] = UNSET
    """Service in Google in which the asset exists."""

    google_project_name: Union[str, None, UnsetType] = UNSET
    """Name of the project in which the asset exists."""

    google_project_id: Union[str, None, UnsetType] = UNSET
    """ID of the project in which the asset exists."""

    cloud_project_number: Union[int, None, UnsetType] = UNSET
    """Number of the project in which the asset exists."""

    google_location: Union[str, None, UnsetType] = UNSET
    """Location of this asset in Google."""

    google_location_type: Union[str, None, UnsetType] = UNSET
    """Type of location of this asset in Google."""

    google_labels: Union[list[dict[str, Any]], None, UnsetType] = UNSET
    """List of labels that have been applied to the asset in Google."""

    google_tags: Union[list[dict[str, Any]], None, UnsetType] = UNSET
    """List of tags that have been applied to the asset in Google."""


class GCSObjectRelationshipAttributes(AssetRelationshipAttributes):
    """GCSObject-specific relationship attributes for nested API format."""

    gcs_bucket: Union[RelatedGCSBucket, None, UnsetType] = UNSET
    """GCS bucket in which the object exists."""


class GCSObjectNested(AssetNested):
    """GCSObject in nested API format for high-performance serialization."""

    attributes: Union[GCSObjectAttributes, UnsetType] = UNSET
    relationship_attributes: Union[GCSObjectRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[
        GCSObjectRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        GCSObjectRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _gcs_object_to_nested(gcs_object: GCSObject) -> GCSObjectNested:
    """Convert flat GCSObject to nested format."""
    attrs_kwargs = build_attributes_kwargs(gcs_object, GCSObjectAttributes)
    attrs = GCSObjectAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["gcs_bucket"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        gcs_object, rel_fields, GCSObjectRelationshipAttributes
    )
    return GCSObjectNested(
        guid=gcs_object.guid,
        type_name=gcs_object.type_name,
        status=gcs_object.status,
        version=gcs_object.version,
        create_time=gcs_object.create_time,
        update_time=gcs_object.update_time,
        created_by=gcs_object.created_by,
        updated_by=gcs_object.updated_by,
        classifications=gcs_object.classifications,
        classification_names=gcs_object.classification_names,
        meanings=gcs_object.meanings,
        labels=gcs_object.labels,
        business_attributes=gcs_object.business_attributes,
        custom_attributes=gcs_object.custom_attributes,
        pending_tasks=gcs_object.pending_tasks,
        proxy=gcs_object.proxy,
        is_incomplete=gcs_object.is_incomplete,
        provenance_type=gcs_object.provenance_type,
        home_id=gcs_object.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _gcs_object_from_nested(nested: GCSObjectNested) -> GCSObject:
    """Convert nested format to flat GCSObject."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else GCSObjectAttributes()
    )
    rel_fields: list[str] = ["gcs_bucket"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        GCSObjectRelationshipAttributes,
    )
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, AssetNested, GCSObjectAttributes
    )
    return GCSObject(**kwargs)


def _gcs_object_to_nested_bytes(gcs_object: GCSObject, serde: Serde) -> bytes:
    """Convert flat GCSObject to nested JSON bytes."""
    return serde.encode(_gcs_object_to_nested(gcs_object))


def _gcs_object_from_nested_bytes(data: bytes, serde: Serde) -> GCSObject:
    """Convert nested JSON bytes to flat GCSObject."""
    nested = serde.decode(data, GCSObjectNested)
    return _gcs_object_from_nested(nested)
