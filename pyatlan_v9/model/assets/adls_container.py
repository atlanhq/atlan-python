# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
ADLSContainer asset model with flattened inheritance.

This module provides:
- ADLSContainer: Flat asset class (easy to use)
- ADLSContainerAttributes: Nested attributes struct (extends AssetAttributes)
- ADLSContainerNested: Nested API format struct
"""

from __future__ import annotations

from typing import Any, Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .adls_related import RelatedADLSAccount, RelatedADLSObject
from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class ADLSContainer(Asset):
    """
    Instance of an Azure Data Lake Storage (ADLS) container in Atlan.
    """

    # Override type_name with ADLSContainer-specific default
    type_name: Union[str, UnsetType] = "ADLSContainer"

    adls_container_url: Union[str, None, UnsetType] = UNSET
    """URL of this container."""

    adls_container_lease_state: Union[str, None, UnsetType] = UNSET
    """Lease state of this container."""

    adls_container_lease_status: Union[str, None, UnsetType] = UNSET
    """Lease status of this container."""

    adls_container_encryption_scope: Union[str, None, UnsetType] = UNSET
    """Encryption scope of this container."""

    adls_container_version_level_immutability_support: Union[bool, None, UnsetType] = (
        UNSET
    )
    """Whether this container supports version-level immutability (true) or not (false)."""

    adls_object_count: Union[int, None, UnsetType] = UNSET
    """Number of objects that exist within this container."""

    adls_account_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the account for this ADLS asset."""

    adls_account_name: Union[str, None, UnsetType] = UNSET
    """Name of the account for this ADLS asset."""

    azure_resource_id: Union[str, None, UnsetType] = UNSET
    """Resource identifier of this asset in Azure."""

    azure_location: Union[str, None, UnsetType] = UNSET
    """Location of this asset in Azure."""

    adls_account_secondary_location: Union[str, None, UnsetType] = UNSET
    """Secondary location of the ADLS account."""

    azure_tags: Union[list[dict[str, Any]], None, UnsetType] = UNSET
    """Tags that have been applied to this asset in Azure."""

    adls_account: Union[RelatedADLSAccount, None, UnsetType] = UNSET
    """Account in which this container exists."""

    adls_objects: Union[list[RelatedADLSObject], None, UnsetType] = UNSET
    """Objects that exist within this container."""

    # =========================================================================
    # Convenience Methods
    # =========================================================================

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        name: str,
        adls_account_qualified_name: str,
        connection_qualified_name: str | None = None,
    ) -> "ADLSContainer":
        validate_required_fields(
            ["name", "adls_account_qualified_name"],
            [name, adls_account_qualified_name],
        )
        if connection_qualified_name:
            fields = connection_qualified_name.split("/")
            connector_name = fields[1] if len(fields) > 1 else None
        else:
            fields = adls_account_qualified_name.split("/")
            connector_name = fields[1] if len(fields) > 1 else None
            connection_qualified_name = (
                "/".join(fields[:3])
                if len(fields) >= 3
                else adls_account_qualified_name
            )

        qualified_name = f"{adls_account_qualified_name}/{name}"
        return cls(
            name=name,
            qualified_name=qualified_name,
            adls_account_qualified_name=adls_account_qualified_name,
            connector_name=connector_name,
            connection_qualified_name=connection_qualified_name,
        )

    @classmethod
    def updater(cls, *, qualified_name: str, name: str) -> "ADLSContainer":
        """Create an ADLSContainer instance for update operations."""
        validate_required_fields(["qualified_name", "name"], [qualified_name, name])
        return cls(qualified_name=qualified_name, name=name)

    def trim_to_required(self) -> "ADLSContainer":
        """Return only fields required for update operations."""
        return ADLSContainer.updater(qualified_name=self.qualified_name, name=self.name)

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _adls_container_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "ADLSContainer":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            ADLSContainer instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _adls_container_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class ADLSContainerAttributes(AssetAttributes):
    """ADLSContainer-specific attributes for nested API format."""

    adls_container_url: Union[str, None, UnsetType] = UNSET
    """URL of this container."""

    adls_container_lease_state: Union[str, None, UnsetType] = UNSET
    """Lease state of this container."""

    adls_container_lease_status: Union[str, None, UnsetType] = UNSET
    """Lease status of this container."""

    adls_container_encryption_scope: Union[str, None, UnsetType] = UNSET
    """Encryption scope of this container."""

    adls_container_version_level_immutability_support: Union[bool, None, UnsetType] = (
        UNSET
    )
    """Whether this container supports version-level immutability (true) or not (false)."""

    adls_object_count: Union[int, None, UnsetType] = UNSET
    """Number of objects that exist within this container."""

    adls_account_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the account for this ADLS asset."""

    adls_account_name: Union[str, None, UnsetType] = UNSET
    """Name of the account for this ADLS asset."""

    azure_resource_id: Union[str, None, UnsetType] = UNSET
    """Resource identifier of this asset in Azure."""

    azure_location: Union[str, None, UnsetType] = UNSET
    """Location of this asset in Azure."""

    adls_account_secondary_location: Union[str, None, UnsetType] = UNSET
    """Secondary location of the ADLS account."""

    azure_tags: Union[list[dict[str, Any]], None, UnsetType] = UNSET
    """Tags that have been applied to this asset in Azure."""


class ADLSContainerRelationshipAttributes(AssetRelationshipAttributes):
    """ADLSContainer-specific relationship attributes for nested API format."""

    adls_account: Union[RelatedADLSAccount, None, UnsetType] = UNSET
    """Account in which this container exists."""

    adls_objects: Union[list[RelatedADLSObject], None, UnsetType] = UNSET
    """Objects that exist within this container."""


class ADLSContainerNested(AssetNested):
    """ADLSContainer in nested API format for high-performance serialization."""

    attributes: Union[ADLSContainerAttributes, UnsetType] = UNSET
    relationship_attributes: Union[ADLSContainerRelationshipAttributes, UnsetType] = (
        UNSET
    )
    append_relationship_attributes: Union[
        ADLSContainerRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        ADLSContainerRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _adls_container_to_nested(adls_container: ADLSContainer) -> ADLSContainerNested:
    """Convert flat ADLSContainer to nested format."""
    attrs_kwargs = build_attributes_kwargs(adls_container, ADLSContainerAttributes)
    attrs = ADLSContainerAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["adls_account", "adls_objects"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        adls_container, rel_fields, ADLSContainerRelationshipAttributes
    )
    return ADLSContainerNested(
        guid=adls_container.guid,
        type_name=adls_container.type_name,
        status=adls_container.status,
        version=adls_container.version,
        create_time=adls_container.create_time,
        update_time=adls_container.update_time,
        created_by=adls_container.created_by,
        updated_by=adls_container.updated_by,
        classifications=adls_container.classifications,
        classification_names=adls_container.classification_names,
        meanings=adls_container.meanings,
        labels=adls_container.labels,
        business_attributes=adls_container.business_attributes,
        custom_attributes=adls_container.custom_attributes,
        pending_tasks=adls_container.pending_tasks,
        proxy=adls_container.proxy,
        is_incomplete=adls_container.is_incomplete,
        provenance_type=adls_container.provenance_type,
        home_id=adls_container.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _adls_container_from_nested(nested: ADLSContainerNested) -> ADLSContainer:
    """Convert nested format to flat ADLSContainer."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else ADLSContainerAttributes()
    )
    rel_fields: list[str] = ["adls_account", "adls_objects"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        ADLSContainerRelationshipAttributes,
    )
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, AssetNested, ADLSContainerAttributes
    )
    return ADLSContainer(**kwargs)


def _adls_container_to_nested_bytes(
    adls_container: ADLSContainer, serde: Serde
) -> bytes:
    """Convert flat ADLSContainer to nested JSON bytes."""
    return serde.encode(_adls_container_to_nested(adls_container))


def _adls_container_from_nested_bytes(data: bytes, serde: Serde) -> ADLSContainer:
    """Convert nested JSON bytes to flat ADLSContainer."""
    nested = serde.decode(data, ADLSContainerNested)
    return _adls_container_from_nested(nested)
