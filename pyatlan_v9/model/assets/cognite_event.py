# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
CogniteEvent asset model with flattened inheritance.

This module provides:
- CogniteEvent: Flat asset class (easy to use)
- CogniteEventAttributes: Nested attributes struct (extends AssetAttributes)
- CogniteEventNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .cognite_related import RelatedCogniteAsset

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class CogniteEvent(Asset):
    """
    Instance of a Cognite event in Atlan.
    """

    # Override type_name with CogniteEvent-specific default
    type_name: Union[str, UnsetType] = "CogniteEvent"

    cognite_asset: Union[RelatedCogniteAsset, None, UnsetType] = UNSET
    """Asset in which this event exists."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _cognite_event_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "CogniteEvent":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            CogniteEvent instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _cognite_event_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class CogniteEventAttributes(AssetAttributes):
    """CogniteEvent-specific attributes for nested API format."""

    pass


class CogniteEventRelationshipAttributes(AssetRelationshipAttributes):
    """CogniteEvent-specific relationship attributes for nested API format."""

    cognite_asset: Union[RelatedCogniteAsset, None, UnsetType] = UNSET
    """Asset in which this event exists."""


class CogniteEventNested(AssetNested):
    """CogniteEvent in nested API format for high-performance serialization."""

    attributes: Union[CogniteEventAttributes, UnsetType] = UNSET
    relationship_attributes: Union[CogniteEventRelationshipAttributes, UnsetType] = (
        UNSET
    )
    append_relationship_attributes: Union[
        CogniteEventRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        CogniteEventRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _cognite_event_to_nested(cognite_event: CogniteEvent) -> CogniteEventNested:
    """Convert flat CogniteEvent to nested format."""
    attrs = CogniteEventAttributes()
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["cognite_asset"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        cognite_event, rel_fields, CogniteEventRelationshipAttributes
    )
    return CogniteEventNested(
        guid=cognite_event.guid,
        type_name=cognite_event.type_name,
        status=cognite_event.status,
        version=cognite_event.version,
        create_time=cognite_event.create_time,
        update_time=cognite_event.update_time,
        created_by=cognite_event.created_by,
        updated_by=cognite_event.updated_by,
        classifications=cognite_event.classifications,
        classification_names=cognite_event.classification_names,
        meanings=cognite_event.meanings,
        labels=cognite_event.labels,
        business_attributes=cognite_event.business_attributes,
        custom_attributes=cognite_event.custom_attributes,
        pending_tasks=cognite_event.pending_tasks,
        proxy=cognite_event.proxy,
        is_incomplete=cognite_event.is_incomplete,
        provenance_type=cognite_event.provenance_type,
        home_id=cognite_event.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _cognite_event_from_nested(nested: CogniteEventNested) -> CogniteEvent:
    """Convert nested format to flat CogniteEvent."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else CogniteEventAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["cognite_asset"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        CogniteEventRelationshipAttributes,
    )
    return CogniteEvent(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _cognite_event_to_nested_bytes(cognite_event: CogniteEvent, serde: Serde) -> bytes:
    """Convert flat CogniteEvent to nested JSON bytes."""
    return serde.encode(_cognite_event_to_nested(cognite_event))


def _cognite_event_from_nested_bytes(data: bytes, serde: Serde) -> CogniteEvent:
    """Convert nested JSON bytes to flat CogniteEvent."""
    nested = serde.decode(data, CogniteEventNested)
    return _cognite_event_from_nested(nested)
