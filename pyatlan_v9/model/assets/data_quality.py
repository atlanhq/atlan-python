# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
DataQuality asset model with flattened inheritance.

This module provides:
- DataQuality: Flat asset class (easy to use)
- DataQualityAttributes: Nested attributes struct (extends AssetAttributes)
- DataQualityNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class DataQuality(Asset):
    """
    Base class for data quality assets.
    """

    # Override type_name with DataQuality-specific default
    type_name: Union[str, UnsetType] = "DataQuality"

    dq_is_part_of_contract: Union[bool, None, UnsetType] = UNSET
    """Whether this data quality is part of contract (true) or not (false)."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _data_quality_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "DataQuality":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            DataQuality instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _data_quality_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class DataQualityAttributes(AssetAttributes):
    """DataQuality-specific attributes for nested API format."""

    dq_is_part_of_contract: Union[bool, None, UnsetType] = UNSET
    """Whether this data quality is part of contract (true) or not (false)."""


class DataQualityRelationshipAttributes(AssetRelationshipAttributes):
    """DataQuality-specific relationship attributes for nested API format."""

    pass


class DataQualityNested(AssetNested):
    """DataQuality in nested API format for high-performance serialization."""

    attributes: Union[DataQualityAttributes, UnsetType] = UNSET
    relationship_attributes: Union[DataQualityRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[
        DataQualityRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        DataQualityRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _data_quality_to_nested(data_quality: DataQuality) -> DataQualityNested:
    """Convert flat DataQuality to nested format."""
    attrs = DataQualityAttributes(
        dq_is_part_of_contract=data_quality.dq_is_part_of_contract,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        data_quality, rel_fields, DataQualityRelationshipAttributes
    )
    return DataQualityNested(
        guid=data_quality.guid,
        type_name=data_quality.type_name,
        status=data_quality.status,
        version=data_quality.version,
        create_time=data_quality.create_time,
        update_time=data_quality.update_time,
        created_by=data_quality.created_by,
        updated_by=data_quality.updated_by,
        classifications=data_quality.classifications,
        classification_names=data_quality.classification_names,
        meanings=data_quality.meanings,
        labels=data_quality.labels,
        business_attributes=data_quality.business_attributes,
        custom_attributes=data_quality.custom_attributes,
        pending_tasks=data_quality.pending_tasks,
        proxy=data_quality.proxy,
        is_incomplete=data_quality.is_incomplete,
        provenance_type=data_quality.provenance_type,
        home_id=data_quality.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _data_quality_from_nested(nested: DataQualityNested) -> DataQuality:
    """Convert nested format to flat DataQuality."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else DataQualityAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        DataQualityRelationshipAttributes,
    )
    return DataQuality(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        dq_is_part_of_contract=attrs.dq_is_part_of_contract,
        # Merged relationship attributes
        **merged_rels,
    )


def _data_quality_to_nested_bytes(data_quality: DataQuality, serde: Serde) -> bytes:
    """Convert flat DataQuality to nested JSON bytes."""
    return serde.encode(_data_quality_to_nested(data_quality))


def _data_quality_from_nested_bytes(data: bytes, serde: Serde) -> DataQuality:
    """Convert nested JSON bytes to flat DataQuality."""
    nested = serde.decode(data, DataQualityNested)
    return _data_quality_from_nested(nested)
