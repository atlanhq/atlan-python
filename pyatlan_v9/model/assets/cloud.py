# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Cloud asset model with flattened inheritance.

This module provides:
- Cloud: Flat asset class (easy to use)
- CloudAttributes: Nested attributes struct (extends AssetAttributes)
- CloudNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Cloud(Asset):
    """
    Base class for cloud assets.
    """

    # Override type_name with Cloud-specific default
    type_name: Union[str, UnsetType] = "Cloud"

    pass

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _cloud_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(json_data: Union[str, bytes], serde: Serde | None = None) -> "Cloud":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Cloud instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _cloud_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class CloudAttributes(AssetAttributes):
    """Cloud-specific attributes for nested API format."""

    pass


class CloudRelationshipAttributes(AssetRelationshipAttributes):
    """Cloud-specific relationship attributes for nested API format."""

    pass


class CloudNested(AssetNested):
    """Cloud in nested API format for high-performance serialization."""

    attributes: Union[CloudAttributes, UnsetType] = UNSET
    relationship_attributes: Union[CloudRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[CloudRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[CloudRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _cloud_to_nested(cloud: Cloud) -> CloudNested:
    """Convert flat Cloud to nested format."""
    attrs = CloudAttributes()
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        cloud, rel_fields, CloudRelationshipAttributes
    )
    return CloudNested(
        guid=cloud.guid,
        type_name=cloud.type_name,
        status=cloud.status,
        version=cloud.version,
        create_time=cloud.create_time,
        update_time=cloud.update_time,
        created_by=cloud.created_by,
        updated_by=cloud.updated_by,
        classifications=cloud.classifications,
        classification_names=cloud.classification_names,
        meanings=cloud.meanings,
        labels=cloud.labels,
        business_attributes=cloud.business_attributes,
        custom_attributes=cloud.custom_attributes,
        pending_tasks=cloud.pending_tasks,
        proxy=cloud.proxy,
        is_incomplete=cloud.is_incomplete,
        provenance_type=cloud.provenance_type,
        home_id=cloud.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _cloud_from_nested(nested: CloudNested) -> Cloud:
    """Convert nested format to flat Cloud."""
    attrs = nested.attributes if nested.attributes is not UNSET else CloudAttributes()
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        CloudRelationshipAttributes,
    )
    return Cloud(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _cloud_to_nested_bytes(cloud: Cloud, serde: Serde) -> bytes:
    """Convert flat Cloud to nested JSON bytes."""
    return serde.encode(_cloud_to_nested(cloud))


def _cloud_from_nested_bytes(data: bytes, serde: Serde) -> Cloud:
    """Convert nested JSON bytes to flat Cloud."""
    nested = serde.decode(data, CloudNested)
    return _cloud_from_nested(nested)
