# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Tableau asset model with flattened inheritance.

This module provides:
- Tableau: Flat asset class (easy to use)
- TableauAttributes: Nested attributes struct (extends AssetAttributes)
- TableauNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Tableau(Asset):
    """
    Base class for Tableau assets.
    """

    # Override type_name with Tableau-specific default
    type_name: Union[str, UnsetType] = "Tableau"

    tableau_project_hierarchy_qualified_names: Union[list[str], None, UnsetType] = UNSET
    """Array of qualified names representing the project hierarchy for this Tableau asset."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _tableau_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "Tableau":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Tableau instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _tableau_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class TableauAttributes(AssetAttributes):
    """Tableau-specific attributes for nested API format."""

    tableau_project_hierarchy_qualified_names: Union[list[str], None, UnsetType] = UNSET
    """Array of qualified names representing the project hierarchy for this Tableau asset."""


class TableauRelationshipAttributes(AssetRelationshipAttributes):
    """Tableau-specific relationship attributes for nested API format."""

    pass


class TableauNested(AssetNested):
    """Tableau in nested API format for high-performance serialization."""

    attributes: Union[TableauAttributes, UnsetType] = UNSET
    relationship_attributes: Union[TableauRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[TableauRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[TableauRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _tableau_to_nested(tableau: Tableau) -> TableauNested:
    """Convert flat Tableau to nested format."""
    attrs = TableauAttributes(
        tableau_project_hierarchy_qualified_names=tableau.tableau_project_hierarchy_qualified_names,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        tableau, rel_fields, TableauRelationshipAttributes
    )
    return TableauNested(
        guid=tableau.guid,
        type_name=tableau.type_name,
        status=tableau.status,
        version=tableau.version,
        create_time=tableau.create_time,
        update_time=tableau.update_time,
        created_by=tableau.created_by,
        updated_by=tableau.updated_by,
        classifications=tableau.classifications,
        classification_names=tableau.classification_names,
        meanings=tableau.meanings,
        labels=tableau.labels,
        business_attributes=tableau.business_attributes,
        custom_attributes=tableau.custom_attributes,
        pending_tasks=tableau.pending_tasks,
        proxy=tableau.proxy,
        is_incomplete=tableau.is_incomplete,
        provenance_type=tableau.provenance_type,
        home_id=tableau.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _tableau_from_nested(nested: TableauNested) -> Tableau:
    """Convert nested format to flat Tableau."""
    attrs = nested.attributes if nested.attributes is not UNSET else TableauAttributes()
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        TableauRelationshipAttributes,
    )
    return Tableau(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        tableau_project_hierarchy_qualified_names=attrs.tableau_project_hierarchy_qualified_names,
        # Merged relationship attributes
        **merged_rels,
    )


def _tableau_to_nested_bytes(tableau: Tableau, serde: Serde) -> bytes:
    """Convert flat Tableau to nested JSON bytes."""
    return serde.encode(_tableau_to_nested(tableau))


def _tableau_from_nested_bytes(data: bytes, serde: Serde) -> Tableau:
    """Convert nested JSON bytes to flat Tableau."""
    nested = serde.decode(data, TableauNested)
    return _tableau_from_nested(nested)
