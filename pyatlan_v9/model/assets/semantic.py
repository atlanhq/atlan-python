# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Semantic asset model with flattened inheritance.

This module provides:
- Semantic: Flat asset class (easy to use)
- SemanticAttributes: Nested attributes struct (extends AssetAttributes)
- SemanticNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.model.conversion_utils import (
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.model.serde import Serde, get_serde
from pyatlan_v9.model.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Semantic(Asset):
    """
    Base class for semantic layer assets.
    """

    # Override type_name with Semantic-specific default
    type_name: Union[str, UnsetType] = "Semantic"

    pass

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _semantic_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "Semantic":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Semantic instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _semantic_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class SemanticAttributes(AssetAttributes):
    """Semantic-specific attributes for nested API format."""

    pass


class SemanticRelationshipAttributes(AssetRelationshipAttributes):
    """Semantic-specific relationship attributes for nested API format."""

    pass


class SemanticNested(AssetNested):
    """Semantic in nested API format for high-performance serialization."""

    attributes: Union[SemanticAttributes, UnsetType] = UNSET
    relationship_attributes: Union[SemanticRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[SemanticRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[SemanticRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _semantic_to_nested(semantic: Semantic) -> SemanticNested:
    """Convert flat Semantic to nested format."""
    attrs = SemanticAttributes()
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        semantic, rel_fields, SemanticRelationshipAttributes
    )
    return SemanticNested(
        guid=semantic.guid,
        type_name=semantic.type_name,
        status=semantic.status,
        version=semantic.version,
        create_time=semantic.create_time,
        update_time=semantic.update_time,
        created_by=semantic.created_by,
        updated_by=semantic.updated_by,
        classifications=semantic.classifications,
        classification_names=semantic.classification_names,
        meanings=semantic.meanings,
        labels=semantic.labels,
        business_attributes=semantic.business_attributes,
        custom_attributes=semantic.custom_attributes,
        pending_tasks=semantic.pending_tasks,
        proxy=semantic.proxy,
        is_incomplete=semantic.is_incomplete,
        provenance_type=semantic.provenance_type,
        home_id=semantic.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _semantic_from_nested(nested: SemanticNested) -> Semantic:
    """Convert nested format to flat Semantic."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else SemanticAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        SemanticRelationshipAttributes,
    )
    return Semantic(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _semantic_to_nested_bytes(semantic: Semantic, serde: Serde) -> bytes:
    """Convert flat Semantic to nested JSON bytes."""
    return serde.encode(_semantic_to_nested(semantic))


def _semantic_from_nested_bytes(data: bytes, serde: Serde) -> Semantic:
    """Convert nested JSON bytes to flat Semantic."""
    nested = serde.decode(data, SemanticNested)
    return _semantic_from_nested(nested)
