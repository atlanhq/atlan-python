# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
"""
Exception classes for the PyAtlan SDK.

This module defines custom exceptions raised by the SDK for various error conditions.
"""

from __future__ import annotations


class PyAtlanError(Exception):
    """Base exception for all PyAtlan errors."""

    pass


class AtlanClientError(PyAtlanError):
    """Exception raised for client-side errors."""

    pass


class AtlanAPIError(PyAtlanError):
    """Exception raised when the Atlan API returns an error response.

    Attributes:
        status_code: The HTTP status code from the response.
        message: A human-readable error message.
        response_body: The raw response body from the API.
        error_code: The Atlan error code, if available.
    """

    def __init__(
        self,
        status_code: int,
        message: str,
        response_body: str = "",
        error_code: str | None = None,
    ):
        self.status_code = status_code
        self.message = message
        self.response_body = response_body
        self.error_code = error_code
        super().__init__(f"HTTP {status_code}: {message}")


class EntityNotFoundError(AtlanAPIError):
    """Exception raised when an entity is not found."""

    def __init__(self, guid: str | None = None, qualified_name: str | None = None):
        identifier = guid or qualified_name or "unknown"
        super().__init__(
            status_code=404,
            message=f"Entity not found: {identifier}",
        )
        self.guid = guid
        self.qualified_name = qualified_name


class AuthenticationError(AtlanAPIError):
    """Exception raised for authentication failures."""

    def __init__(self, message: str = "Authentication failed"):
        super().__init__(status_code=401, message=message)


class AuthorizationError(AtlanAPIError):
    """Exception raised for authorization failures."""

    def __init__(self, message: str = "Not authorized"):
        super().__init__(status_code=403, message=message)


class ValidationError(PyAtlanError):
    """Exception raised for validation errors in the SDK."""

    pass
