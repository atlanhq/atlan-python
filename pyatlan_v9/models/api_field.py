# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
APIField asset model with flattened inheritance.

This module provides:
- APIField: Flat asset class (easy to use)
- APIFieldAttributes: Nested attributes struct (extends AssetAttributes)
- APIFieldNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .api_related import RelatedAPIObject, RelatedAPIQuery
from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class APIField(Asset):
    """
    Instances of APIField in Atlan.
    """

    # Override type_name with APIField-specific default
    type_name: Union[str, UnsetType] = "APIField"

    api_field_type: Union[str, None, UnsetType] = UNSET
    """Type of APIField, as free text (e.g. STRING, NUMBER etc)."""

    api_field_type_secondary: Union[str, None, UnsetType] = UNSET
    """Secondary type of APIField (e.g. LIST/STRING, then LIST would be the secondary type)."""

    api_query_param_type: Union[str, None, UnsetType] = UNSET
    """If parent relationship type is APIQuery, then this attribute denotes if this is input or output parameter."""

    api_spec_type: Union[str, None, UnsetType] = UNSET
    """Type of API, for example: OpenAPI, GraphQL, etc."""

    api_spec_version: Union[str, None, UnsetType] = UNSET
    """Version of the API specification."""

    api_spec_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the API spec, if this asset is contained in an API spec."""

    api_spec_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the API spec, if this asset is contained in an API spec."""

    api_external_docs: Union[dict[str, str], None, UnsetType] = UNSET
    """External documentation of the API."""

    api_is_auth_optional: Union[bool, None, UnsetType] = UNSET
    """Whether authentication is optional (true) or required (false)."""

    api_is_object_reference: Union[bool, None, UnsetType] = UNSET
    """If this asset refers to an APIObject"""

    api_object_qualified_name: Union[str, None, UnsetType] = UNSET
    """Qualified name of the APIObject that is referred to by this asset. When apiIsObjectReference is true."""

    api_object: Union[RelatedAPIObject, None, UnsetType] = UNSET
    """APIObject asset containing this APIField."""

    api_query: Union[RelatedAPIQuery, None, UnsetType] = UNSET
    """APIQuery asset containing this APIField."""

    # =========================================================================
    # Convenience Methods
    # =========================================================================

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        name: str,
        parent_api_object_qualified_name: Union[str, None] = None,
        parent_api_query_qualified_name: Union[str, None] = None,
        connection_qualified_name: Union[str, None] = None,
        api_field_type: Union[str, None] = None,
        api_field_type_secondary: Union[str, None] = None,
        is_api_object_reference: bool = False,
        reference_api_object_qualified_name: Union[str, None] = None,
        api_query_param_type: Union[str, None] = None,
    ) -> "APIField":
        """Create a new APIField asset."""
        validate_required_fields(["name"], [name])
        if parent_api_object_qualified_name is None or (
            isinstance(parent_api_object_qualified_name, str)
            and not parent_api_object_qualified_name.strip()
        ):
            if parent_api_query_qualified_name is None or (
                isinstance(parent_api_query_qualified_name, str)
                and not parent_api_query_qualified_name.strip()
            ):
                raise ValueError(
                    "Either parent_api_object_qualified_name or parent_api_query_qualified_name requires a valid value"
                )
        elif (
            isinstance(parent_api_query_qualified_name, str)
            and parent_api_query_qualified_name.strip()
        ):
            raise ValueError(
                "Both parent_api_object_qualified_name and parent_api_query_qualified_name cannot be valid"
            )

        if is_api_object_reference:
            if not reference_api_object_qualified_name or (
                isinstance(reference_api_object_qualified_name, str)
                and not reference_api_object_qualified_name.strip()
            ):
                raise ValueError(
                    "Set valid qualified name for reference_api_object_qualified_name"
                )
        elif (
            reference_api_object_qualified_name
            and isinstance(reference_api_object_qualified_name, str)
            and reference_api_object_qualified_name.strip()
        ):
            raise ValueError(
                "Set is_api_object_reference to true to set reference_api_object_qualified_name"
            )

        if connection_qualified_name:
            connection_qn = connection_qualified_name
        elif parent_api_object_qualified_name:
            parts = parent_api_object_qualified_name.split("/")
            connection_qn = (
                "/".join(parts[:3])
                if len(parts) >= 3
                else parent_api_object_qualified_name
            )
        else:
            parts = (parent_api_query_qualified_name or "").split("/")
            connection_qn = (
                "/".join(parts[:3])
                if len(parts) >= 3
                else parent_api_query_qualified_name
            )

        conn_parts = (connection_qn or "").split("/")
        connector_name = conn_parts[1] if len(conn_parts) > 1 else None

        if parent_api_object_qualified_name:
            return cls(
                name=name,
                qualified_name=f"{parent_api_object_qualified_name}/{name}",
                connection_qualified_name=connection_qn,
                connector_name=connector_name,
                api_field_type=api_field_type,
                api_field_type_secondary=api_field_type_secondary,
                api_is_object_reference=is_api_object_reference,
                api_object_qualified_name=(
                    reference_api_object_qualified_name
                    if is_api_object_reference
                    else None
                ),
                api_object=RelatedAPIObject(
                    qualified_name=parent_api_object_qualified_name,
                    unique_attributes={
                        "qualifiedName": parent_api_object_qualified_name
                    },
                ),
                api_query_param_type=api_query_param_type,
            )
        return cls(
            name=name,
            qualified_name=f"{parent_api_query_qualified_name}/{name}",
            connection_qualified_name=connection_qn,
            connector_name=connector_name,
            api_field_type=api_field_type,
            api_field_type_secondary=api_field_type_secondary,
            api_is_object_reference=is_api_object_reference,
            api_object_qualified_name=(
                reference_api_object_qualified_name if is_api_object_reference else None
            ),
            api_query=RelatedAPIQuery(
                qualified_name=parent_api_query_qualified_name,
                unique_attributes={"qualifiedName": parent_api_query_qualified_name},
            ),
            api_query_param_type=api_query_param_type,
        )

    @classmethod
    def updater(cls, *, qualified_name: str, name: str) -> "APIField":
        """Create an APIField instance for update operations."""
        validate_required_fields(["qualified_name", "name"], [qualified_name, name])
        return cls(qualified_name=qualified_name, name=name)

    def trim_to_required(self) -> "APIField":
        """Return only fields required for update operations."""
        return APIField.updater(qualified_name=self.qualified_name, name=self.name)

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _api_field_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "APIField":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            APIField instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _api_field_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class APIFieldAttributes(AssetAttributes):
    """APIField-specific attributes for nested API format."""

    api_field_type: Union[str, None, UnsetType] = UNSET
    """Type of APIField, as free text (e.g. STRING, NUMBER etc)."""

    api_field_type_secondary: Union[str, None, UnsetType] = UNSET
    """Secondary type of APIField (e.g. LIST/STRING, then LIST would be the secondary type)."""

    api_query_param_type: Union[str, None, UnsetType] = UNSET
    """If parent relationship type is APIQuery, then this attribute denotes if this is input or output parameter."""

    api_spec_type: Union[str, None, UnsetType] = UNSET
    """Type of API, for example: OpenAPI, GraphQL, etc."""

    api_spec_version: Union[str, None, UnsetType] = UNSET
    """Version of the API specification."""

    api_spec_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the API spec, if this asset is contained in an API spec."""

    api_spec_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the API spec, if this asset is contained in an API spec."""

    api_external_docs: Union[dict[str, str], None, UnsetType] = UNSET
    """External documentation of the API."""

    api_is_auth_optional: Union[bool, None, UnsetType] = UNSET
    """Whether authentication is optional (true) or required (false)."""

    api_is_object_reference: Union[bool, None, UnsetType] = UNSET
    """If this asset refers to an APIObject"""

    api_object_qualified_name: Union[str, None, UnsetType] = UNSET
    """Qualified name of the APIObject that is referred to by this asset. When apiIsObjectReference is true."""


class APIFieldRelationshipAttributes(AssetRelationshipAttributes):
    """APIField-specific relationship attributes for nested API format."""

    api_object: Union[RelatedAPIObject, None, UnsetType] = UNSET
    """APIObject asset containing this APIField."""

    api_query: Union[RelatedAPIQuery, None, UnsetType] = UNSET
    """APIQuery asset containing this APIField."""


class APIFieldNested(AssetNested):
    """APIField in nested API format for high-performance serialization."""

    attributes: Union[APIFieldAttributes, UnsetType] = UNSET
    relationship_attributes: Union[APIFieldRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[APIFieldRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[APIFieldRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _api_field_to_nested(api_field: APIField) -> APIFieldNested:
    """Convert flat APIField to nested format."""
    attrs_kwargs = build_attributes_kwargs(api_field, APIFieldAttributes)
    attrs = APIFieldAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["api_object", "api_query"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        api_field, rel_fields, APIFieldRelationshipAttributes
    )
    return APIFieldNested(
        guid=api_field.guid,
        type_name=api_field.type_name,
        status=api_field.status,
        version=api_field.version,
        create_time=api_field.create_time,
        update_time=api_field.update_time,
        created_by=api_field.created_by,
        updated_by=api_field.updated_by,
        classifications=api_field.classifications,
        classification_names=api_field.classification_names,
        meanings=api_field.meanings,
        labels=api_field.labels,
        business_attributes=api_field.business_attributes,
        custom_attributes=api_field.custom_attributes,
        pending_tasks=api_field.pending_tasks,
        proxy=api_field.proxy,
        is_incomplete=api_field.is_incomplete,
        provenance_type=api_field.provenance_type,
        home_id=api_field.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _api_field_from_nested(nested: APIFieldNested) -> APIField:
    """Convert nested format to flat APIField."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else APIFieldAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["api_object", "api_query"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        APIFieldRelationshipAttributes,
    )
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, APIFieldNested, APIFieldAttributes
    )
    return APIField(**kwargs)


def _api_field_to_nested_bytes(api_field: APIField, serde: Serde) -> bytes:
    """Convert flat APIField to nested JSON bytes."""
    return serde.encode(_api_field_to_nested(api_field))


def _api_field_from_nested_bytes(data: bytes, serde: Serde) -> APIField:
    """Convert nested JSON bytes to flat APIField."""
    nested = serde.decode(data, APIFieldNested)
    return _api_field_from_nested(nested)
