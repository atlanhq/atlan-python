# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Anomalo asset model with flattened inheritance.

This module provides:
- Anomalo: Flat asset class (easy to use)
- AnomaloAttributes: Nested attributes struct (extends AssetAttributes)
- AnomaloNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Anomalo(Asset):
    """
    Base class for Anomalo assets.
    """

    # Override type_name with Anomalo-specific default
    type_name: Union[str, UnsetType] = "Anomalo"

    dq_is_part_of_contract: Union[bool, None, UnsetType] = UNSET
    """Whether this data quality is part of contract (true) or not (false)."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _anomalo_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "Anomalo":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Anomalo instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _anomalo_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class AnomaloAttributes(AssetAttributes):
    """Anomalo-specific attributes for nested API format."""

    dq_is_part_of_contract: Union[bool, None, UnsetType] = UNSET
    """Whether this data quality is part of contract (true) or not (false)."""


class AnomaloRelationshipAttributes(AssetRelationshipAttributes):
    """Anomalo-specific relationship attributes for nested API format."""

    pass


class AnomaloNested(AssetNested):
    """Anomalo in nested API format for high-performance serialization."""

    attributes: Union[AnomaloAttributes, UnsetType] = UNSET
    relationship_attributes: Union[AnomaloRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[AnomaloRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[AnomaloRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _anomalo_to_nested(anomalo: Anomalo) -> AnomaloNested:
    """Convert flat Anomalo to nested format."""
    attrs = AnomaloAttributes(
        dq_is_part_of_contract=anomalo.dq_is_part_of_contract,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        anomalo, rel_fields, AnomaloRelationshipAttributes
    )
    return AnomaloNested(
        guid=anomalo.guid,
        type_name=anomalo.type_name,
        status=anomalo.status,
        version=anomalo.version,
        create_time=anomalo.create_time,
        update_time=anomalo.update_time,
        created_by=anomalo.created_by,
        updated_by=anomalo.updated_by,
        classifications=anomalo.classifications,
        classification_names=anomalo.classification_names,
        meanings=anomalo.meanings,
        labels=anomalo.labels,
        business_attributes=anomalo.business_attributes,
        custom_attributes=anomalo.custom_attributes,
        pending_tasks=anomalo.pending_tasks,
        proxy=anomalo.proxy,
        is_incomplete=anomalo.is_incomplete,
        provenance_type=anomalo.provenance_type,
        home_id=anomalo.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _anomalo_from_nested(nested: AnomaloNested) -> Anomalo:
    """Convert nested format to flat Anomalo."""
    attrs = nested.attributes if nested.attributes is not UNSET else AnomaloAttributes()
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        AnomaloRelationshipAttributes,
    )
    return Anomalo(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        dq_is_part_of_contract=attrs.dq_is_part_of_contract,
        # Merged relationship attributes
        **merged_rels,
    )


def _anomalo_to_nested_bytes(anomalo: Anomalo, serde: Serde) -> bytes:
    """Convert flat Anomalo to nested JSON bytes."""
    return serde.encode(_anomalo_to_nested(anomalo))


def _anomalo_from_nested_bytes(data: bytes, serde: Serde) -> Anomalo:
    """Convert nested JSON bytes to flat Anomalo."""
    nested = serde.decode(data, AnomaloNested)
    return _anomalo_from_nested(nested)
