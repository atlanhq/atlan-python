# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Preset asset model with flattened inheritance.

This module provides:
- Preset: Flat asset class (easy to use)
- PresetAttributes: Nested attributes struct (extends AssetAttributes)
- PresetNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Preset(Asset):
    """
    Base class for Preset assets.
    """

    # Override type_name with Preset-specific default
    type_name: Union[str, UnsetType] = "Preset"

    preset_workspace_id: Union[int, None, UnsetType] = UNSET
    """Identifier of the workspace in which this asset exists, in Preset."""

    preset_workspace_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the workspace in which this asset exists."""

    preset_dashboard_id: Union[int, None, UnsetType] = UNSET
    """Identifier of the dashboard in which this asset exists, in Preset."""

    preset_dashboard_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the dashboard in which this asset exists."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _preset_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(json_data: Union[str, bytes], serde: Serde | None = None) -> "Preset":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Preset instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _preset_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class PresetAttributes(AssetAttributes):
    """Preset-specific attributes for nested API format."""

    preset_workspace_id: Union[int, None, UnsetType] = UNSET
    """Identifier of the workspace in which this asset exists, in Preset."""

    preset_workspace_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the workspace in which this asset exists."""

    preset_dashboard_id: Union[int, None, UnsetType] = UNSET
    """Identifier of the dashboard in which this asset exists, in Preset."""

    preset_dashboard_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the dashboard in which this asset exists."""


class PresetRelationshipAttributes(AssetRelationshipAttributes):
    """Preset-specific relationship attributes for nested API format."""

    pass


class PresetNested(AssetNested):
    """Preset in nested API format for high-performance serialization."""

    attributes: Union[PresetAttributes, UnsetType] = UNSET
    relationship_attributes: Union[PresetRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[PresetRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[PresetRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _preset_to_nested(preset: Preset) -> PresetNested:
    """Convert flat Preset to nested format."""
    attrs = PresetAttributes(
        preset_workspace_id=preset.preset_workspace_id,
        preset_workspace_qualified_name=preset.preset_workspace_qualified_name,
        preset_dashboard_id=preset.preset_dashboard_id,
        preset_dashboard_qualified_name=preset.preset_dashboard_qualified_name,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        preset, rel_fields, PresetRelationshipAttributes
    )
    return PresetNested(
        guid=preset.guid,
        type_name=preset.type_name,
        status=preset.status,
        version=preset.version,
        create_time=preset.create_time,
        update_time=preset.update_time,
        created_by=preset.created_by,
        updated_by=preset.updated_by,
        classifications=preset.classifications,
        classification_names=preset.classification_names,
        meanings=preset.meanings,
        labels=preset.labels,
        business_attributes=preset.business_attributes,
        custom_attributes=preset.custom_attributes,
        pending_tasks=preset.pending_tasks,
        proxy=preset.proxy,
        is_incomplete=preset.is_incomplete,
        provenance_type=preset.provenance_type,
        home_id=preset.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _preset_from_nested(nested: PresetNested) -> Preset:
    """Convert nested format to flat Preset."""
    attrs = nested.attributes if nested.attributes is not UNSET else PresetAttributes()
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        PresetRelationshipAttributes,
    )
    return Preset(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        preset_workspace_id=attrs.preset_workspace_id,
        preset_workspace_qualified_name=attrs.preset_workspace_qualified_name,
        preset_dashboard_id=attrs.preset_dashboard_id,
        preset_dashboard_qualified_name=attrs.preset_dashboard_qualified_name,
        # Merged relationship attributes
        **merged_rels,
    )


def _preset_to_nested_bytes(preset: Preset, serde: Serde) -> bytes:
    """Convert flat Preset to nested JSON bytes."""
    return serde.encode(_preset_to_nested(preset))


def _preset_from_nested_bytes(data: bytes, serde: Serde) -> Preset:
    """Convert nested JSON bytes to flat Preset."""
    nested = serde.decode(data, PresetNested)
    return _preset_from_nested(nested)
