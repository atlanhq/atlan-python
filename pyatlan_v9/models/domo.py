# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Domo asset model with flattened inheritance.

This module provides:
- Domo: Flat asset class (easy to use)
- DomoAttributes: Nested attributes struct (extends AssetAttributes)
- DomoNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Domo(Asset):
    """
    Base class for Domo assets.
    """

    # Override type_name with Domo-specific default
    type_name: Union[str, UnsetType] = "Domo"

    domo_id: Union[str, None, UnsetType] = UNSET
    """Id of the Domo dataset."""

    domo_owner_id: Union[str, None, UnsetType] = UNSET
    """Id of the owner of the Domo dataset."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _domo_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(json_data: Union[str, bytes], serde: Serde | None = None) -> "Domo":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Domo instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _domo_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class DomoAttributes(AssetAttributes):
    """Domo-specific attributes for nested API format."""

    domo_id: Union[str, None, UnsetType] = UNSET
    """Id of the Domo dataset."""

    domo_owner_id: Union[str, None, UnsetType] = UNSET
    """Id of the owner of the Domo dataset."""


class DomoRelationshipAttributes(AssetRelationshipAttributes):
    """Domo-specific relationship attributes for nested API format."""

    pass


class DomoNested(AssetNested):
    """Domo in nested API format for high-performance serialization."""

    attributes: Union[DomoAttributes, UnsetType] = UNSET
    relationship_attributes: Union[DomoRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[DomoRelationshipAttributes, UnsetType] = UNSET
    remove_relationship_attributes: Union[DomoRelationshipAttributes, UnsetType] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _domo_to_nested(domo: Domo) -> DomoNested:
    """Convert flat Domo to nested format."""
    attrs = DomoAttributes(
        domo_id=domo.domo_id,
        domo_owner_id=domo.domo_owner_id,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        domo, rel_fields, DomoRelationshipAttributes
    )
    return DomoNested(
        guid=domo.guid,
        type_name=domo.type_name,
        status=domo.status,
        version=domo.version,
        create_time=domo.create_time,
        update_time=domo.update_time,
        created_by=domo.created_by,
        updated_by=domo.updated_by,
        classifications=domo.classifications,
        classification_names=domo.classification_names,
        meanings=domo.meanings,
        labels=domo.labels,
        business_attributes=domo.business_attributes,
        custom_attributes=domo.custom_attributes,
        pending_tasks=domo.pending_tasks,
        proxy=domo.proxy,
        is_incomplete=domo.is_incomplete,
        provenance_type=domo.provenance_type,
        home_id=domo.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _domo_from_nested(nested: DomoNested) -> Domo:
    """Convert nested format to flat Domo."""
    attrs = nested.attributes if nested.attributes is not UNSET else DomoAttributes()
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        DomoRelationshipAttributes,
    )
    return Domo(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        domo_id=attrs.domo_id,
        domo_owner_id=attrs.domo_owner_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _domo_to_nested_bytes(domo: Domo, serde: Serde) -> bytes:
    """Convert flat Domo to nested JSON bytes."""
    return serde.encode(_domo_to_nested(domo))


def _domo_from_nested_bytes(data: bytes, serde: Serde) -> Domo:
    """Convert nested JSON bytes to flat Domo."""
    nested = serde.decode(data, DomoNested)
    return _domo_from_nested(nested)
