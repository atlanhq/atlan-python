# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
CogniteTimeSeries asset model with flattened inheritance.

This module provides:
- CogniteTimeSeries: Flat asset class (easy to use)
- CogniteTimeSeriesAttributes: Nested attributes struct (extends AssetAttributes)
- CogniteTimeSeriesNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .cognite_related import RelatedCogniteAsset

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class CogniteTimeSeries(Asset):
    """
    Instance of a Cognite time series in Atlan.
    """

    # Override type_name with CogniteTimeSeries-specific default
    type_name: Union[str, UnsetType] = "CogniteTimeSeries"

    cognite_asset: Union[RelatedCogniteAsset, None, UnsetType] = UNSET
    """Asset in which this time series exists."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _cognite_time_series_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "CogniteTimeSeries":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            CogniteTimeSeries instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _cognite_time_series_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class CogniteTimeSeriesAttributes(AssetAttributes):
    """CogniteTimeSeries-specific attributes for nested API format."""

    pass


class CogniteTimeSeriesRelationshipAttributes(AssetRelationshipAttributes):
    """CogniteTimeSeries-specific relationship attributes for nested API format."""

    cognite_asset: Union[RelatedCogniteAsset, None, UnsetType] = UNSET
    """Asset in which this time series exists."""


class CogniteTimeSeriesNested(AssetNested):
    """CogniteTimeSeries in nested API format for high-performance serialization."""

    attributes: Union[CogniteTimeSeriesAttributes, UnsetType] = UNSET
    relationship_attributes: Union[
        CogniteTimeSeriesRelationshipAttributes, UnsetType
    ] = UNSET
    append_relationship_attributes: Union[
        CogniteTimeSeriesRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        CogniteTimeSeriesRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _cognite_time_series_to_nested(
    cognite_time_series: CogniteTimeSeries,
) -> CogniteTimeSeriesNested:
    """Convert flat CogniteTimeSeries to nested format."""
    attrs = CogniteTimeSeriesAttributes()
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["cognite_asset"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        cognite_time_series, rel_fields, CogniteTimeSeriesRelationshipAttributes
    )
    return CogniteTimeSeriesNested(
        guid=cognite_time_series.guid,
        type_name=cognite_time_series.type_name,
        status=cognite_time_series.status,
        version=cognite_time_series.version,
        create_time=cognite_time_series.create_time,
        update_time=cognite_time_series.update_time,
        created_by=cognite_time_series.created_by,
        updated_by=cognite_time_series.updated_by,
        classifications=cognite_time_series.classifications,
        classification_names=cognite_time_series.classification_names,
        meanings=cognite_time_series.meanings,
        labels=cognite_time_series.labels,
        business_attributes=cognite_time_series.business_attributes,
        custom_attributes=cognite_time_series.custom_attributes,
        pending_tasks=cognite_time_series.pending_tasks,
        proxy=cognite_time_series.proxy,
        is_incomplete=cognite_time_series.is_incomplete,
        provenance_type=cognite_time_series.provenance_type,
        home_id=cognite_time_series.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _cognite_time_series_from_nested(
    nested: CogniteTimeSeriesNested,
) -> CogniteTimeSeries:
    """Convert nested format to flat CogniteTimeSeries."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else CogniteTimeSeriesAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["cognite_asset"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        CogniteTimeSeriesRelationshipAttributes,
    )
    return CogniteTimeSeries(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _cognite_time_series_to_nested_bytes(
    cognite_time_series: CogniteTimeSeries, serde: Serde
) -> bytes:
    """Convert flat CogniteTimeSeries to nested JSON bytes."""
    return serde.encode(_cognite_time_series_to_nested(cognite_time_series))


def _cognite_time_series_from_nested_bytes(
    data: bytes, serde: Serde
) -> CogniteTimeSeries:
    """Convert nested JSON bytes to flat CogniteTimeSeries."""
    nested = serde.decode(data, CogniteTimeSeriesNested)
    return _cognite_time_series_from_nested(nested)
