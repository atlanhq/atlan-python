# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Cognite3DModel asset model with flattened inheritance.

This module provides:
- Cognite3DModel: Flat asset class (easy to use)
- Cognite3DModelAttributes: Nested attributes struct (extends AssetAttributes)
- Cognite3DModelNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .cognite_related import RelatedCogniteAsset

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Cognite3DModel(Asset):
    """
    Instance of a Cognite 3D model in Atlan.
    """

    # Override type_name with Cognite3DModel-specific default
    type_name: Union[str, UnsetType] = "Cognite3DModel"

    cognite_asset: Union[RelatedCogniteAsset, None, UnsetType] = UNSET
    """Asset in which this 3D model exists."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _cognite3_d_model_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "Cognite3DModel":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Cognite3DModel instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _cognite3_d_model_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class Cognite3DModelAttributes(AssetAttributes):
    """Cognite3DModel-specific attributes for nested API format."""

    pass


class Cognite3DModelRelationshipAttributes(AssetRelationshipAttributes):
    """Cognite3DModel-specific relationship attributes for nested API format."""

    cognite_asset: Union[RelatedCogniteAsset, None, UnsetType] = UNSET
    """Asset in which this 3D model exists."""


class Cognite3DModelNested(AssetNested):
    """Cognite3DModel in nested API format for high-performance serialization."""

    attributes: Union[Cognite3DModelAttributes, UnsetType] = UNSET
    relationship_attributes: Union[Cognite3DModelRelationshipAttributes, UnsetType] = (
        UNSET
    )
    append_relationship_attributes: Union[
        Cognite3DModelRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        Cognite3DModelRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _cognite3_d_model_to_nested(
    cognite3_d_model: Cognite3DModel,
) -> Cognite3DModelNested:
    """Convert flat Cognite3DModel to nested format."""
    attrs = Cognite3DModelAttributes()
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["cognite_asset"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        cognite3_d_model, rel_fields, Cognite3DModelRelationshipAttributes
    )
    return Cognite3DModelNested(
        guid=cognite3_d_model.guid,
        type_name=cognite3_d_model.type_name,
        status=cognite3_d_model.status,
        version=cognite3_d_model.version,
        create_time=cognite3_d_model.create_time,
        update_time=cognite3_d_model.update_time,
        created_by=cognite3_d_model.created_by,
        updated_by=cognite3_d_model.updated_by,
        classifications=cognite3_d_model.classifications,
        classification_names=cognite3_d_model.classification_names,
        meanings=cognite3_d_model.meanings,
        labels=cognite3_d_model.labels,
        business_attributes=cognite3_d_model.business_attributes,
        custom_attributes=cognite3_d_model.custom_attributes,
        pending_tasks=cognite3_d_model.pending_tasks,
        proxy=cognite3_d_model.proxy,
        is_incomplete=cognite3_d_model.is_incomplete,
        provenance_type=cognite3_d_model.provenance_type,
        home_id=cognite3_d_model.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _cognite3_d_model_from_nested(nested: Cognite3DModelNested) -> Cognite3DModel:
    """Convert nested format to flat Cognite3DModel."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else Cognite3DModelAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["cognite_asset"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        Cognite3DModelRelationshipAttributes,
    )
    return Cognite3DModel(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _cognite3_d_model_to_nested_bytes(
    cognite3_d_model: Cognite3DModel, serde: Serde
) -> bytes:
    """Convert flat Cognite3DModel to nested JSON bytes."""
    return serde.encode(_cognite3_d_model_to_nested(cognite3_d_model))


def _cognite3_d_model_from_nested_bytes(data: bytes, serde: Serde) -> Cognite3DModel:
    """Convert nested JSON bytes to flat Cognite3DModel."""
    nested = serde.decode(data, Cognite3DModelNested)
    return _cognite3_d_model_from_nested(nested)
