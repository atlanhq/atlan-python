# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Redash asset model with flattened inheritance.

This module provides:
- Redash: Flat asset class (easy to use)
- RedashAttributes: Nested attributes struct (extends AssetAttributes)
- RedashNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Redash(Asset):
    """
    Base class for Redash assets.
    """

    # Override type_name with Redash-specific default
    type_name: Union[str, UnsetType] = "Redash"

    redash_is_published: Union[bool, None, UnsetType] = UNSET
    """Whether this asset is published in Redash (true) or not (false)."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _redash_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(json_data: Union[str, bytes], serde: Serde | None = None) -> "Redash":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Redash instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _redash_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class RedashAttributes(AssetAttributes):
    """Redash-specific attributes for nested API format."""

    redash_is_published: Union[bool, None, UnsetType] = UNSET
    """Whether this asset is published in Redash (true) or not (false)."""


class RedashRelationshipAttributes(AssetRelationshipAttributes):
    """Redash-specific relationship attributes for nested API format."""

    pass


class RedashNested(AssetNested):
    """Redash in nested API format for high-performance serialization."""

    attributes: Union[RedashAttributes, UnsetType] = UNSET
    relationship_attributes: Union[RedashRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[RedashRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[RedashRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _redash_to_nested(redash: Redash) -> RedashNested:
    """Convert flat Redash to nested format."""
    attrs = RedashAttributes(
        redash_is_published=redash.redash_is_published,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        redash, rel_fields, RedashRelationshipAttributes
    )
    return RedashNested(
        guid=redash.guid,
        type_name=redash.type_name,
        status=redash.status,
        version=redash.version,
        create_time=redash.create_time,
        update_time=redash.update_time,
        created_by=redash.created_by,
        updated_by=redash.updated_by,
        classifications=redash.classifications,
        classification_names=redash.classification_names,
        meanings=redash.meanings,
        labels=redash.labels,
        business_attributes=redash.business_attributes,
        custom_attributes=redash.custom_attributes,
        pending_tasks=redash.pending_tasks,
        proxy=redash.proxy,
        is_incomplete=redash.is_incomplete,
        provenance_type=redash.provenance_type,
        home_id=redash.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _redash_from_nested(nested: RedashNested) -> Redash:
    """Convert nested format to flat Redash."""
    attrs = nested.attributes if nested.attributes is not UNSET else RedashAttributes()
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        RedashRelationshipAttributes,
    )
    return Redash(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        redash_is_published=attrs.redash_is_published,
        # Merged relationship attributes
        **merged_rels,
    )


def _redash_to_nested_bytes(redash: Redash, serde: Serde) -> bytes:
    """Convert flat Redash to nested JSON bytes."""
    return serde.encode(_redash_to_nested(redash))


def _redash_from_nested_bytes(data: bytes, serde: Serde) -> Redash:
    """Convert nested JSON bytes to flat Redash."""
    nested = serde.decode(data, RedashNested)
    return _redash_from_nested(nested)
