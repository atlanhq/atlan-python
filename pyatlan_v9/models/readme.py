# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Readme asset model with flattened inheritance.

This module provides:
- Readme: Flat asset class (easy to use)
- ReadmeAttributes: Nested attributes struct (extends AssetAttributes)
- ReadmeNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union
from urllib.parse import quote, unquote

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .asset_related import RelatedAsset
from .resource_related import RelatedReadme

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Readme(Asset):
    """
    Instance of a README in Atlan.
    """

    # Override type_name with Readme-specific default
    type_name: Union[str, UnsetType] = "Readme"

    link: Union[str, None, UnsetType] = UNSET
    """URL to the resource."""

    is_global: Union[bool, None, UnsetType] = UNSET
    """Whether the resource is global (true) or not (false)."""

    reference: Union[str, None, UnsetType] = UNSET
    """Reference to the resource."""

    resource_metadata: Union[dict[str, str], None, UnsetType] = UNSET
    """Metadata of the resource."""

    asset: Union[RelatedAsset, None, UnsetType] = UNSET
    """Asset that this README describes."""

    see_also: Union[list[RelatedReadme], None, UnsetType] = UNSET
    """"""

    @property
    def description(self) -> Union[str, None, UnsetType]:
        """Decode URL-encoded description content for parity with legacy models."""
        if self.user_description is not UNSET:
            return (
                unquote(self.user_description)
                if self.user_description is not None
                else None
            )
        if self.asset_source_readme is not UNSET:
            return (
                unquote(self.asset_source_readme)
                if self.asset_source_readme is not None
                else None
            )
        return UNSET

    @description.setter
    def description(self, description: Union[str, None, UnsetType]) -> None:
        """Store README content in user_description with URL encoding."""
        if description is UNSET:
            self.user_description = UNSET
            return
        self.user_description = quote(description) if description is not None else None

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        asset: Asset,
        content: str,
        asset_name: Union[str, None] = None,
    ) -> "Readme":
        """Create a new Readme asset."""
        validate_required_fields(["asset", "content"], [asset, content])
        actual_asset_name = asset.name if asset.name is not UNSET else None
        if actual_asset_name:
            if asset_name:
                raise ValueError(
                    "asset_name can not be given when name is available from asset"
                )
            asset_name = actual_asset_name
        elif not asset_name:
            raise ValueError(
                "asset_name is required when name is not available from asset"
            )
        if asset.guid is UNSET or not asset.guid:
            raise ValueError(
                "asset guid must be present, use the client.asset.ref_by_guid() method to retrieve an asset by its GUID"
            )
        return cls(
            qualified_name=f"{asset.guid}/readme",
            name=f"{asset_name} Readme",
            asset=RelatedAsset(guid=asset.guid),
            user_description=quote(content),
        )

    @classmethod
    def updater(cls, *, qualified_name: str, name: str) -> "Readme":
        """Create a Readme instance for update operations."""
        validate_required_fields(["qualified_name", "name"], [qualified_name, name])
        return cls(qualified_name=qualified_name, name=name)

    def trim_to_required(self) -> "Readme":
        """Return only fields required for update operations."""
        return Readme.updater(qualified_name=self.qualified_name, name=self.name)

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _readme_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(json_data: Union[str, bytes], serde: Serde | None = None) -> "Readme":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Readme instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _readme_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class ReadmeAttributes(AssetAttributes):
    """Readme-specific attributes for nested API format."""

    link: Union[str, None, UnsetType] = UNSET
    """URL to the resource."""

    is_global: Union[bool, None, UnsetType] = UNSET
    """Whether the resource is global (true) or not (false)."""

    reference: Union[str, None, UnsetType] = UNSET
    """Reference to the resource."""

    resource_metadata: Union[dict[str, str], None, UnsetType] = UNSET
    """Metadata of the resource."""


class ReadmeRelationshipAttributes(AssetRelationshipAttributes):
    """Readme-specific relationship attributes for nested API format."""

    asset: Union[RelatedAsset, None, UnsetType] = UNSET
    """Asset that this README describes."""

    see_also: Union[list[RelatedReadme], None, UnsetType] = UNSET
    """"""


class ReadmeNested(AssetNested):
    """Readme in nested API format for high-performance serialization."""

    attributes: Union[ReadmeAttributes, UnsetType] = UNSET
    relationship_attributes: Union[ReadmeRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[ReadmeRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[ReadmeRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _readme_to_nested(readme: Readme) -> ReadmeNested:
    """Convert flat Readme to nested format."""
    attrs_kwargs = build_attributes_kwargs(readme, ReadmeAttributes)
    attrs = ReadmeAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["asset", "see_also"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        readme, rel_fields, ReadmeRelationshipAttributes
    )
    return ReadmeNested(
        guid=readme.guid,
        type_name=readme.type_name,
        status=readme.status,
        version=readme.version,
        create_time=readme.create_time,
        update_time=readme.update_time,
        created_by=readme.created_by,
        updated_by=readme.updated_by,
        classifications=readme.classifications,
        classification_names=readme.classification_names,
        meanings=readme.meanings,
        labels=readme.labels,
        business_attributes=readme.business_attributes,
        custom_attributes=readme.custom_attributes,
        pending_tasks=readme.pending_tasks,
        proxy=readme.proxy,
        is_incomplete=readme.is_incomplete,
        provenance_type=readme.provenance_type,
        home_id=readme.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _readme_from_nested(nested: ReadmeNested) -> Readme:
    """Convert nested format to flat Readme."""
    attrs = nested.attributes if nested.attributes is not UNSET else ReadmeAttributes()
    rel_fields: list[str] = ["asset", "see_also"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        ReadmeRelationshipAttributes,
    )
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, AssetNested, ReadmeAttributes
    )
    return Readme(**kwargs)


def _readme_to_nested_bytes(readme: Readme, serde: Serde) -> bytes:
    """Convert flat Readme to nested JSON bytes."""
    return serde.encode(_readme_to_nested(readme))


def _readme_from_nested_bytes(data: bytes, serde: Serde) -> Readme:
    """Convert nested JSON bytes to flat Readme."""
    nested = serde.decode(data, ReadmeNested)
    return _readme_from_nested(nested)
