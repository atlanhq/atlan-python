# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Database asset model with flattened inheritance.

This module provides:
- Database: Flat asset class (easy to use)
- DatabaseAttributes: Nested attributes struct (extends AssetAttributes)
- DatabaseNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

import msgspec
from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .sql_related import RelatedSchema

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Database(Asset):
    """
    Instance of a (relational) database in Atlan.
    """

    # Override type_name with Database-specific default
    type_name: Union[str, UnsetType] = "Database"

    schema_count: Union[int, None, UnsetType] = UNSET
    """Number of schemas in this database."""

    query_count: Union[int, None, UnsetType] = UNSET
    """Number of times this asset has been queried."""

    query_user_count: Union[int, None, UnsetType] = UNSET
    """Number of unique users who have queried this asset."""

    query_user_map: Union[dict[str, int], None, UnsetType] = UNSET
    """Map of unique users who have queried this asset to the number of times they have queried it."""

    query_count_updated_at: Union[int, None, UnsetType] = UNSET
    """Time (epoch) at which the query count was last updated, in milliseconds."""

    database_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the database in which this SQL asset exists, or empty if it does not exist within a database."""

    database_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the database in which this SQL asset exists, or empty if it does not exist within a database."""

    schema_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the schema in which this SQL asset exists, or empty if it does not exist within a schema."""

    schema_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the schema in which this SQL asset exists, or empty if it does not exist within a schema."""

    table_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the table in which this SQL asset exists, or empty if it does not exist within a table."""

    table_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the table in which this SQL asset exists, or empty if it does not exist within a table."""

    view_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the view in which this SQL asset exists, or empty if it does not exist within a view."""

    view_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the view in which this SQL asset exists, or empty if it does not exist within a view."""

    calculation_view_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the calculation view in which this SQL asset exists, or empty if it does not exist within a calculation view."""

    calculation_view_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the calculation view in which this SQL asset exists, or empty if it does not exist within a calculation view."""

    is_profiled: Union[bool, None, UnsetType] = UNSET
    """Whether this asset has been profiled (true) or not (false)."""

    last_profiled_at: Union[int, None, UnsetType] = UNSET
    """Time (epoch) at which this asset was last profiled, in milliseconds."""

    sql_ai_model_context_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the context in which the model versions exist, or empty if it does not exist within an AI model context."""

    sql_is_secure: Union[bool, None, UnsetType] = UNSET
    """Whether this asset is secure (true) or not (false)."""

    schemas: Union[list[RelatedSchema], None, UnsetType] = UNSET
    """Schemas that exist within this database."""

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        name: str,
        connection_qualified_name: str,
    ) -> "Database":
        """
        Create a new Database asset.

        Args:
            name: Name of the database
            connection_qualified_name: Unique name of the connection in which this database exists

        Returns:
            Database instance ready to be created

        Raises:
            ValueError: If required parameters are missing or invalid
        """
        validate_required_fields(
            ["name", "connection_qualified_name"], [name, connection_qualified_name]
        )

        fields = connection_qualified_name.split("/")
        if len(fields) != 3:
            raise ValueError(
                f"Invalid connection_qualified_name: {connection_qualified_name}. "
                "Expected format: default/connector/connection_id"
            )

        connector_name = fields[1]
        qualified_name = f"{connection_qualified_name}/{name}"

        return cls(
            name=name,
            qualified_name=qualified_name,
            connection_qualified_name=connection_qualified_name,
            connector_name=connector_name,
        )

    @classmethod
    def create(cls, *, name: str, connection_qualified_name: str) -> "Database":
        """
        Create a new Database asset (deprecated - use creator instead).

        Args:
            name: Name of the database
            connection_qualified_name: Unique name of the connection in which this database exists

        Returns:
            Database instance ready to be created
        """
        return cls.creator(
            name=name, connection_qualified_name=connection_qualified_name
        )

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _database_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "Database":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Database instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _database_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class DatabaseAttributes(AssetAttributes):
    """Database-specific attributes for nested API format."""

    schema_count: Union[int, None, UnsetType] = UNSET
    """Number of schemas in this database."""

    query_count: Union[int, None, UnsetType] = UNSET
    """Number of times this asset has been queried."""

    query_user_count: Union[int, None, UnsetType] = UNSET
    """Number of unique users who have queried this asset."""

    query_user_map: Union[dict[str, int], None, UnsetType] = UNSET
    """Map of unique users who have queried this asset to the number of times they have queried it."""

    query_count_updated_at: Union[int, None, UnsetType] = UNSET
    """Time (epoch) at which the query count was last updated, in milliseconds."""

    database_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the database in which this SQL asset exists, or empty if it does not exist within a database."""

    database_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the database in which this SQL asset exists, or empty if it does not exist within a database."""

    schema_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the schema in which this SQL asset exists, or empty if it does not exist within a schema."""

    schema_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the schema in which this SQL asset exists, or empty if it does not exist within a schema."""

    table_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the table in which this SQL asset exists, or empty if it does not exist within a table."""

    table_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the table in which this SQL asset exists, or empty if it does not exist within a table."""

    view_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the view in which this SQL asset exists, or empty if it does not exist within a view."""

    view_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the view in which this SQL asset exists, or empty if it does not exist within a view."""

    calculation_view_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the calculation view in which this SQL asset exists, or empty if it does not exist within a calculation view."""

    calculation_view_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the calculation view in which this SQL asset exists, or empty if it does not exist within a calculation view."""

    is_profiled: Union[bool, None, UnsetType] = UNSET
    """Whether this asset has been profiled (true) or not (false)."""

    last_profiled_at: Union[int, None, UnsetType] = UNSET
    """Time (epoch) at which this asset was last profiled, in milliseconds."""

    sql_ai_model_context_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the context in which the model versions exist, or empty if it does not exist within an AI model context."""

    sql_is_secure: Union[bool, None, UnsetType] = UNSET
    """Whether this asset is secure (true) or not (false)."""


class DatabaseRelationshipAttributes(AssetRelationshipAttributes):
    """Database-specific relationship attributes for nested API format."""

    schemas: Union[list[RelatedSchema], None, UnsetType] = UNSET
    """Schemas that exist within this database."""


class DatabaseNested(AssetNested):
    """Database in nested API format for high-performance serialization."""

    attributes: Union[DatabaseAttributes, UnsetType] = UNSET
    relationship_attributes: Union[DatabaseRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[DatabaseRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[DatabaseRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _database_to_nested(database: Database) -> DatabaseNested:
    """Convert flat Database to nested format."""
    # Get all attribute field names dynamically
    attr_field_names = {f.name for f in msgspec.structs.fields(DatabaseAttributes)}

    # Build attributes dict with only the fields that exist in DatabaseAttributes
    attrs_kwargs = {
        name: getattr(database, name)
        for name in attr_field_names
        if hasattr(database, name)
    }
    attrs = DatabaseAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["schemas"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        database, rel_fields, DatabaseRelationshipAttributes
    )
    return DatabaseNested(
        guid=database.guid,
        type_name=database.type_name,
        status=database.status,
        version=database.version,
        create_time=database.create_time,
        update_time=database.update_time,
        created_by=database.created_by,
        updated_by=database.updated_by,
        classifications=database.classifications,
        classification_names=database.classification_names,
        meanings=database.meanings,
        labels=database.labels,
        business_attributes=database.business_attributes,
        custom_attributes=database.custom_attributes,
        pending_tasks=database.pending_tasks,
        proxy=database.proxy,
        is_incomplete=database.is_incomplete,
        provenance_type=database.provenance_type,
        home_id=database.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _database_from_nested(nested: DatabaseNested) -> Database:
    """Convert nested format to flat Database."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else DatabaseAttributes()
    )

    # Merge relationships from all three buckets
    rel_fields: list[str] = ["schemas"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        DatabaseRelationshipAttributes,
    )

    # Get top-level field names (exclude attributes and relationship fields)
    top_level_fields = {
        f.name
        for f in msgspec.structs.fields(AssetNested)
        if f.name
        not in (
            "attributes",
            "relationship_attributes",
            "append_relationship_attributes",
            "remove_relationship_attributes",
        )
    }

    # Get attribute field names
    attr_field_names = {f.name for f in msgspec.structs.fields(DatabaseAttributes)}

    # Build kwargs: top-level fields + attribute fields + relationships
    kwargs = {}

    # Add top-level fields from nested
    for name in top_level_fields:
        if hasattr(nested, name):
            kwargs[name] = getattr(nested, name)

    # Add attribute fields from attrs
    for name in attr_field_names:
        if hasattr(attrs, name):
            kwargs[name] = getattr(attrs, name)

    # Add merged relationships
    kwargs.update(merged_rels)

    return Database(**kwargs)


def _database_to_nested_bytes(database: Database, serde: Serde) -> bytes:
    """Convert flat Database to nested JSON bytes."""
    return serde.encode(_database_to_nested(database))


def _database_from_nested_bytes(data: bytes, serde: Serde) -> Database:
    """Convert nested JSON bytes to flat Database."""
    nested = serde.decode(data, DatabaseNested)
    return _database_from_nested(nested)
