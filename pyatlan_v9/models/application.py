# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Application asset model with flattened inheritance.

This module provides:
- Application: Flat asset class (easy to use)
- ApplicationAttributes: Nested attributes struct (extends AssetAttributes)
- ApplicationNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .app_related import RelatedApplicationField
from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .asset_related import RelatedAsset

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Application(Asset):
    """
    Instances of Application in Atlan.
    """

    # Override type_name with Application-specific default
    type_name: Union[str, UnsetType] = "Application"

    app_id: Union[str, None, UnsetType] = UNSET
    """Unique identifier for the application asset from the source system."""

    application_owned_assets: Union[list[RelatedAsset], None, UnsetType] = UNSET
    """Assets owned by the Application."""

    application_child_fields: Union[list[RelatedApplicationField], None, UnsetType] = (
        UNSET
    )
    """ApplicationFields owned by the Application."""

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        name: str,
        connection_qualified_name: str,
    ) -> "Application":
        """Create a new Application asset."""
        validate_required_fields(
            ["name", "connection_qualified_name"], [name, connection_qualified_name]
        )
        connector_name = (
            connection_qualified_name.split("/")[1]
            if len(connection_qualified_name.split("/")) > 1
            else ""
        )
        return cls(
            name=name,
            qualified_name=f"{connection_qualified_name}/{name}",
            connection_qualified_name=connection_qualified_name,
            connector_name=connector_name,
        )

    @classmethod
    def updater(cls, *, qualified_name: str, name: str) -> "Application":
        """Create an Application instance for update operations."""
        validate_required_fields(["qualified_name", "name"], [qualified_name, name])
        return cls(qualified_name=qualified_name, name=name)

    def trim_to_required(self) -> "Application":
        """Return only fields required for update operations."""
        return Application.updater(qualified_name=self.qualified_name, name=self.name)

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _application_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "Application":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Application instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _application_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class ApplicationAttributes(AssetAttributes):
    """Application-specific attributes for nested API format."""

    app_id: Union[str, None, UnsetType] = UNSET
    """Unique identifier for the application asset from the source system."""


class ApplicationRelationshipAttributes(AssetRelationshipAttributes):
    """Application-specific relationship attributes for nested API format."""

    application_owned_assets: Union[list[RelatedAsset], None, UnsetType] = UNSET
    """Assets owned by the Application."""

    application_child_fields: Union[list[RelatedApplicationField], None, UnsetType] = (
        UNSET
    )
    """ApplicationFields owned by the Application."""


class ApplicationNested(AssetNested):
    """Application in nested API format for high-performance serialization."""

    attributes: Union[ApplicationAttributes, UnsetType] = UNSET
    relationship_attributes: Union[ApplicationRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[
        ApplicationRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        ApplicationRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _application_to_nested(application: Application) -> ApplicationNested:
    """Convert flat Application to nested format."""
    attrs_kwargs = build_attributes_kwargs(application, ApplicationAttributes)
    attrs = ApplicationAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["application_owned_assets", "application_child_fields"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        application, rel_fields, ApplicationRelationshipAttributes
    )
    return ApplicationNested(
        guid=application.guid,
        type_name=application.type_name,
        status=application.status,
        version=application.version,
        create_time=application.create_time,
        update_time=application.update_time,
        created_by=application.created_by,
        updated_by=application.updated_by,
        classifications=application.classifications,
        classification_names=application.classification_names,
        meanings=application.meanings,
        labels=application.labels,
        business_attributes=application.business_attributes,
        custom_attributes=application.custom_attributes,
        pending_tasks=application.pending_tasks,
        proxy=application.proxy,
        is_incomplete=application.is_incomplete,
        provenance_type=application.provenance_type,
        home_id=application.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _application_from_nested(nested: ApplicationNested) -> Application:
    """Convert nested format to flat Application."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else ApplicationAttributes()
    )
    rel_fields: list[str] = ["application_owned_assets", "application_child_fields"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        ApplicationRelationshipAttributes,
    )
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, AssetNested, ApplicationAttributes
    )
    return Application(**kwargs)


def _application_to_nested_bytes(application: Application, serde: Serde) -> bytes:
    """Convert flat Application to nested JSON bytes."""
    return serde.encode(_application_to_nested(application))


def _application_from_nested_bytes(data: bytes, serde: Serde) -> Application:
    """Convert nested JSON bytes to flat Application."""
    nested = serde.decode(data, ApplicationNested)
    return _application_from_nested(nested)
