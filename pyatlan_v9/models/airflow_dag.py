# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
AirflowDag asset model with flattened inheritance.

This module provides:
- AirflowDag: Flat asset class (easy to use)
- AirflowDagAttributes: Nested attributes struct (extends AssetAttributes)
- AirflowDagNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .airflow_related import RelatedAirflowTask
from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class AirflowDag(Asset):
    """
    Instance of an Airflow DAG in Atlan.
    """

    # Override type_name with AirflowDag-specific default
    type_name: Union[str, UnsetType] = "AirflowDag"

    airflow_dag_schedule: Union[str, None, UnsetType] = UNSET
    """Schedule for the DAG."""

    airflow_dag_schedule_delta: Union[int, None, UnsetType] = UNSET
    """Duration between scheduled runs, in seconds."""

    airflow_tags: Union[list[str], None, UnsetType] = UNSET
    """Tags assigned to the asset in Airflow."""

    airflow_run_version: Union[str, None, UnsetType] = UNSET
    """Version of the run in Airflow."""

    airflow_run_open_lineage_version: Union[str, None, UnsetType] = UNSET
    """Version of the run in OpenLineage."""

    airflow_run_name: Union[str, None, UnsetType] = UNSET
    """Name of the run."""

    airflow_run_type: Union[str, None, UnsetType] = UNSET
    """Type of the run."""

    airflow_run_start_time: Union[int, None, UnsetType] = UNSET
    """Start time of the run."""

    airflow_run_end_time: Union[int, None, UnsetType] = UNSET
    """End time of the run."""

    airflow_run_open_lineage_state: Union[str, None, UnsetType] = UNSET
    """State of the run in OpenLineage."""

    airflow_tasks: Union[list[RelatedAirflowTask], None, UnsetType] = UNSET
    """Tasks that exist within this DAG."""

    # =========================================================================
    # Convenience Methods
    # =========================================================================

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        name: str,
        connection_qualified_name: str,
    ) -> "AirflowDag":
        validate_required_fields(
            ["name", "connection_qualified_name"],
            [name, connection_qualified_name],
        )
        fields = connection_qualified_name.split("/")
        connector_name = fields[1] if len(fields) > 1 else None
        qualified_name = f"{connection_qualified_name}/{name}"
        return cls(
            name=name,
            qualified_name=qualified_name,
            connector_name=connector_name,
            connection_qualified_name=connection_qualified_name,
        )

    @classmethod
    def create(cls, **kwargs) -> "AirflowDag":
        return cls.creator(**kwargs)

    @classmethod
    def create_for_modification(cls, **kwargs) -> "AirflowDag":
        return cls.updater(**kwargs)

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _airflow_dag_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "AirflowDag":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            AirflowDag instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _airflow_dag_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class AirflowDagAttributes(AssetAttributes):
    """AirflowDag-specific attributes for nested API format."""

    airflow_dag_schedule: Union[str, None, UnsetType] = UNSET
    """Schedule for the DAG."""

    airflow_dag_schedule_delta: Union[int, None, UnsetType] = UNSET
    """Duration between scheduled runs, in seconds."""

    airflow_tags: Union[list[str], None, UnsetType] = UNSET
    """Tags assigned to the asset in Airflow."""

    airflow_run_version: Union[str, None, UnsetType] = UNSET
    """Version of the run in Airflow."""

    airflow_run_open_lineage_version: Union[str, None, UnsetType] = UNSET
    """Version of the run in OpenLineage."""

    airflow_run_name: Union[str, None, UnsetType] = UNSET
    """Name of the run."""

    airflow_run_type: Union[str, None, UnsetType] = UNSET
    """Type of the run."""

    airflow_run_start_time: Union[int, None, UnsetType] = UNSET
    """Start time of the run."""

    airflow_run_end_time: Union[int, None, UnsetType] = UNSET
    """End time of the run."""

    airflow_run_open_lineage_state: Union[str, None, UnsetType] = UNSET
    """State of the run in OpenLineage."""


class AirflowDagRelationshipAttributes(AssetRelationshipAttributes):
    """AirflowDag-specific relationship attributes for nested API format."""

    airflow_tasks: Union[list[RelatedAirflowTask], None, UnsetType] = UNSET
    """Tasks that exist within this DAG."""


class AirflowDagNested(AssetNested):
    """AirflowDag in nested API format for high-performance serialization."""

    attributes: Union[AirflowDagAttributes, UnsetType] = UNSET
    relationship_attributes: Union[AirflowDagRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[
        AirflowDagRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        AirflowDagRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _airflow_dag_to_nested(airflow_dag: AirflowDag) -> AirflowDagNested:
    """Convert flat AirflowDag to nested format."""
    attrs_kwargs = build_attributes_kwargs(airflow_dag, AirflowDagAttributes)
    attrs = AirflowDagAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["airflow_tasks"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        airflow_dag, rel_fields, AirflowDagRelationshipAttributes
    )
    return AirflowDagNested(
        guid=airflow_dag.guid,
        type_name=airflow_dag.type_name,
        status=airflow_dag.status,
        version=airflow_dag.version,
        create_time=airflow_dag.create_time,
        update_time=airflow_dag.update_time,
        created_by=airflow_dag.created_by,
        updated_by=airflow_dag.updated_by,
        classifications=airflow_dag.classifications,
        classification_names=airflow_dag.classification_names,
        meanings=airflow_dag.meanings,
        labels=airflow_dag.labels,
        business_attributes=airflow_dag.business_attributes,
        custom_attributes=airflow_dag.custom_attributes,
        pending_tasks=airflow_dag.pending_tasks,
        proxy=airflow_dag.proxy,
        is_incomplete=airflow_dag.is_incomplete,
        provenance_type=airflow_dag.provenance_type,
        home_id=airflow_dag.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _airflow_dag_from_nested(nested: AirflowDagNested) -> AirflowDag:
    """Convert nested format to flat AirflowDag."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else AirflowDagAttributes()
    )
    rel_fields: list[str] = ["airflow_tasks"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        AirflowDagRelationshipAttributes,
    )
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, AssetNested, AirflowDagAttributes
    )
    return AirflowDag(**kwargs)


def _airflow_dag_to_nested_bytes(airflow_dag: AirflowDag, serde: Serde) -> bytes:
    """Convert flat AirflowDag to nested JSON bytes."""
    return serde.encode(_airflow_dag_to_nested(airflow_dag))


def _airflow_dag_from_nested_bytes(data: bytes, serde: Serde) -> AirflowDag:
    """Convert nested JSON bytes to flat AirflowDag."""
    nested = serde.decode(data, AirflowDagNested)
    return _airflow_dag_from_nested(nested)
