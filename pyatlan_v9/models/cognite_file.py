# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
CogniteFile asset model with flattened inheritance.

This module provides:
- CogniteFile: Flat asset class (easy to use)
- CogniteFileAttributes: Nested attributes struct (extends AssetAttributes)
- CogniteFileNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .cognite_related import RelatedCogniteAsset

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class CogniteFile(Asset):
    """
    Instance of a Cognite file in Atlan.
    """

    # Override type_name with CogniteFile-specific default
    type_name: Union[str, UnsetType] = "CogniteFile"

    cognite_asset: Union[RelatedCogniteAsset, None, UnsetType] = UNSET
    """Asset in which this file exists."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _cognite_file_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "CogniteFile":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            CogniteFile instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _cognite_file_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class CogniteFileAttributes(AssetAttributes):
    """CogniteFile-specific attributes for nested API format."""

    pass


class CogniteFileRelationshipAttributes(AssetRelationshipAttributes):
    """CogniteFile-specific relationship attributes for nested API format."""

    cognite_asset: Union[RelatedCogniteAsset, None, UnsetType] = UNSET
    """Asset in which this file exists."""


class CogniteFileNested(AssetNested):
    """CogniteFile in nested API format for high-performance serialization."""

    attributes: Union[CogniteFileAttributes, UnsetType] = UNSET
    relationship_attributes: Union[CogniteFileRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[
        CogniteFileRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        CogniteFileRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _cognite_file_to_nested(cognite_file: CogniteFile) -> CogniteFileNested:
    """Convert flat CogniteFile to nested format."""
    attrs = CogniteFileAttributes()
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["cognite_asset"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        cognite_file, rel_fields, CogniteFileRelationshipAttributes
    )
    return CogniteFileNested(
        guid=cognite_file.guid,
        type_name=cognite_file.type_name,
        status=cognite_file.status,
        version=cognite_file.version,
        create_time=cognite_file.create_time,
        update_time=cognite_file.update_time,
        created_by=cognite_file.created_by,
        updated_by=cognite_file.updated_by,
        classifications=cognite_file.classifications,
        classification_names=cognite_file.classification_names,
        meanings=cognite_file.meanings,
        labels=cognite_file.labels,
        business_attributes=cognite_file.business_attributes,
        custom_attributes=cognite_file.custom_attributes,
        pending_tasks=cognite_file.pending_tasks,
        proxy=cognite_file.proxy,
        is_incomplete=cognite_file.is_incomplete,
        provenance_type=cognite_file.provenance_type,
        home_id=cognite_file.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _cognite_file_from_nested(nested: CogniteFileNested) -> CogniteFile:
    """Convert nested format to flat CogniteFile."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else CogniteFileAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["cognite_asset"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        CogniteFileRelationshipAttributes,
    )
    return CogniteFile(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _cognite_file_to_nested_bytes(cognite_file: CogniteFile, serde: Serde) -> bytes:
    """Convert flat CogniteFile to nested JSON bytes."""
    return serde.encode(_cognite_file_to_nested(cognite_file))


def _cognite_file_from_nested_bytes(data: bytes, serde: Serde) -> CogniteFile:
    """Convert nested JSON bytes to flat CogniteFile."""
    nested = serde.decode(data, CogniteFileNested)
    return _cognite_file_from_nested(nested)
