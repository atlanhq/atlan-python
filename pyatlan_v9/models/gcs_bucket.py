# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
GCSBucket asset model with flattened inheritance.

This module provides:
- GCSBucket: Flat asset class (easy to use)
- GCSBucketAttributes: Nested attributes struct (extends AssetAttributes)
- GCSBucketNested: Nested API format struct
"""

from __future__ import annotations

from typing import Any, Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .gcs_related import RelatedGCSObject

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class GCSBucket(Asset):
    """
    Instance of a Google Cloud Storage bucket in Atlan.
    """

    # Override type_name with GCSBucket-specific default
    type_name: Union[str, UnsetType] = "GCSBucket"

    gcs_object_count: Union[int, None, UnsetType] = UNSET
    """Number of objects within the bucket."""

    gcs_bucket_versioning_enabled: Union[bool, None, UnsetType] = UNSET
    """Whether versioning is enabled on the bucket (true) or not (false)."""

    gcs_bucket_retention_locked: Union[bool, None, UnsetType] = UNSET
    """Whether retention is locked for this bucket (true) or not (false)."""

    gcs_bucket_retention_period: Union[int, None, UnsetType] = UNSET
    """Retention period for objects in this bucket."""

    gcs_bucket_retention_effective_time: Union[int, None, UnsetType] = UNSET
    """Effective time for retention of objects in this bucket."""

    gcs_bucket_lifecycle_rules: Union[str, None, UnsetType] = UNSET
    """Lifecycle rules for this bucket."""

    gcs_bucket_retention_policy: Union[str, None, UnsetType] = UNSET
    """Retention policy for this bucket."""

    gcs_storage_class: Union[str, None, UnsetType] = UNSET
    """Storage class of this asset."""

    gcs_encryption_type: Union[str, None, UnsetType] = UNSET
    """Encryption algorithm used to encrypt this asset."""

    gcs_e_tag: Union[str, None, UnsetType] = UNSET
    """Entity tag for the asset. An entity tag is a hash of the object and represents changes to the contents of an object only, not its metadata."""

    gcs_requester_pays: Union[bool, None, UnsetType] = UNSET
    """Whether the requester pays header was sent when this asset was created (true) or not (false)."""

    gcs_access_control: Union[str, None, UnsetType] = UNSET
    """Access control list for this asset."""

    gcs_meta_generation_id: Union[int, None, UnsetType] = UNSET
    """Version of metadata for this asset at this generation. Used for preconditions and detecting changes in metadata. A metageneration number is only meaningful in the context of a particular generation of a particular asset."""

    google_service: Union[str, None, UnsetType] = UNSET
    """Service in Google in which the asset exists."""

    google_project_name: Union[str, None, UnsetType] = UNSET
    """Name of the project in which the asset exists."""

    google_project_id: Union[str, None, UnsetType] = UNSET
    """ID of the project in which the asset exists."""

    cloud_project_number: Union[int, None, UnsetType] = UNSET
    """Number of the project in which the asset exists."""

    google_location: Union[str, None, UnsetType] = UNSET
    """Location of this asset in Google."""

    google_location_type: Union[str, None, UnsetType] = UNSET
    """Type of location of this asset in Google."""

    google_labels: Union[list[dict[str, Any]], None, UnsetType] = UNSET
    """List of labels that have been applied to the asset in Google."""

    google_tags: Union[list[dict[str, Any]], None, UnsetType] = UNSET
    """List of tags that have been applied to the asset in Google."""

    gcs_objects: Union[list[RelatedGCSObject], None, UnsetType] = UNSET
    """GCS objects within this bucket."""

    # =========================================================================
    # Creator Methods
    # =========================================================================

    @classmethod
    @init_guid
    def creator(cls, *, name: str, connection_qualified_name: str) -> "GCSBucket":
        """
        Create a new GCSBucket asset.

        Args:
            name: Name of the bucket
            connection_qualified_name: Unique name of the connection

        Returns:
            GCSBucket instance ready to be created

        Raises:
            ValueError: If required parameters are missing
        """
        validate_required_fields(
            ["name", "connection_qualified_name"], [name, connection_qualified_name]
        )
        # Extract connector name from the connection_qualified_name
        connector_name = connection_qualified_name.split("/")[1]
        return cls(
            name=name,
            qualified_name=f"{connection_qualified_name}/{name}",
            connection_qualified_name=connection_qualified_name,
            connector_name=connector_name,
        )

    @classmethod
    def updater(cls, *, qualified_name: str, name: str) -> "GCSBucket":
        """
        Create a GCSBucket instance for modification.

        Args:
            qualified_name: Unique name of the GCSBucket to update
            name: Human-readable name of the GCSBucket

        Returns:
            GCSBucket instance ready for update

        Raises:
            ValueError: If required parameters are missing
        """
        validate_required_fields(["qualified_name", "name"], [qualified_name, name])
        return cls(qualified_name=qualified_name, name=name)

    def trim_to_required(self) -> "GCSBucket":
        """
        Return a copy of this GCSBucket with only the minimum required fields for update.

        Returns:
            GCSBucket with only qualified_name and name set
        """
        return GCSBucket.updater(qualified_name=self.qualified_name, name=self.name)

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _gcs_bucket_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "GCSBucket":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            GCSBucket instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _gcs_bucket_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class GCSBucketAttributes(AssetAttributes):
    """GCSBucket-specific attributes for nested API format."""

    gcs_object_count: Union[int, None, UnsetType] = UNSET
    """Number of objects within the bucket."""

    gcs_bucket_versioning_enabled: Union[bool, None, UnsetType] = UNSET
    """Whether versioning is enabled on the bucket (true) or not (false)."""

    gcs_bucket_retention_locked: Union[bool, None, UnsetType] = UNSET
    """Whether retention is locked for this bucket (true) or not (false)."""

    gcs_bucket_retention_period: Union[int, None, UnsetType] = UNSET
    """Retention period for objects in this bucket."""

    gcs_bucket_retention_effective_time: Union[int, None, UnsetType] = UNSET
    """Effective time for retention of objects in this bucket."""

    gcs_bucket_lifecycle_rules: Union[str, None, UnsetType] = UNSET
    """Lifecycle rules for this bucket."""

    gcs_bucket_retention_policy: Union[str, None, UnsetType] = UNSET
    """Retention policy for this bucket."""

    gcs_storage_class: Union[str, None, UnsetType] = UNSET
    """Storage class of this asset."""

    gcs_encryption_type: Union[str, None, UnsetType] = UNSET
    """Encryption algorithm used to encrypt this asset."""

    gcs_e_tag: Union[str, None, UnsetType] = UNSET
    """Entity tag for the asset. An entity tag is a hash of the object and represents changes to the contents of an object only, not its metadata."""

    gcs_requester_pays: Union[bool, None, UnsetType] = UNSET
    """Whether the requester pays header was sent when this asset was created (true) or not (false)."""

    gcs_access_control: Union[str, None, UnsetType] = UNSET
    """Access control list for this asset."""

    gcs_meta_generation_id: Union[int, None, UnsetType] = UNSET
    """Version of metadata for this asset at this generation. Used for preconditions and detecting changes in metadata. A metageneration number is only meaningful in the context of a particular generation of a particular asset."""

    google_service: Union[str, None, UnsetType] = UNSET
    """Service in Google in which the asset exists."""

    google_project_name: Union[str, None, UnsetType] = UNSET
    """Name of the project in which the asset exists."""

    google_project_id: Union[str, None, UnsetType] = UNSET
    """ID of the project in which the asset exists."""

    cloud_project_number: Union[int, None, UnsetType] = UNSET
    """Number of the project in which the asset exists."""

    google_location: Union[str, None, UnsetType] = UNSET
    """Location of this asset in Google."""

    google_location_type: Union[str, None, UnsetType] = UNSET
    """Type of location of this asset in Google."""

    google_labels: Union[list[dict[str, Any]], None, UnsetType] = UNSET
    """List of labels that have been applied to the asset in Google."""

    google_tags: Union[list[dict[str, Any]], None, UnsetType] = UNSET
    """List of tags that have been applied to the asset in Google."""


class GCSBucketRelationshipAttributes(AssetRelationshipAttributes):
    """GCSBucket-specific relationship attributes for nested API format."""

    gcs_objects: Union[list[RelatedGCSObject], None, UnsetType] = UNSET
    """GCS objects within this bucket."""


class GCSBucketNested(AssetNested):
    """GCSBucket in nested API format for high-performance serialization."""

    attributes: Union[GCSBucketAttributes, UnsetType] = UNSET
    relationship_attributes: Union[GCSBucketRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[
        GCSBucketRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        GCSBucketRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _gcs_bucket_to_nested(gcs_bucket: GCSBucket) -> GCSBucketNested:
    """Convert flat GCSBucket to nested format."""
    attrs_kwargs = build_attributes_kwargs(gcs_bucket, GCSBucketAttributes)
    attrs = GCSBucketAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["gcs_objects"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        gcs_bucket, rel_fields, GCSBucketRelationshipAttributes
    )
    return GCSBucketNested(
        guid=gcs_bucket.guid,
        type_name=gcs_bucket.type_name,
        status=gcs_bucket.status,
        version=gcs_bucket.version,
        create_time=gcs_bucket.create_time,
        update_time=gcs_bucket.update_time,
        created_by=gcs_bucket.created_by,
        updated_by=gcs_bucket.updated_by,
        classifications=gcs_bucket.classifications,
        classification_names=gcs_bucket.classification_names,
        meanings=gcs_bucket.meanings,
        labels=gcs_bucket.labels,
        business_attributes=gcs_bucket.business_attributes,
        custom_attributes=gcs_bucket.custom_attributes,
        pending_tasks=gcs_bucket.pending_tasks,
        proxy=gcs_bucket.proxy,
        is_incomplete=gcs_bucket.is_incomplete,
        provenance_type=gcs_bucket.provenance_type,
        home_id=gcs_bucket.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _gcs_bucket_from_nested(nested: GCSBucketNested) -> GCSBucket:
    """Convert nested format to flat GCSBucket."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else GCSBucketAttributes()
    )
    rel_fields: list[str] = ["gcs_objects"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        GCSBucketRelationshipAttributes,
    )
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, AssetNested, GCSBucketAttributes
    )
    return GCSBucket(**kwargs)


def _gcs_bucket_to_nested_bytes(gcs_bucket: GCSBucket, serde: Serde) -> bytes:
    """Convert flat GCSBucket to nested JSON bytes."""
    return serde.encode(_gcs_bucket_to_nested(gcs_bucket))


def _gcs_bucket_from_nested_bytes(data: bytes, serde: Serde) -> GCSBucket:
    """Convert nested JSON bytes to flat GCSBucket."""
    nested = serde.decode(data, GCSBucketNested)
    return _gcs_bucket_from_nested(nested)
