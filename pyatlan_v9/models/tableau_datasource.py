# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
TableauDatasource asset model with flattened inheritance.

This module provides:
- TableauDatasource: Flat asset class (easy to use)
- TableauDatasourceAttributes: Nested attributes struct (extends AssetAttributes)
- TableauDatasourceNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .tableau_related import (
    RelatedTableauDatasourceField,
    RelatedTableauProject,
    RelatedTableauWorkbook,
)

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class TableauDatasource(Asset):
    """
    Instance of a Tableau datasource in Atlan. These include both published and embedded datasources, and are the link between source data and Tableau.
    """

    # Override type_name with TableauDatasource-specific default
    type_name: Union[str, UnsetType] = "TableauDatasource"

    site_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the site in which this datasource exists."""

    project_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the project in which this datasource exists."""

    top_level_project_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the top-level project in which this datasource exists."""

    workbook_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the workbook in which this datasource exists."""

    project_hierarchy: Union[list[dict[str, str]], None, UnsetType] = UNSET
    """List of top-level projects with their nested child projects."""

    is_published: Union[bool, None, UnsetType] = UNSET
    """Whether this datasource is published (true) or embedded (false)."""

    has_extracts: Union[bool, None, UnsetType] = UNSET
    """Whether this datasource has extracts (true) or not (false)."""

    is_certified: Union[bool, None, UnsetType] = UNSET
    """Whether this datasource is certified in Tableau (true) or not (false)."""

    certifier: Union[dict[str, str], None, UnsetType] = UNSET
    """Users that have marked this datasource as cerified, in Tableau."""

    certification_note: Union[str, None, UnsetType] = UNSET
    """Notes related to this datasource being cerfified, in Tableau."""

    certifier_display_name: Union[str, None, UnsetType] = UNSET
    """Name of the user who cerified this datasource, in Tableau."""

    upstream_tables: Union[list[dict[str, str]], None, UnsetType] = UNSET
    """List of tables that are upstream of this datasource."""

    upstream_datasources: Union[list[dict[str, str]], None, UnsetType] = UNSET
    """List of datasources that are upstream of this datasource."""

    tableau_project_hierarchy_qualified_names: Union[list[str], None, UnsetType] = UNSET
    """Array of qualified names representing the project hierarchy for this Tableau asset."""

    project: Union[RelatedTableauProject, None, UnsetType] = UNSET
    """Project in which this datasource exists."""

    workbook: Union[RelatedTableauWorkbook, None, UnsetType] = UNSET
    """Workbook in which this datasource exists."""

    fields: Union[list[RelatedTableauDatasourceField], None, UnsetType] = UNSET
    """Fields that exist within this datasource."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _tableau_datasource_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "TableauDatasource":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            TableauDatasource instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _tableau_datasource_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class TableauDatasourceAttributes(AssetAttributes):
    """TableauDatasource-specific attributes for nested API format."""

    site_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the site in which this datasource exists."""

    project_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the project in which this datasource exists."""

    top_level_project_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the top-level project in which this datasource exists."""

    workbook_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the workbook in which this datasource exists."""

    project_hierarchy: Union[list[dict[str, str]], None, UnsetType] = UNSET
    """List of top-level projects with their nested child projects."""

    is_published: Union[bool, None, UnsetType] = UNSET
    """Whether this datasource is published (true) or embedded (false)."""

    has_extracts: Union[bool, None, UnsetType] = UNSET
    """Whether this datasource has extracts (true) or not (false)."""

    is_certified: Union[bool, None, UnsetType] = UNSET
    """Whether this datasource is certified in Tableau (true) or not (false)."""

    certifier: Union[dict[str, str], None, UnsetType] = UNSET
    """Users that have marked this datasource as cerified, in Tableau."""

    certification_note: Union[str, None, UnsetType] = UNSET
    """Notes related to this datasource being cerfified, in Tableau."""

    certifier_display_name: Union[str, None, UnsetType] = UNSET
    """Name of the user who cerified this datasource, in Tableau."""

    upstream_tables: Union[list[dict[str, str]], None, UnsetType] = UNSET
    """List of tables that are upstream of this datasource."""

    upstream_datasources: Union[list[dict[str, str]], None, UnsetType] = UNSET
    """List of datasources that are upstream of this datasource."""

    tableau_project_hierarchy_qualified_names: Union[list[str], None, UnsetType] = UNSET
    """Array of qualified names representing the project hierarchy for this Tableau asset."""


class TableauDatasourceRelationshipAttributes(AssetRelationshipAttributes):
    """TableauDatasource-specific relationship attributes for nested API format."""

    project: Union[RelatedTableauProject, None, UnsetType] = UNSET
    """Project in which this datasource exists."""

    workbook: Union[RelatedTableauWorkbook, None, UnsetType] = UNSET
    """Workbook in which this datasource exists."""

    fields: Union[list[RelatedTableauDatasourceField], None, UnsetType] = UNSET
    """Fields that exist within this datasource."""


class TableauDatasourceNested(AssetNested):
    """TableauDatasource in nested API format for high-performance serialization."""

    attributes: Union[TableauDatasourceAttributes, UnsetType] = UNSET
    relationship_attributes: Union[
        TableauDatasourceRelationshipAttributes, UnsetType
    ] = UNSET
    append_relationship_attributes: Union[
        TableauDatasourceRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        TableauDatasourceRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _tableau_datasource_to_nested(
    tableau_datasource: TableauDatasource,
) -> TableauDatasourceNested:
    """Convert flat TableauDatasource to nested format."""
    attrs = TableauDatasourceAttributes(
        site_qualified_name=tableau_datasource.site_qualified_name,
        project_qualified_name=tableau_datasource.project_qualified_name,
        top_level_project_qualified_name=tableau_datasource.top_level_project_qualified_name,
        workbook_qualified_name=tableau_datasource.workbook_qualified_name,
        project_hierarchy=tableau_datasource.project_hierarchy,
        is_published=tableau_datasource.is_published,
        has_extracts=tableau_datasource.has_extracts,
        is_certified=tableau_datasource.is_certified,
        certifier=tableau_datasource.certifier,
        certification_note=tableau_datasource.certification_note,
        certifier_display_name=tableau_datasource.certifier_display_name,
        upstream_tables=tableau_datasource.upstream_tables,
        upstream_datasources=tableau_datasource.upstream_datasources,
        tableau_project_hierarchy_qualified_names=tableau_datasource.tableau_project_hierarchy_qualified_names,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["project", "workbook", "fields"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        tableau_datasource, rel_fields, TableauDatasourceRelationshipAttributes
    )
    return TableauDatasourceNested(
        guid=tableau_datasource.guid,
        type_name=tableau_datasource.type_name,
        status=tableau_datasource.status,
        version=tableau_datasource.version,
        create_time=tableau_datasource.create_time,
        update_time=tableau_datasource.update_time,
        created_by=tableau_datasource.created_by,
        updated_by=tableau_datasource.updated_by,
        classifications=tableau_datasource.classifications,
        classification_names=tableau_datasource.classification_names,
        meanings=tableau_datasource.meanings,
        labels=tableau_datasource.labels,
        business_attributes=tableau_datasource.business_attributes,
        custom_attributes=tableau_datasource.custom_attributes,
        pending_tasks=tableau_datasource.pending_tasks,
        proxy=tableau_datasource.proxy,
        is_incomplete=tableau_datasource.is_incomplete,
        provenance_type=tableau_datasource.provenance_type,
        home_id=tableau_datasource.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _tableau_datasource_from_nested(
    nested: TableauDatasourceNested,
) -> TableauDatasource:
    """Convert nested format to flat TableauDatasource."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else TableauDatasourceAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["project", "workbook", "fields"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        TableauDatasourceRelationshipAttributes,
    )
    return TableauDatasource(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        site_qualified_name=attrs.site_qualified_name,
        project_qualified_name=attrs.project_qualified_name,
        top_level_project_qualified_name=attrs.top_level_project_qualified_name,
        workbook_qualified_name=attrs.workbook_qualified_name,
        project_hierarchy=attrs.project_hierarchy,
        is_published=attrs.is_published,
        has_extracts=attrs.has_extracts,
        is_certified=attrs.is_certified,
        certifier=attrs.certifier,
        certification_note=attrs.certification_note,
        certifier_display_name=attrs.certifier_display_name,
        upstream_tables=attrs.upstream_tables,
        upstream_datasources=attrs.upstream_datasources,
        tableau_project_hierarchy_qualified_names=attrs.tableau_project_hierarchy_qualified_names,
        # Merged relationship attributes
        **merged_rels,
    )


def _tableau_datasource_to_nested_bytes(
    tableau_datasource: TableauDatasource, serde: Serde
) -> bytes:
    """Convert flat TableauDatasource to nested JSON bytes."""
    return serde.encode(_tableau_datasource_to_nested(tableau_datasource))


def _tableau_datasource_from_nested_bytes(
    data: bytes, serde: Serde
) -> TableauDatasource:
    """Convert nested JSON bytes to flat TableauDatasource."""
    nested = serde.decode(data, TableauDatasourceNested)
    return _tableau_datasource_from_nested(nested)
