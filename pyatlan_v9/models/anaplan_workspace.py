# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
AnaplanWorkspace asset model with flattened inheritance.

This module provides:
- AnaplanWorkspace: Flat asset class (easy to use)
- AnaplanWorkspaceAttributes: Nested attributes struct (extends AssetAttributes)
- AnaplanWorkspaceNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .anaplan_related import RelatedAnaplanModel
from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class AnaplanWorkspace(Asset):
    """
    Instances of an AnaplanWorkspace in Atlan.
    """

    # Override type_name with AnaplanWorkspace-specific default
    type_name: Union[str, UnsetType] = "AnaplanWorkspace"

    anaplan_workspace_current_size: Union[int, None, UnsetType] = UNSET
    """Current size of the AnaplanWorkspace from the source system, estimated in MB."""

    anaplan_workspace_allowance_size: Union[int, None, UnsetType] = UNSET
    """Alloted size quota for the AnaplanWorkspace from the source system, estimated in MB."""

    anaplan_workspace_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AnaplanWorkspace asset that contains this asset (AnaplanModel and everything under its hierarchy)."""

    anaplan_workspace_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the AnaplanWorkspace asset that contains this asset (AnaplanModel and everything under its hierarchy)."""

    anaplan_model_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AnaplanModel asset that contains this asset (AnaplanModule and everything under its hierarchy)."""

    anaplan_model_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the AnaplanModel asset that contains this asset (AnaplanModule and everything under its hierarchy)."""

    anaplan_module_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AnaplanModule asset that contains this asset (AnaplanLineItem, AnaplanList, AnaplanView and everything under their hierarchy)."""

    anaplan_module_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the AnaplanModule asset that contains this asset (AnaplanLineItem, AnaplanList, AnaplanView and everything under their hierarchy)."""

    anaplan_source_id: Union[str, None, UnsetType] = UNSET
    """Id/Guid of the Anaplan asset in the source system."""

    anaplan_models: Union[list[RelatedAnaplanModel], None, UnsetType] = UNSET
    """Individual models contained in the workspace."""

    @classmethod
    @init_guid
    def creator(
        cls, *, name: str, connection_qualified_name: str
    ) -> "AnaplanWorkspace":
        """Create a new AnaplanWorkspace asset."""
        validate_required_fields(
            ["name", "connection_qualified_name"], [name, connection_qualified_name]
        )
        fields = connection_qualified_name.split("/")
        connector_name = fields[1] if len(fields) > 1 else None
        return cls(
            name=name,
            qualified_name=f"{connection_qualified_name}/{name}",
            connection_qualified_name=connection_qualified_name,
            connector_name=connector_name,
        )

    @classmethod
    def updater(cls, *, qualified_name: str, name: str) -> "AnaplanWorkspace":
        """Create an AnaplanWorkspace instance for update operations."""
        validate_required_fields(["qualified_name", "name"], [qualified_name, name])
        return cls(qualified_name=qualified_name, name=name)

    def trim_to_required(self) -> "AnaplanWorkspace":
        """Return only fields required for update operations."""
        return AnaplanWorkspace.updater(
            qualified_name=self.qualified_name, name=self.name
        )

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _anaplan_workspace_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "AnaplanWorkspace":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            AnaplanWorkspace instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _anaplan_workspace_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class AnaplanWorkspaceAttributes(AssetAttributes):
    """AnaplanWorkspace-specific attributes for nested API format."""

    anaplan_workspace_current_size: Union[int, None, UnsetType] = UNSET
    """Current size of the AnaplanWorkspace from the source system, estimated in MB."""

    anaplan_workspace_allowance_size: Union[int, None, UnsetType] = UNSET
    """Alloted size quota for the AnaplanWorkspace from the source system, estimated in MB."""

    anaplan_workspace_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AnaplanWorkspace asset that contains this asset (AnaplanModel and everything under its hierarchy)."""

    anaplan_workspace_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the AnaplanWorkspace asset that contains this asset (AnaplanModel and everything under its hierarchy)."""

    anaplan_model_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AnaplanModel asset that contains this asset (AnaplanModule and everything under its hierarchy)."""

    anaplan_model_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the AnaplanModel asset that contains this asset (AnaplanModule and everything under its hierarchy)."""

    anaplan_module_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AnaplanModule asset that contains this asset (AnaplanLineItem, AnaplanList, AnaplanView and everything under their hierarchy)."""

    anaplan_module_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the AnaplanModule asset that contains this asset (AnaplanLineItem, AnaplanList, AnaplanView and everything under their hierarchy)."""

    anaplan_source_id: Union[str, None, UnsetType] = UNSET
    """Id/Guid of the Anaplan asset in the source system."""


class AnaplanWorkspaceRelationshipAttributes(AssetRelationshipAttributes):
    """AnaplanWorkspace-specific relationship attributes for nested API format."""

    anaplan_models: Union[list[RelatedAnaplanModel], None, UnsetType] = UNSET
    """Individual models contained in the workspace."""


class AnaplanWorkspaceNested(AssetNested):
    """AnaplanWorkspace in nested API format for high-performance serialization."""

    attributes: Union[AnaplanWorkspaceAttributes, UnsetType] = UNSET
    relationship_attributes: Union[
        AnaplanWorkspaceRelationshipAttributes, UnsetType
    ] = UNSET
    append_relationship_attributes: Union[
        AnaplanWorkspaceRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        AnaplanWorkspaceRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _anaplan_workspace_to_nested(
    anaplan_workspace: AnaplanWorkspace,
) -> AnaplanWorkspaceNested:
    """Convert flat AnaplanWorkspace to nested format."""
    attrs = AnaplanWorkspaceAttributes(
        anaplan_workspace_current_size=anaplan_workspace.anaplan_workspace_current_size,
        anaplan_workspace_allowance_size=anaplan_workspace.anaplan_workspace_allowance_size,
        anaplan_workspace_qualified_name=anaplan_workspace.anaplan_workspace_qualified_name,
        anaplan_workspace_name=anaplan_workspace.anaplan_workspace_name,
        anaplan_model_qualified_name=anaplan_workspace.anaplan_model_qualified_name,
        anaplan_model_name=anaplan_workspace.anaplan_model_name,
        anaplan_module_qualified_name=anaplan_workspace.anaplan_module_qualified_name,
        anaplan_module_name=anaplan_workspace.anaplan_module_name,
        anaplan_source_id=anaplan_workspace.anaplan_source_id,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["anaplan_models"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        anaplan_workspace, rel_fields, AnaplanWorkspaceRelationshipAttributes
    )
    return AnaplanWorkspaceNested(
        guid=anaplan_workspace.guid,
        type_name=anaplan_workspace.type_name,
        status=anaplan_workspace.status,
        version=anaplan_workspace.version,
        create_time=anaplan_workspace.create_time,
        update_time=anaplan_workspace.update_time,
        created_by=anaplan_workspace.created_by,
        updated_by=anaplan_workspace.updated_by,
        classifications=anaplan_workspace.classifications,
        classification_names=anaplan_workspace.classification_names,
        meanings=anaplan_workspace.meanings,
        labels=anaplan_workspace.labels,
        business_attributes=anaplan_workspace.business_attributes,
        custom_attributes=anaplan_workspace.custom_attributes,
        pending_tasks=anaplan_workspace.pending_tasks,
        proxy=anaplan_workspace.proxy,
        is_incomplete=anaplan_workspace.is_incomplete,
        provenance_type=anaplan_workspace.provenance_type,
        home_id=anaplan_workspace.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _anaplan_workspace_from_nested(nested: AnaplanWorkspaceNested) -> AnaplanWorkspace:
    """Convert nested format to flat AnaplanWorkspace."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else AnaplanWorkspaceAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["anaplan_models"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        AnaplanWorkspaceRelationshipAttributes,
    )
    return AnaplanWorkspace(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        anaplan_workspace_current_size=attrs.anaplan_workspace_current_size,
        anaplan_workspace_allowance_size=attrs.anaplan_workspace_allowance_size,
        anaplan_workspace_qualified_name=attrs.anaplan_workspace_qualified_name,
        anaplan_workspace_name=attrs.anaplan_workspace_name,
        anaplan_model_qualified_name=attrs.anaplan_model_qualified_name,
        anaplan_model_name=attrs.anaplan_model_name,
        anaplan_module_qualified_name=attrs.anaplan_module_qualified_name,
        anaplan_module_name=attrs.anaplan_module_name,
        anaplan_source_id=attrs.anaplan_source_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _anaplan_workspace_to_nested_bytes(
    anaplan_workspace: AnaplanWorkspace, serde: Serde
) -> bytes:
    """Convert flat AnaplanWorkspace to nested JSON bytes."""
    return serde.encode(_anaplan_workspace_to_nested(anaplan_workspace))


def _anaplan_workspace_from_nested_bytes(data: bytes, serde: Serde) -> AnaplanWorkspace:
    """Convert nested JSON bytes to flat AnaplanWorkspace."""
    nested = serde.decode(data, AnaplanWorkspaceNested)
    return _anaplan_workspace_from_nested(nested)
