# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
SigmaWorkbook asset model with flattened inheritance.

This module provides:
- SigmaWorkbook: Flat asset class (easy to use)
- SigmaWorkbookAttributes: Nested attributes struct (extends AssetAttributes)
- SigmaWorkbookNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .sigma_related import RelatedSigmaPage

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class SigmaWorkbook(Asset):
    """
    Instance of a Sigma workbook in Atlan.
    """

    # Override type_name with SigmaWorkbook-specific default
    type_name: Union[str, UnsetType] = "SigmaWorkbook"

    sigma_page_count: Union[int, None, UnsetType] = UNSET
    """Number of pages in this workbook."""

    sigma_workbook_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the workbook in which this asset exists."""

    sigma_workbook_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the workbook in which this asset exists."""

    sigma_page_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the page on which this asset exists."""

    sigma_page_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the page on which this asset exists."""

    sigma_data_element_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the data element in which this asset exists."""

    sigma_data_element_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the data element in which this asset exists."""

    sigma_pages: Union[list[RelatedSigmaPage], None, UnsetType] = UNSET
    """Pages that exist in this workbook."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _sigma_workbook_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "SigmaWorkbook":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            SigmaWorkbook instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _sigma_workbook_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class SigmaWorkbookAttributes(AssetAttributes):
    """SigmaWorkbook-specific attributes for nested API format."""

    sigma_page_count: Union[int, None, UnsetType] = UNSET
    """Number of pages in this workbook."""

    sigma_workbook_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the workbook in which this asset exists."""

    sigma_workbook_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the workbook in which this asset exists."""

    sigma_page_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the page on which this asset exists."""

    sigma_page_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the page on which this asset exists."""

    sigma_data_element_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the data element in which this asset exists."""

    sigma_data_element_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the data element in which this asset exists."""


class SigmaWorkbookRelationshipAttributes(AssetRelationshipAttributes):
    """SigmaWorkbook-specific relationship attributes for nested API format."""

    sigma_pages: Union[list[RelatedSigmaPage], None, UnsetType] = UNSET
    """Pages that exist in this workbook."""


class SigmaWorkbookNested(AssetNested):
    """SigmaWorkbook in nested API format for high-performance serialization."""

    attributes: Union[SigmaWorkbookAttributes, UnsetType] = UNSET
    relationship_attributes: Union[SigmaWorkbookRelationshipAttributes, UnsetType] = (
        UNSET
    )
    append_relationship_attributes: Union[
        SigmaWorkbookRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        SigmaWorkbookRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _sigma_workbook_to_nested(sigma_workbook: SigmaWorkbook) -> SigmaWorkbookNested:
    """Convert flat SigmaWorkbook to nested format."""
    attrs = SigmaWorkbookAttributes(
        sigma_page_count=sigma_workbook.sigma_page_count,
        sigma_workbook_qualified_name=sigma_workbook.sigma_workbook_qualified_name,
        sigma_workbook_name=sigma_workbook.sigma_workbook_name,
        sigma_page_qualified_name=sigma_workbook.sigma_page_qualified_name,
        sigma_page_name=sigma_workbook.sigma_page_name,
        sigma_data_element_qualified_name=sigma_workbook.sigma_data_element_qualified_name,
        sigma_data_element_name=sigma_workbook.sigma_data_element_name,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["sigma_pages"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        sigma_workbook, rel_fields, SigmaWorkbookRelationshipAttributes
    )
    return SigmaWorkbookNested(
        guid=sigma_workbook.guid,
        type_name=sigma_workbook.type_name,
        status=sigma_workbook.status,
        version=sigma_workbook.version,
        create_time=sigma_workbook.create_time,
        update_time=sigma_workbook.update_time,
        created_by=sigma_workbook.created_by,
        updated_by=sigma_workbook.updated_by,
        classifications=sigma_workbook.classifications,
        classification_names=sigma_workbook.classification_names,
        meanings=sigma_workbook.meanings,
        labels=sigma_workbook.labels,
        business_attributes=sigma_workbook.business_attributes,
        custom_attributes=sigma_workbook.custom_attributes,
        pending_tasks=sigma_workbook.pending_tasks,
        proxy=sigma_workbook.proxy,
        is_incomplete=sigma_workbook.is_incomplete,
        provenance_type=sigma_workbook.provenance_type,
        home_id=sigma_workbook.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _sigma_workbook_from_nested(nested: SigmaWorkbookNested) -> SigmaWorkbook:
    """Convert nested format to flat SigmaWorkbook."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else SigmaWorkbookAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["sigma_pages"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        SigmaWorkbookRelationshipAttributes,
    )
    return SigmaWorkbook(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        sigma_page_count=attrs.sigma_page_count,
        sigma_workbook_qualified_name=attrs.sigma_workbook_qualified_name,
        sigma_workbook_name=attrs.sigma_workbook_name,
        sigma_page_qualified_name=attrs.sigma_page_qualified_name,
        sigma_page_name=attrs.sigma_page_name,
        sigma_data_element_qualified_name=attrs.sigma_data_element_qualified_name,
        sigma_data_element_name=attrs.sigma_data_element_name,
        # Merged relationship attributes
        **merged_rels,
    )


def _sigma_workbook_to_nested_bytes(
    sigma_workbook: SigmaWorkbook, serde: Serde
) -> bytes:
    """Convert flat SigmaWorkbook to nested JSON bytes."""
    return serde.encode(_sigma_workbook_to_nested(sigma_workbook))


def _sigma_workbook_from_nested_bytes(data: bytes, serde: Serde) -> SigmaWorkbook:
    """Convert nested JSON bytes to flat SigmaWorkbook."""
    nested = serde.decode(data, SigmaWorkbookNested)
    return _sigma_workbook_from_nested(nested)
