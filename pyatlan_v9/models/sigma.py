# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Sigma asset model with flattened inheritance.

This module provides:
- Sigma: Flat asset class (easy to use)
- SigmaAttributes: Nested attributes struct (extends AssetAttributes)
- SigmaNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Sigma(Asset):
    """
    Base class for Sigma assets.
    """

    # Override type_name with Sigma-specific default
    type_name: Union[str, UnsetType] = "Sigma"

    sigma_workbook_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the workbook in which this asset exists."""

    sigma_workbook_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the workbook in which this asset exists."""

    sigma_page_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the page on which this asset exists."""

    sigma_page_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the page on which this asset exists."""

    sigma_data_element_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the data element in which this asset exists."""

    sigma_data_element_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the data element in which this asset exists."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _sigma_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(json_data: Union[str, bytes], serde: Serde | None = None) -> "Sigma":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Sigma instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _sigma_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class SigmaAttributes(AssetAttributes):
    """Sigma-specific attributes for nested API format."""

    sigma_workbook_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the workbook in which this asset exists."""

    sigma_workbook_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the workbook in which this asset exists."""

    sigma_page_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the page on which this asset exists."""

    sigma_page_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the page on which this asset exists."""

    sigma_data_element_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the data element in which this asset exists."""

    sigma_data_element_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the data element in which this asset exists."""


class SigmaRelationshipAttributes(AssetRelationshipAttributes):
    """Sigma-specific relationship attributes for nested API format."""

    pass


class SigmaNested(AssetNested):
    """Sigma in nested API format for high-performance serialization."""

    attributes: Union[SigmaAttributes, UnsetType] = UNSET
    relationship_attributes: Union[SigmaRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[SigmaRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[SigmaRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _sigma_to_nested(sigma: Sigma) -> SigmaNested:
    """Convert flat Sigma to nested format."""
    attrs = SigmaAttributes(
        sigma_workbook_qualified_name=sigma.sigma_workbook_qualified_name,
        sigma_workbook_name=sigma.sigma_workbook_name,
        sigma_page_qualified_name=sigma.sigma_page_qualified_name,
        sigma_page_name=sigma.sigma_page_name,
        sigma_data_element_qualified_name=sigma.sigma_data_element_qualified_name,
        sigma_data_element_name=sigma.sigma_data_element_name,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        sigma, rel_fields, SigmaRelationshipAttributes
    )
    return SigmaNested(
        guid=sigma.guid,
        type_name=sigma.type_name,
        status=sigma.status,
        version=sigma.version,
        create_time=sigma.create_time,
        update_time=sigma.update_time,
        created_by=sigma.created_by,
        updated_by=sigma.updated_by,
        classifications=sigma.classifications,
        classification_names=sigma.classification_names,
        meanings=sigma.meanings,
        labels=sigma.labels,
        business_attributes=sigma.business_attributes,
        custom_attributes=sigma.custom_attributes,
        pending_tasks=sigma.pending_tasks,
        proxy=sigma.proxy,
        is_incomplete=sigma.is_incomplete,
        provenance_type=sigma.provenance_type,
        home_id=sigma.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _sigma_from_nested(nested: SigmaNested) -> Sigma:
    """Convert nested format to flat Sigma."""
    attrs = nested.attributes if nested.attributes is not UNSET else SigmaAttributes()
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        SigmaRelationshipAttributes,
    )
    return Sigma(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        sigma_workbook_qualified_name=attrs.sigma_workbook_qualified_name,
        sigma_workbook_name=attrs.sigma_workbook_name,
        sigma_page_qualified_name=attrs.sigma_page_qualified_name,
        sigma_page_name=attrs.sigma_page_name,
        sigma_data_element_qualified_name=attrs.sigma_data_element_qualified_name,
        sigma_data_element_name=attrs.sigma_data_element_name,
        # Merged relationship attributes
        **merged_rels,
    )


def _sigma_to_nested_bytes(sigma: Sigma, serde: Serde) -> bytes:
    """Convert flat Sigma to nested JSON bytes."""
    return serde.encode(_sigma_to_nested(sigma))


def _sigma_from_nested_bytes(data: bytes, serde: Serde) -> Sigma:
    """Convert nested JSON bytes to flat Sigma."""
    nested = serde.decode(data, SigmaNested)
    return _sigma_from_nested(nested)
