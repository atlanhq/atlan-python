# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
SaaS asset model with flattened inheritance.

This module provides:
- SaaS: Flat asset class (easy to use)
- SaaSAttributes: Nested attributes struct (extends AssetAttributes)
- SaaSNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class SaaS(Asset):
    """
    Base class for SaaS application assets.
    """

    # Override type_name with SaaS-specific default
    type_name: Union[str, UnsetType] = "SaaS"

    pass

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _saa_s_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(json_data: Union[str, bytes], serde: Serde | None = None) -> "SaaS":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            SaaS instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _saa_s_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class SaaSAttributes(AssetAttributes):
    """SaaS-specific attributes for nested API format."""

    pass


class SaaSRelationshipAttributes(AssetRelationshipAttributes):
    """SaaS-specific relationship attributes for nested API format."""

    pass


class SaaSNested(AssetNested):
    """SaaS in nested API format for high-performance serialization."""

    attributes: Union[SaaSAttributes, UnsetType] = UNSET
    relationship_attributes: Union[SaaSRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[SaaSRelationshipAttributes, UnsetType] = UNSET
    remove_relationship_attributes: Union[SaaSRelationshipAttributes, UnsetType] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _saa_s_to_nested(saa_s: SaaS) -> SaaSNested:
    """Convert flat SaaS to nested format."""
    attrs = SaaSAttributes()
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        saa_s, rel_fields, SaaSRelationshipAttributes
    )
    return SaaSNested(
        guid=saa_s.guid,
        type_name=saa_s.type_name,
        status=saa_s.status,
        version=saa_s.version,
        create_time=saa_s.create_time,
        update_time=saa_s.update_time,
        created_by=saa_s.created_by,
        updated_by=saa_s.updated_by,
        classifications=saa_s.classifications,
        classification_names=saa_s.classification_names,
        meanings=saa_s.meanings,
        labels=saa_s.labels,
        business_attributes=saa_s.business_attributes,
        custom_attributes=saa_s.custom_attributes,
        pending_tasks=saa_s.pending_tasks,
        proxy=saa_s.proxy,
        is_incomplete=saa_s.is_incomplete,
        provenance_type=saa_s.provenance_type,
        home_id=saa_s.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _saa_s_from_nested(nested: SaaSNested) -> SaaS:
    """Convert nested format to flat SaaS."""
    attrs = nested.attributes if nested.attributes is not UNSET else SaaSAttributes()
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        SaaSRelationshipAttributes,
    )
    return SaaS(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _saa_s_to_nested_bytes(saa_s: SaaS, serde: Serde) -> bytes:
    """Convert flat SaaS to nested JSON bytes."""
    return serde.encode(_saa_s_to_nested(saa_s))


def _saa_s_from_nested_bytes(data: bytes, serde: Serde) -> SaaS:
    """Convert nested JSON bytes to flat SaaS."""
    nested = serde.decode(data, SaaSNested)
    return _saa_s_from_nested(nested)
