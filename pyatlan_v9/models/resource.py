# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Resource asset model with flattened inheritance.

This module provides:
- Resource: Flat asset class (easy to use)
- ResourceAttributes: Nested attributes struct (extends AssetAttributes)
- ResourceNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Resource(Asset):
    """
    Base class for resources.
    """

    # Override type_name with Resource-specific default
    type_name: Union[str, UnsetType] = "Resource"

    link: Union[str, None, UnsetType] = UNSET
    """URL to the resource."""

    is_global: Union[bool, None, UnsetType] = UNSET
    """Whether the resource is global (true) or not (false)."""

    reference: Union[str, None, UnsetType] = UNSET
    """Reference to the resource."""

    resource_metadata: Union[dict[str, str], None, UnsetType] = UNSET
    """Metadata of the resource."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _resource_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "Resource":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Resource instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _resource_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class ResourceAttributes(AssetAttributes):
    """Resource-specific attributes for nested API format."""

    link: Union[str, None, UnsetType] = UNSET
    """URL to the resource."""

    is_global: Union[bool, None, UnsetType] = UNSET
    """Whether the resource is global (true) or not (false)."""

    reference: Union[str, None, UnsetType] = UNSET
    """Reference to the resource."""

    resource_metadata: Union[dict[str, str], None, UnsetType] = UNSET
    """Metadata of the resource."""


class ResourceRelationshipAttributes(AssetRelationshipAttributes):
    """Resource-specific relationship attributes for nested API format."""

    pass


class ResourceNested(AssetNested):
    """Resource in nested API format for high-performance serialization."""

    attributes: Union[ResourceAttributes, UnsetType] = UNSET
    relationship_attributes: Union[ResourceRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[ResourceRelationshipAttributes, UnsetType] = (
        UNSET
    )
    remove_relationship_attributes: Union[ResourceRelationshipAttributes, UnsetType] = (
        UNSET
    )


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _resource_to_nested(resource: Resource) -> ResourceNested:
    """Convert flat Resource to nested format."""
    attrs = ResourceAttributes(
        link=resource.link,
        is_global=resource.is_global,
        reference=resource.reference,
        resource_metadata=resource.resource_metadata,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        resource, rel_fields, ResourceRelationshipAttributes
    )
    return ResourceNested(
        guid=resource.guid,
        type_name=resource.type_name,
        status=resource.status,
        version=resource.version,
        create_time=resource.create_time,
        update_time=resource.update_time,
        created_by=resource.created_by,
        updated_by=resource.updated_by,
        classifications=resource.classifications,
        classification_names=resource.classification_names,
        meanings=resource.meanings,
        labels=resource.labels,
        business_attributes=resource.business_attributes,
        custom_attributes=resource.custom_attributes,
        pending_tasks=resource.pending_tasks,
        proxy=resource.proxy,
        is_incomplete=resource.is_incomplete,
        provenance_type=resource.provenance_type,
        home_id=resource.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _resource_from_nested(nested: ResourceNested) -> Resource:
    """Convert nested format to flat Resource."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else ResourceAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        ResourceRelationshipAttributes,
    )
    return Resource(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        link=attrs.link,
        is_global=attrs.is_global,
        reference=attrs.reference,
        resource_metadata=attrs.resource_metadata,
        # Merged relationship attributes
        **merged_rels,
    )


def _resource_to_nested_bytes(resource: Resource, serde: Serde) -> bytes:
    """Convert flat Resource to nested JSON bytes."""
    return serde.encode(_resource_to_nested(resource))


def _resource_from_nested_bytes(data: bytes, serde: Serde) -> Resource:
    """Convert nested JSON bytes to flat Resource."""
    nested = serde.decode(data, ResourceNested)
    return _resource_from_nested(nested)
