# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
ObjectStore asset model with flattened inheritance.

This module provides:
- ObjectStore: Flat asset class (easy to use)
- ObjectStoreAttributes: Nested attributes struct (extends AssetAttributes)
- ObjectStoreNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class ObjectStore(Asset):
    """
    Base class for object store assets.
    """

    # Override type_name with ObjectStore-specific default
    type_name: Union[str, UnsetType] = "ObjectStore"

    pass

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _object_store_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "ObjectStore":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            ObjectStore instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _object_store_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class ObjectStoreAttributes(AssetAttributes):
    """ObjectStore-specific attributes for nested API format."""

    pass


class ObjectStoreRelationshipAttributes(AssetRelationshipAttributes):
    """ObjectStore-specific relationship attributes for nested API format."""

    pass


class ObjectStoreNested(AssetNested):
    """ObjectStore in nested API format for high-performance serialization."""

    attributes: Union[ObjectStoreAttributes, UnsetType] = UNSET
    relationship_attributes: Union[ObjectStoreRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[
        ObjectStoreRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        ObjectStoreRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _object_store_to_nested(object_store: ObjectStore) -> ObjectStoreNested:
    """Convert flat ObjectStore to nested format."""
    attrs = ObjectStoreAttributes()
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        object_store, rel_fields, ObjectStoreRelationshipAttributes
    )
    return ObjectStoreNested(
        guid=object_store.guid,
        type_name=object_store.type_name,
        status=object_store.status,
        version=object_store.version,
        create_time=object_store.create_time,
        update_time=object_store.update_time,
        created_by=object_store.created_by,
        updated_by=object_store.updated_by,
        classifications=object_store.classifications,
        classification_names=object_store.classification_names,
        meanings=object_store.meanings,
        labels=object_store.labels,
        business_attributes=object_store.business_attributes,
        custom_attributes=object_store.custom_attributes,
        pending_tasks=object_store.pending_tasks,
        proxy=object_store.proxy,
        is_incomplete=object_store.is_incomplete,
        provenance_type=object_store.provenance_type,
        home_id=object_store.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _object_store_from_nested(nested: ObjectStoreNested) -> ObjectStore:
    """Convert nested format to flat ObjectStore."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else ObjectStoreAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        ObjectStoreRelationshipAttributes,
    )
    return ObjectStore(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _object_store_to_nested_bytes(object_store: ObjectStore, serde: Serde) -> bytes:
    """Convert flat ObjectStore to nested JSON bytes."""
    return serde.encode(_object_store_to_nested(object_store))


def _object_store_from_nested_bytes(data: bytes, serde: Serde) -> ObjectStore:
    """Convert nested JSON bytes to flat ObjectStore."""
    nested = serde.decode(data, ObjectStoreNested)
    return _object_store_from_nested(nested)
