# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
SchemaRegistry asset model with flattened inheritance.

This module provides:
- SchemaRegistry: Flat asset class (easy to use)
- SchemaRegistryAttributes: Nested attributes struct (extends AssetAttributes)
- SchemaRegistryNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class SchemaRegistry(Asset):
    """
    Instance of a schema registry in Atlan.
    """

    # Override type_name with SchemaRegistry-specific default
    type_name: Union[str, UnsetType] = "SchemaRegistry"

    schema_registry_schema_type: Union[str, None, UnsetType] = UNSET
    """Type of language or specification used to define the schema, for example: JSON, Protobuf, etc."""

    schema_registry_schema_id: Union[str, None, UnsetType] = UNSET
    """Unique identifier for schema definition set by the schema registry."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _schema_registry_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "SchemaRegistry":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            SchemaRegistry instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _schema_registry_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class SchemaRegistryAttributes(AssetAttributes):
    """SchemaRegistry-specific attributes for nested API format."""

    schema_registry_schema_type: Union[str, None, UnsetType] = UNSET
    """Type of language or specification used to define the schema, for example: JSON, Protobuf, etc."""

    schema_registry_schema_id: Union[str, None, UnsetType] = UNSET
    """Unique identifier for schema definition set by the schema registry."""


class SchemaRegistryRelationshipAttributes(AssetRelationshipAttributes):
    """SchemaRegistry-specific relationship attributes for nested API format."""

    pass


class SchemaRegistryNested(AssetNested):
    """SchemaRegistry in nested API format for high-performance serialization."""

    attributes: Union[SchemaRegistryAttributes, UnsetType] = UNSET
    relationship_attributes: Union[SchemaRegistryRelationshipAttributes, UnsetType] = (
        UNSET
    )
    append_relationship_attributes: Union[
        SchemaRegistryRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        SchemaRegistryRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _schema_registry_to_nested(schema_registry: SchemaRegistry) -> SchemaRegistryNested:
    """Convert flat SchemaRegistry to nested format."""
    attrs = SchemaRegistryAttributes(
        schema_registry_schema_type=schema_registry.schema_registry_schema_type,
        schema_registry_schema_id=schema_registry.schema_registry_schema_id,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        schema_registry, rel_fields, SchemaRegistryRelationshipAttributes
    )
    return SchemaRegistryNested(
        guid=schema_registry.guid,
        type_name=schema_registry.type_name,
        status=schema_registry.status,
        version=schema_registry.version,
        create_time=schema_registry.create_time,
        update_time=schema_registry.update_time,
        created_by=schema_registry.created_by,
        updated_by=schema_registry.updated_by,
        classifications=schema_registry.classifications,
        classification_names=schema_registry.classification_names,
        meanings=schema_registry.meanings,
        labels=schema_registry.labels,
        business_attributes=schema_registry.business_attributes,
        custom_attributes=schema_registry.custom_attributes,
        pending_tasks=schema_registry.pending_tasks,
        proxy=schema_registry.proxy,
        is_incomplete=schema_registry.is_incomplete,
        provenance_type=schema_registry.provenance_type,
        home_id=schema_registry.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _schema_registry_from_nested(nested: SchemaRegistryNested) -> SchemaRegistry:
    """Convert nested format to flat SchemaRegistry."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else SchemaRegistryAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        SchemaRegistryRelationshipAttributes,
    )
    return SchemaRegistry(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        schema_registry_schema_type=attrs.schema_registry_schema_type,
        schema_registry_schema_id=attrs.schema_registry_schema_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _schema_registry_to_nested_bytes(
    schema_registry: SchemaRegistry, serde: Serde
) -> bytes:
    """Convert flat SchemaRegistry to nested JSON bytes."""
    return serde.encode(_schema_registry_to_nested(schema_registry))


def _schema_registry_from_nested_bytes(data: bytes, serde: Serde) -> SchemaRegistry:
    """Convert nested JSON bytes to flat SchemaRegistry."""
    nested = serde.decode(data, SchemaRegistryNested)
    return _schema_registry_from_nested(nested)
