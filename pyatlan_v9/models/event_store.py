# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
EventStore asset model with flattened inheritance.

This module provides:
- EventStore: Flat asset class (easy to use)
- EventStoreAttributes: Nested attributes struct (extends AssetAttributes)
- EventStoreNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class EventStore(Asset):
    """
    Base class for event store assets.
    """

    # Override type_name with EventStore-specific default
    type_name: Union[str, UnsetType] = "EventStore"

    pass

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _event_store_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "EventStore":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            EventStore instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _event_store_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class EventStoreAttributes(AssetAttributes):
    """EventStore-specific attributes for nested API format."""

    pass


class EventStoreRelationshipAttributes(AssetRelationshipAttributes):
    """EventStore-specific relationship attributes for nested API format."""

    pass


class EventStoreNested(AssetNested):
    """EventStore in nested API format for high-performance serialization."""

    attributes: Union[EventStoreAttributes, UnsetType] = UNSET
    relationship_attributes: Union[EventStoreRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[
        EventStoreRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        EventStoreRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _event_store_to_nested(event_store: EventStore) -> EventStoreNested:
    """Convert flat EventStore to nested format."""
    attrs = EventStoreAttributes()
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = []
    replace_rels, append_rels, remove_rels = categorize_relationships(
        event_store, rel_fields, EventStoreRelationshipAttributes
    )
    return EventStoreNested(
        guid=event_store.guid,
        type_name=event_store.type_name,
        status=event_store.status,
        version=event_store.version,
        create_time=event_store.create_time,
        update_time=event_store.update_time,
        created_by=event_store.created_by,
        updated_by=event_store.updated_by,
        classifications=event_store.classifications,
        classification_names=event_store.classification_names,
        meanings=event_store.meanings,
        labels=event_store.labels,
        business_attributes=event_store.business_attributes,
        custom_attributes=event_store.custom_attributes,
        pending_tasks=event_store.pending_tasks,
        proxy=event_store.proxy,
        is_incomplete=event_store.is_incomplete,
        provenance_type=event_store.provenance_type,
        home_id=event_store.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _event_store_from_nested(nested: EventStoreNested) -> EventStore:
    """Convert nested format to flat EventStore."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else EventStoreAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = []
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        EventStoreRelationshipAttributes,
    )
    return EventStore(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _event_store_to_nested_bytes(event_store: EventStore, serde: Serde) -> bytes:
    """Convert flat EventStore to nested JSON bytes."""
    return serde.encode(_event_store_to_nested(event_store))


def _event_store_from_nested_bytes(data: bytes, serde: Serde) -> EventStore:
    """Convert nested JSON bytes to flat EventStore."""
    nested = serde.decode(data, EventStoreNested)
    return _event_store_from_nested(nested)
