# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
PresetDataset asset model with flattened inheritance.

This module provides:
- PresetDataset: Flat asset class (easy to use)
- PresetDatasetAttributes: Nested attributes struct (extends AssetAttributes)
- PresetDatasetNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import (
    build_attributes_kwargs,
    build_flat_kwargs,
    categorize_relationships,
    merge_relationships,
)
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .preset_related import RelatedPresetDashboard

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class PresetDataset(Asset):
    """
    Instance of a Preset dataset in Atlan.
    """

    # Override type_name with PresetDataset-specific default
    type_name: Union[str, UnsetType] = "PresetDataset"

    preset_dataset_datasource_name: Union[str, None, UnsetType] = UNSET
    """"""

    preset_dataset_id: Union[int, None, UnsetType] = UNSET
    """"""

    preset_dataset_type: Union[str, None, UnsetType] = UNSET
    """"""

    preset_workspace_id: Union[int, None, UnsetType] = UNSET
    """Identifier of the workspace in which this asset exists, in Preset."""

    preset_workspace_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the workspace in which this asset exists."""

    preset_dashboard_id: Union[int, None, UnsetType] = UNSET
    """Identifier of the dashboard in which this asset exists, in Preset."""

    preset_dashboard_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the dashboard in which this asset exists."""

    preset_dashboard: Union[RelatedPresetDashboard, None, UnsetType] = UNSET
    """Dashboard in which this dataset exists."""

    # =========================================================================
    # Convenience Methods
    # =========================================================================

    @classmethod
    @init_guid
    def creator(
        cls,
        *,
        name: str,
        preset_dashboard_qualified_name: str,
        connection_qualified_name: str | None = None,
    ) -> "PresetDataset":
        validate_required_fields(
            ["name", "preset_dashboard_qualified_name"],
            [name, preset_dashboard_qualified_name],
        )
        fields = preset_dashboard_qualified_name.split("/")
        connector_name = fields[1] if len(fields) > 1 else None
        connection_qn = connection_qualified_name or (
            f"{fields[0]}/{fields[1]}/{fields[2]}" if len(fields) >= 3 else None
        )
        return cls(
            name=name,
            qualified_name=f"{preset_dashboard_qualified_name}/{name}",
            preset_dashboard_qualified_name=preset_dashboard_qualified_name,
            connection_qualified_name=connection_qn,
            connector_name=connector_name,
            preset_dashboard=RelatedPresetDashboard(
                qualified_name=preset_dashboard_qualified_name
            ),
        )

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _preset_dataset_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "PresetDataset":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            PresetDataset instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _preset_dataset_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class PresetDatasetAttributes(AssetAttributes):
    """PresetDataset-specific attributes for nested API format."""

    preset_dataset_datasource_name: Union[str, None, UnsetType] = UNSET
    """"""

    preset_dataset_id: Union[int, None, UnsetType] = UNSET
    """"""

    preset_dataset_type: Union[str, None, UnsetType] = UNSET
    """"""

    preset_workspace_id: Union[int, None, UnsetType] = UNSET
    """Identifier of the workspace in which this asset exists, in Preset."""

    preset_workspace_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the workspace in which this asset exists."""

    preset_dashboard_id: Union[int, None, UnsetType] = UNSET
    """Identifier of the dashboard in which this asset exists, in Preset."""

    preset_dashboard_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the dashboard in which this asset exists."""


class PresetDatasetRelationshipAttributes(AssetRelationshipAttributes):
    """PresetDataset-specific relationship attributes for nested API format."""

    preset_dashboard: Union[RelatedPresetDashboard, None, UnsetType] = UNSET
    """Dashboard in which this dataset exists."""


class PresetDatasetNested(AssetNested):
    """PresetDataset in nested API format for high-performance serialization."""

    attributes: Union[PresetDatasetAttributes, UnsetType] = UNSET
    relationship_attributes: Union[PresetDatasetRelationshipAttributes, UnsetType] = (
        UNSET
    )
    append_relationship_attributes: Union[
        PresetDatasetRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        PresetDatasetRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _preset_dataset_to_nested(preset_dataset: PresetDataset) -> PresetDatasetNested:
    """Convert flat PresetDataset to nested format."""
    attrs_kwargs = build_attributes_kwargs(preset_dataset, PresetDatasetAttributes)
    attrs = PresetDatasetAttributes(**attrs_kwargs)
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["preset_dashboard"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        preset_dataset, rel_fields, PresetDatasetRelationshipAttributes
    )
    return PresetDatasetNested(
        guid=preset_dataset.guid,
        type_name=preset_dataset.type_name,
        status=preset_dataset.status,
        version=preset_dataset.version,
        create_time=preset_dataset.create_time,
        update_time=preset_dataset.update_time,
        created_by=preset_dataset.created_by,
        updated_by=preset_dataset.updated_by,
        classifications=preset_dataset.classifications,
        classification_names=preset_dataset.classification_names,
        meanings=preset_dataset.meanings,
        labels=preset_dataset.labels,
        business_attributes=preset_dataset.business_attributes,
        custom_attributes=preset_dataset.custom_attributes,
        pending_tasks=preset_dataset.pending_tasks,
        proxy=preset_dataset.proxy,
        is_incomplete=preset_dataset.is_incomplete,
        provenance_type=preset_dataset.provenance_type,
        home_id=preset_dataset.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _preset_dataset_from_nested(nested: PresetDatasetNested) -> PresetDataset:
    """Convert nested format to flat PresetDataset."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else PresetDatasetAttributes()
    )
    rel_fields: list[str] = ["preset_dashboard"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        PresetDatasetRelationshipAttributes,
    )
    kwargs = build_flat_kwargs(
        nested, attrs, merged_rels, AssetNested, PresetDatasetAttributes
    )
    return PresetDataset(**kwargs)


def _preset_dataset_to_nested_bytes(
    preset_dataset: PresetDataset, serde: Serde
) -> bytes:
    """Convert flat PresetDataset to nested JSON bytes."""
    return serde.encode(_preset_dataset_to_nested(preset_dataset))


def _preset_dataset_from_nested_bytes(data: bytes, serde: Serde) -> PresetDataset:
    """Convert nested JSON bytes to flat PresetDataset."""
    nested = serde.decode(data, PresetDatasetNested)
    return _preset_dataset_from_nested(nested)
