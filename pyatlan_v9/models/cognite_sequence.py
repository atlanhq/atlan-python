# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
CogniteSequence asset model with flattened inheritance.

This module provides:
- CogniteSequence: Flat asset class (easy to use)
- CogniteSequenceAttributes: Nested attributes struct (extends AssetAttributes)
- CogniteSequenceNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .cognite_related import RelatedCogniteAsset

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class CogniteSequence(Asset):
    """
    Instance of a Cognite sequence in Atlan.
    """

    # Override type_name with CogniteSequence-specific default
    type_name: Union[str, UnsetType] = "CogniteSequence"

    cognite_asset: Union[RelatedCogniteAsset, None, UnsetType] = UNSET
    """Asset in which this sequence exists."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _cognite_sequence_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "CogniteSequence":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            CogniteSequence instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _cognite_sequence_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class CogniteSequenceAttributes(AssetAttributes):
    """CogniteSequence-specific attributes for nested API format."""

    pass


class CogniteSequenceRelationshipAttributes(AssetRelationshipAttributes):
    """CogniteSequence-specific relationship attributes for nested API format."""

    cognite_asset: Union[RelatedCogniteAsset, None, UnsetType] = UNSET
    """Asset in which this sequence exists."""


class CogniteSequenceNested(AssetNested):
    """CogniteSequence in nested API format for high-performance serialization."""

    attributes: Union[CogniteSequenceAttributes, UnsetType] = UNSET
    relationship_attributes: Union[CogniteSequenceRelationshipAttributes, UnsetType] = (
        UNSET
    )
    append_relationship_attributes: Union[
        CogniteSequenceRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        CogniteSequenceRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _cognite_sequence_to_nested(
    cognite_sequence: CogniteSequence,
) -> CogniteSequenceNested:
    """Convert flat CogniteSequence to nested format."""
    attrs = CogniteSequenceAttributes()
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["cognite_asset"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        cognite_sequence, rel_fields, CogniteSequenceRelationshipAttributes
    )
    return CogniteSequenceNested(
        guid=cognite_sequence.guid,
        type_name=cognite_sequence.type_name,
        status=cognite_sequence.status,
        version=cognite_sequence.version,
        create_time=cognite_sequence.create_time,
        update_time=cognite_sequence.update_time,
        created_by=cognite_sequence.created_by,
        updated_by=cognite_sequence.updated_by,
        classifications=cognite_sequence.classifications,
        classification_names=cognite_sequence.classification_names,
        meanings=cognite_sequence.meanings,
        labels=cognite_sequence.labels,
        business_attributes=cognite_sequence.business_attributes,
        custom_attributes=cognite_sequence.custom_attributes,
        pending_tasks=cognite_sequence.pending_tasks,
        proxy=cognite_sequence.proxy,
        is_incomplete=cognite_sequence.is_incomplete,
        provenance_type=cognite_sequence.provenance_type,
        home_id=cognite_sequence.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _cognite_sequence_from_nested(nested: CogniteSequenceNested) -> CogniteSequence:
    """Convert nested format to flat CogniteSequence."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else CogniteSequenceAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["cognite_asset"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        CogniteSequenceRelationshipAttributes,
    )
    return CogniteSequence(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _cognite_sequence_to_nested_bytes(
    cognite_sequence: CogniteSequence, serde: Serde
) -> bytes:
    """Convert flat CogniteSequence to nested JSON bytes."""
    return serde.encode(_cognite_sequence_to_nested(cognite_sequence))


def _cognite_sequence_from_nested_bytes(data: bytes, serde: Serde) -> CogniteSequence:
    """Convert nested JSON bytes to flat CogniteSequence."""
    nested = serde.decode(data, CogniteSequenceNested)
    return _cognite_sequence_from_nested(nested)
