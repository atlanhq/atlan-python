# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
AnaplanDimension asset model with flattened inheritance.

This module provides:
- AnaplanDimension: Flat asset class (easy to use)
- AnaplanDimensionAttributes: Nested attributes struct (extends AssetAttributes)
- AnaplanDimensionNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset
from pyatlan_v9.utils import init_guid, validate_required_fields

from .anaplan_related import (
    RelatedAnaplanLineItem,
    RelatedAnaplanModel,
    RelatedAnaplanView,
)
from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class AnaplanDimension(Asset):
    """
    Instances of an AnaplanDimension in Atlan.
    """

    # Override type_name with AnaplanDimension-specific default
    type_name: Union[str, UnsetType] = "AnaplanDimension"

    anaplan_workspace_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AnaplanWorkspace asset that contains this asset (AnaplanModel and everything under its hierarchy)."""

    anaplan_workspace_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the AnaplanWorkspace asset that contains this asset (AnaplanModel and everything under its hierarchy)."""

    anaplan_model_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AnaplanModel asset that contains this asset (AnaplanModule and everything under its hierarchy)."""

    anaplan_model_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the AnaplanModel asset that contains this asset (AnaplanModule and everything under its hierarchy)."""

    anaplan_module_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AnaplanModule asset that contains this asset (AnaplanLineItem, AnaplanList, AnaplanView and everything under their hierarchy)."""

    anaplan_module_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the AnaplanModule asset that contains this asset (AnaplanLineItem, AnaplanList, AnaplanView and everything under their hierarchy)."""

    anaplan_source_id: Union[str, None, UnsetType] = UNSET
    """Id/Guid of the Anaplan asset in the source system."""

    anaplan_model: Union[RelatedAnaplanModel, None, UnsetType] = UNSET
    """Model containing the dimension."""

    anaplan_line_items: Union[list[RelatedAnaplanLineItem], None, UnsetType] = UNSET
    """Line items related to the dimension."""

    anaplan_row_views: Union[list[RelatedAnaplanView], None, UnsetType] = UNSET
    """Views related to the row dimension."""

    anaplan_column_views: Union[list[RelatedAnaplanView], None, UnsetType] = UNSET
    """Views related to the column dimension."""

    anaplan_page_views: Union[list[RelatedAnaplanView], None, UnsetType] = UNSET
    """Views related to the page dimension."""

    @classmethod
    @init_guid
    def creator(cls, *, name: str, model_qualified_name: str) -> "AnaplanDimension":
        """Create a new AnaplanDimension asset."""
        validate_required_fields(
            ["name", "model_qualified_name"], [name, model_qualified_name]
        )
        fields = model_qualified_name.split("/")
        connection_qualified_name = (
            "/".join(fields[:3]) if len(fields) >= 3 else model_qualified_name
        )
        connector_name = fields[1] if len(fields) > 1 else None
        workspace_qualified_name = "/".join(fields[:4]) if len(fields) >= 4 else UNSET
        workspace_name = fields[3] if len(fields) > 3 else UNSET
        model_name = fields[4] if len(fields) > 4 else UNSET
        return cls(
            name=name,
            qualified_name=f"{model_qualified_name}/{name}",
            connection_qualified_name=connection_qualified_name,
            connector_name=connector_name,
            anaplan_workspace_qualified_name=workspace_qualified_name,
            anaplan_workspace_name=workspace_name,
            anaplan_model_qualified_name=model_qualified_name,
            anaplan_model_name=model_name,
        )

    @classmethod
    def updater(cls, *, qualified_name: str, name: str) -> "AnaplanDimension":
        """Create an AnaplanDimension instance for update operations."""
        validate_required_fields(["qualified_name", "name"], [qualified_name, name])
        return cls(qualified_name=qualified_name, name=name)

    def trim_to_required(self) -> "AnaplanDimension":
        """Return only fields required for update operations."""
        return AnaplanDimension.updater(
            qualified_name=self.qualified_name, name=self.name
        )

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _anaplan_dimension_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "AnaplanDimension":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            AnaplanDimension instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _anaplan_dimension_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class AnaplanDimensionAttributes(AssetAttributes):
    """AnaplanDimension-specific attributes for nested API format."""

    anaplan_workspace_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AnaplanWorkspace asset that contains this asset (AnaplanModel and everything under its hierarchy)."""

    anaplan_workspace_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the AnaplanWorkspace asset that contains this asset (AnaplanModel and everything under its hierarchy)."""

    anaplan_model_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AnaplanModel asset that contains this asset (AnaplanModule and everything under its hierarchy)."""

    anaplan_model_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the AnaplanModel asset that contains this asset (AnaplanModule and everything under its hierarchy)."""

    anaplan_module_qualified_name: Union[str, None, UnsetType] = UNSET
    """Unique name of the AnaplanModule asset that contains this asset (AnaplanLineItem, AnaplanList, AnaplanView and everything under their hierarchy)."""

    anaplan_module_name: Union[str, None, UnsetType] = UNSET
    """Simple name of the AnaplanModule asset that contains this asset (AnaplanLineItem, AnaplanList, AnaplanView and everything under their hierarchy)."""

    anaplan_source_id: Union[str, None, UnsetType] = UNSET
    """Id/Guid of the Anaplan asset in the source system."""


class AnaplanDimensionRelationshipAttributes(AssetRelationshipAttributes):
    """AnaplanDimension-specific relationship attributes for nested API format."""

    anaplan_model: Union[RelatedAnaplanModel, None, UnsetType] = UNSET
    """Model containing the dimension."""

    anaplan_line_items: Union[list[RelatedAnaplanLineItem], None, UnsetType] = UNSET
    """Line items related to the dimension."""

    anaplan_row_views: Union[list[RelatedAnaplanView], None, UnsetType] = UNSET
    """Views related to the row dimension."""

    anaplan_column_views: Union[list[RelatedAnaplanView], None, UnsetType] = UNSET
    """Views related to the column dimension."""

    anaplan_page_views: Union[list[RelatedAnaplanView], None, UnsetType] = UNSET
    """Views related to the page dimension."""


class AnaplanDimensionNested(AssetNested):
    """AnaplanDimension in nested API format for high-performance serialization."""

    attributes: Union[AnaplanDimensionAttributes, UnsetType] = UNSET
    relationship_attributes: Union[
        AnaplanDimensionRelationshipAttributes, UnsetType
    ] = UNSET
    append_relationship_attributes: Union[
        AnaplanDimensionRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        AnaplanDimensionRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _anaplan_dimension_to_nested(
    anaplan_dimension: AnaplanDimension,
) -> AnaplanDimensionNested:
    """Convert flat AnaplanDimension to nested format."""
    attrs = AnaplanDimensionAttributes(
        anaplan_workspace_qualified_name=anaplan_dimension.anaplan_workspace_qualified_name,
        anaplan_workspace_name=anaplan_dimension.anaplan_workspace_name,
        anaplan_model_qualified_name=anaplan_dimension.anaplan_model_qualified_name,
        anaplan_model_name=anaplan_dimension.anaplan_model_name,
        anaplan_module_qualified_name=anaplan_dimension.anaplan_module_qualified_name,
        anaplan_module_name=anaplan_dimension.anaplan_module_name,
        anaplan_source_id=anaplan_dimension.anaplan_source_id,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = [
        "anaplan_model",
        "anaplan_line_items",
        "anaplan_row_views",
        "anaplan_column_views",
        "anaplan_page_views",
    ]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        anaplan_dimension, rel_fields, AnaplanDimensionRelationshipAttributes
    )
    return AnaplanDimensionNested(
        guid=anaplan_dimension.guid,
        type_name=anaplan_dimension.type_name,
        status=anaplan_dimension.status,
        version=anaplan_dimension.version,
        create_time=anaplan_dimension.create_time,
        update_time=anaplan_dimension.update_time,
        created_by=anaplan_dimension.created_by,
        updated_by=anaplan_dimension.updated_by,
        classifications=anaplan_dimension.classifications,
        classification_names=anaplan_dimension.classification_names,
        meanings=anaplan_dimension.meanings,
        labels=anaplan_dimension.labels,
        business_attributes=anaplan_dimension.business_attributes,
        custom_attributes=anaplan_dimension.custom_attributes,
        pending_tasks=anaplan_dimension.pending_tasks,
        proxy=anaplan_dimension.proxy,
        is_incomplete=anaplan_dimension.is_incomplete,
        provenance_type=anaplan_dimension.provenance_type,
        home_id=anaplan_dimension.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _anaplan_dimension_from_nested(nested: AnaplanDimensionNested) -> AnaplanDimension:
    """Convert nested format to flat AnaplanDimension."""
    attrs = (
        nested.attributes
        if nested.attributes is not UNSET
        else AnaplanDimensionAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = [
        "anaplan_model",
        "anaplan_line_items",
        "anaplan_row_views",
        "anaplan_column_views",
        "anaplan_page_views",
    ]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        AnaplanDimensionRelationshipAttributes,
    )
    return AnaplanDimension(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        anaplan_workspace_qualified_name=attrs.anaplan_workspace_qualified_name,
        anaplan_workspace_name=attrs.anaplan_workspace_name,
        anaplan_model_qualified_name=attrs.anaplan_model_qualified_name,
        anaplan_model_name=attrs.anaplan_model_name,
        anaplan_module_qualified_name=attrs.anaplan_module_qualified_name,
        anaplan_module_name=attrs.anaplan_module_name,
        anaplan_source_id=attrs.anaplan_source_id,
        # Merged relationship attributes
        **merged_rels,
    )


def _anaplan_dimension_to_nested_bytes(
    anaplan_dimension: AnaplanDimension, serde: Serde
) -> bytes:
    """Convert flat AnaplanDimension to nested JSON bytes."""
    return serde.encode(_anaplan_dimension_to_nested(anaplan_dimension))


def _anaplan_dimension_from_nested_bytes(data: bytes, serde: Serde) -> AnaplanDimension:
    """Convert nested JSON bytes to flat AnaplanDimension."""
    nested = serde.decode(data, AnaplanDimensionNested)
    return _anaplan_dimension_from_nested(nested)
