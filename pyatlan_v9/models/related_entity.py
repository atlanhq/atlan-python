# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
RelatedEntity base class for relationship attribute references.

This module provides the base class for all Related{Type} classes used to
reference related entities in relationship attributes. RelatedEntity contains
the minimal set of fields needed to identify and reference another entity.
"""

from __future__ import annotations

from enum import Enum
from typing import Any, Union

import msgspec
from msgspec import UNSET, UnsetType


class SaveSemantic(str, Enum):
    """
    Save semantic for relationship operations.

    Controls how relationship attributes are handled during save operations.
    """

    REPLACE = "REPLACE"
    """Replace all existing relationships with the new values."""

    APPEND = "APPEND"
    """Add to existing relationships without removing any."""

    REMOVE = "REMOVE"
    """Remove the specified relationships."""


class RelatedEntity(msgspec.Struct, kw_only=True, omit_defaults=True, rename="camel"):
    """
    Base class for related entity references in relationship attributes.

    This class contains the minimal fields needed to reference another entity
    in a relationship. Specific Related{Type} classes extend this with
    type-specific attributes.
    """

    # Core identity
    guid: Union[str, UnsetType] = UNSET
    """Globally unique identifier for the related entity."""

    type_name: Union[str, UnsetType] = UNSET
    """The type name of the related entity."""

    unique_attributes: Union[dict[str, Any], UnsetType] = UNSET
    """Unique attributes that can identify this entity (e.g., qualifiedName)."""

    # Display and status
    display_text: Union[str, UnsetType] = UNSET
    """Display text for this related entity (e.g., "Annual Recurring Revenue")."""

    entity_status: Union[str, UnsetType] = UNSET
    """Status of the related entity (ACTIVE, DELETED)."""

    # Relationship metadata
    relationship_guid: Union[str, UnsetType] = UNSET
    """Globally unique identifier for the relationship instance."""

    relationship_status: Union[str, UnsetType] = UNSET
    """Status of the relationship (ACTIVE, DELETED)."""

    relationship_type: Union[str, UnsetType] = UNSET
    """Type name of the relationship (e.g., "AtlasGlossaryRelatedTerm")."""

    # Save semantic (not serialized to JSON, used internally)
    semantic: Union[SaveSemantic, UnsetType] = UNSET
    """The save semantic for this relationship (REPLACE, APPEND, REMOVE)."""
