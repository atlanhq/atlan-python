# Auto-generated by PythonMsgspecRenderer.pkl - DO NOT EDIT
# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Atlan Pte. Ltd.

"""
Collection asset model with flattened inheritance.

This module provides:
- Collection: Flat asset class (easy to use)
- CollectionAttributes: Nested attributes struct (extends AssetAttributes)
- CollectionNested: Nested API format struct
"""

from __future__ import annotations

from typing import Union

from msgspec import UNSET, UnsetType

from pyatlan_v9.conversion_utils import categorize_relationships, merge_relationships
from pyatlan_v9.serde import Serde, get_serde
from pyatlan_v9.transform import register_asset

from .asset import Asset, AssetAttributes, AssetNested, AssetRelationshipAttributes
from .namespace_related import RelatedFolder

# =============================================================================
# FLAT ASSET CLASS
# =============================================================================


@register_asset
class Collection(Asset):
    """
    Instance of a query collection in Atlan.
    """

    # Override type_name with Collection-specific default
    type_name: Union[str, UnsetType] = "Collection"

    icon: Union[str, None, UnsetType] = UNSET
    """Image used to represent this collection."""

    icon_type: Union[str, None, UnsetType] = UNSET
    """Type of image used to represent the collection (for example, an emoji)."""

    children_folders: Union[list[RelatedFolder], None, UnsetType] = UNSET
    """Folders that exist within this namespace."""

    # =========================================================================
    # Optimized Serialization Methods (override Asset base class)
    # =========================================================================

    def to_json(self, nested: bool = True, serde: Serde | None = None) -> str:
        """
        Convert to JSON string using optimized nested struct serialization.

        Args:
            nested: If True (default), use nested API format. If False, use flat format.
            serde: Optional Serde instance for encoder reuse. Uses shared singleton if None.

        Returns:
            JSON string representation
        """
        if serde is None:
            serde = get_serde()
        if nested:
            return _collection_to_nested_bytes(self, serde).decode("utf-8")
        else:
            return serde.encode(self).decode("utf-8")

    @staticmethod
    def from_json(
        json_data: Union[str, bytes], serde: Serde | None = None
    ) -> "Collection":
        """
        Create from JSON string or bytes using optimized nested struct deserialization.

        Args:
            json_data: JSON string or bytes to deserialize
            serde: Optional Serde instance for decoder reuse. Uses shared singleton if None.

        Returns:
            Collection instance
        """
        if isinstance(json_data, str):
            json_data = json_data.encode("utf-8")
        if serde is None:
            serde = get_serde()
        return _collection_from_nested_bytes(json_data, serde)


# =============================================================================
# NESTED FORMAT CLASSES
# =============================================================================


class CollectionAttributes(AssetAttributes):
    """Collection-specific attributes for nested API format."""

    icon: Union[str, None, UnsetType] = UNSET
    """Image used to represent this collection."""

    icon_type: Union[str, None, UnsetType] = UNSET
    """Type of image used to represent the collection (for example, an emoji)."""


class CollectionRelationshipAttributes(AssetRelationshipAttributes):
    """Collection-specific relationship attributes for nested API format."""

    children_folders: Union[list[RelatedFolder], None, UnsetType] = UNSET
    """Folders that exist within this namespace."""


class CollectionNested(AssetNested):
    """Collection in nested API format for high-performance serialization."""

    attributes: Union[CollectionAttributes, UnsetType] = UNSET
    relationship_attributes: Union[CollectionRelationshipAttributes, UnsetType] = UNSET
    append_relationship_attributes: Union[
        CollectionRelationshipAttributes, UnsetType
    ] = UNSET
    remove_relationship_attributes: Union[
        CollectionRelationshipAttributes, UnsetType
    ] = UNSET


# =============================================================================
# CONVERSION FUNCTIONS
# =============================================================================


def _collection_to_nested(collection: Collection) -> CollectionNested:
    """Convert flat Collection to nested format."""
    attrs = CollectionAttributes(
        icon=collection.icon,
        icon_type=collection.icon_type,
    )
    # Categorize relationships by save semantic (REPLACE, APPEND, REMOVE)
    rel_fields: list[str] = ["children_folders"]
    replace_rels, append_rels, remove_rels = categorize_relationships(
        collection, rel_fields, CollectionRelationshipAttributes
    )
    return CollectionNested(
        guid=collection.guid,
        type_name=collection.type_name,
        status=collection.status,
        version=collection.version,
        create_time=collection.create_time,
        update_time=collection.update_time,
        created_by=collection.created_by,
        updated_by=collection.updated_by,
        classifications=collection.classifications,
        classification_names=collection.classification_names,
        meanings=collection.meanings,
        labels=collection.labels,
        business_attributes=collection.business_attributes,
        custom_attributes=collection.custom_attributes,
        pending_tasks=collection.pending_tasks,
        proxy=collection.proxy,
        is_incomplete=collection.is_incomplete,
        provenance_type=collection.provenance_type,
        home_id=collection.home_id,
        attributes=attrs,
        relationship_attributes=replace_rels,
        append_relationship_attributes=append_rels,
        remove_relationship_attributes=remove_rels,
    )


def _collection_from_nested(nested: CollectionNested) -> Collection:
    """Convert nested format to flat Collection."""
    attrs = (
        nested.attributes if nested.attributes is not UNSET else CollectionAttributes()
    )
    # Merge relationships from all three buckets
    rel_fields: list[str] = ["children_folders"]
    merged_rels = merge_relationships(
        nested.relationship_attributes,
        nested.append_relationship_attributes,
        nested.remove_relationship_attributes,
        rel_fields,
        CollectionRelationshipAttributes,
    )
    return Collection(
        guid=nested.guid,
        type_name=nested.type_name,
        status=nested.status,
        version=nested.version,
        create_time=nested.create_time,
        update_time=nested.update_time,
        created_by=nested.created_by,
        updated_by=nested.updated_by,
        classifications=nested.classifications,
        classification_names=nested.classification_names,
        meanings=nested.meanings,
        labels=nested.labels,
        business_attributes=nested.business_attributes,
        custom_attributes=nested.custom_attributes,
        pending_tasks=nested.pending_tasks,
        proxy=nested.proxy,
        is_incomplete=nested.is_incomplete,
        provenance_type=nested.provenance_type,
        home_id=nested.home_id,
        icon=attrs.icon,
        icon_type=attrs.icon_type,
        # Merged relationship attributes
        **merged_rels,
    )


def _collection_to_nested_bytes(collection: Collection, serde: Serde) -> bytes:
    """Convert flat Collection to nested JSON bytes."""
    return serde.encode(_collection_to_nested(collection))


def _collection_from_nested_bytes(data: bytes, serde: Serde) -> Collection:
    """Convert nested JSON bytes to flat Collection."""
    nested = serde.decode(data, CollectionNested)
    return _collection_from_nested(nested)
