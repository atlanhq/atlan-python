# Auto-generated by PythonMsgspecRenderer.pkl
# NOTE: Additional exports (CheckpointStore, compute_content_hash) added manually
"""
PyAtlan V9 - Python SDK for Atlan's Atlas API using msgspec.

This SDK provides type-safe access to Atlan's Atlas API using msgspec Structs
with flattened attributes for a more Pythonic developer experience.

Example:
    from pyatlan_v9.model import Database, Table

    db = Database(
        name="test_database",
        qualified_name="default/snowflake/123/test_database",
    )
    json_str = db.to_json()
"""

__all__ = []
__version__ = "0.1.0"

# Optional imports - these may have additional dependencies
try:
    from pyatlan_v9.checkpoint import (
        BatchContext,
        CheckpointStore,
        checkpoint_exists,
        cleanup_incomplete_checkpoint,
        compute_content_hash,
        copy_checkpoint,
        parse_epoch_value,
        swap_checkpoints,
    )

    __all__.extend(
        [
            "BatchContext",
            "CheckpointStore",
            "checkpoint_exists",
            "cleanup_incomplete_checkpoint",
            "compute_content_hash",
            "copy_checkpoint",
            "parse_epoch_value",
            "swap_checkpoints",
        ]
    )
except ImportError:
    pass  # lmdb not installed
