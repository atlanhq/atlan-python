
    @classmethod
    # @validate_arguments()
    @init_guid
    def create(cls, *, name: str, preset_workspace_qualified_name: str) -> {{ entity_def.name }}:
        validate_required_fields(
            ["name", "preset_workspace_qualified_name"], [name, preset_workspace_qualified_name]
        )
        attributes = {{ entity_def.name }}.Attributes.create(
            name=name, preset_workspace_qualified_name=preset_workspace_qualified_name
        )
        return cls(attributes=attributes)
